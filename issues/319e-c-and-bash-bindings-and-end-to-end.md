# 319e — C and Bash bindings, end-to-end test, docs

## Status
open — planning stub. Full design in parent issue 319.

## Parent issue
Sub-issue of 319. Completes the feature by adding the C and Bash
language-spec bindings for `create_box` / `connect`, an
end-to-end test map under `tests/maps/`, and updates to
`docs/004-ipc-and-threading.md` and `issues/phase-3-progress.md`.

## Depends on

- 319d (the Lua-binding slice — establishes the runtime built-in
  implementation that the other languages also wrap).

## Suggested implementation

To be expanded when picked up.
