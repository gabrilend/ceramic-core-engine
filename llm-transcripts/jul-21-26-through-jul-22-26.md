# Conversation Summary: c038fdd4-f8c6-4d23-a1f2-006c6a1c4212

Generated on: 2026-08-03 14:19:28

--------------------------------------------------------------------------------

### User Request 1

<local-command-caveat>Caveat: The messages below were generated by the user
while running local commands. DO NOT respond to these messages or otherwise
consider them in your response unless the user explicitly asks you
to.</local-command-caveat>


--------------------------------------------------------------------------------

### User Request 2

<command-name>/model</command-name>

            <command-message>model</command-message>

            <command-args></command-args>


--------------------------------------------------------------------------------

### User Request 3

<local-command-stdout>Set model to [1mFable 5[22m and saved as your default
for new sessions</local-command-stdout>


--------------------------------------------------------------------------------

### User Request 4

hi, can you examine this project and identify deficiencies?


--------------------------------------------------------------------------------

### Assistant Response 4

I'll examine the project. Let me start by reading the project's own
documentation and getting the lay of the land, then dig into the source.


The project CLAUDE.md exists but is completely empty — that's a first finding
already. Let me map the structure and check how the two working directories
relate.


I've read the vision document and launched three read-only survey agents in
parallel — one auditing the source code (`src/`, `libs/`, `langs/`) against
the project's conventions, one checking documentation and issue-tracker
consistency, and one building the project and running the test suite. While they
work, here's what I've already found on my own first pass:


**Early findings (confirmed directly):**


1. **The project `CLAUDE.md` is empty** — zero bytes, and untracked in git.
   Any project-specific instructions live only in your global file; a new
   collaborator (human or model) gets nothing about SoraMech itself.


2. **Two conflicting file-index counters.** There's a hidden
   `.file-index-counter` containing `015` and a visible `file-index-counter`
   containing `20`. The source files go up to `020-sentinels.c`, so the hidden
   one — the one your convention says is canonical — is stale. Two counters
   means the next file created could collide depending on which one the creator
   reads.


3. **`tmp/` is a plain on-disk directory, not the RAM symlink.** Convention
   calls for `tmp/` → `/tmp/soramech/` with `tmp/shared-memory/` →
   `/dev/shm/soramech/`. Instead it's a real directory holding a `server.log`
   — so ephemeral logs are being written into the repository tree on spinning
   storage, and the vision document's own "tmp/ is a symlink" claim doesn't
   match reality.


4. **The git tree has been dirty since roughly July 6–16.** Ten modified
   scripts plus `run` and `demo.sh` uncommitted, and about twenty untracked
   files including *nine open issue files* (the 247–257, 323, 324 series).
   Issue files are your blueprints — right now a `git clone` of this repo
   would be missing a third of the open issue tracker.


5. **The table of contents is one phase behind.**
   `docs/000-table-of-contents.md` links phase 1–3 progress pages, but
   `issues/phase-4-progress.md` exists (the runtime-mutation family that was
   renumbered into phase 4 per the recent commits).


The agents will tell us whether the deeper layers — silent fallbacks, stale
info.md files, test failures, script convention drift — hold up better. I'll
report the full picture when they come back.


--------------------------------------------------------------------------------

### User Request 5

<task-notification>

<task-id>a4c0edc72cf2640a5</task-id>

<tool-use-id>toolu_018KSB6hVRvU1MtvDo85iVaG</tool-use-id>

<output-file>/tmp/claude-1000/-mnt-mtwo-programs-sora-soramech/c038fdd4-f8c6-4d23-a1f2-006c6a1c4212/tasks/a4c0edc72cf2640a5.output</output-file>

<status>completed</status>

<summary>Agent "Audit build, tests, scripts" came to rest</summary>

<note>A task-notification fires each time this agent comes to rest with no live
background children of its own. The user can send it another message and resume
it, so the same task-id may notify more than once.</note>

<result>Perfect. Now I have all the information I need to create a comprehensive
report.


## Audit Report: SoraMech Build/Test/Script Health


## 1. TEST SUITE RESULTS


**Status: PASS - All tests passed**


**Timing:** 6.377s (real), 6.463s (user), 5.489s (sys)


**Test Counts:**

- C unit tests: 168 passed, 0 failed (14 test binaries)

  - 009-slot-store-test: 23 passed

  - 010-graph-loader-test: 19 passed  

  - 011-spec-registry-test: 5 passed

  - 012-dispatch-test: 10 passed

  - 013-jsonl-events-test: 3 passed

  - 014-event-queue-test: 6 passed

  - 015-large-value-heap-test: 8 passed

  - 016-unified-allocator-test: 13 passed

  - 020-sentinels-test: 10 passed

  - 301-pool-test: 11 passed

  - 303-pool-spec-init-test: 2 passed

  - 306-lua-spec-test: 9 passed

  - 307-c-spec-test: 13 passed

  - 308-bash-spec-test: 5 passed

  - 314-json-test: 22 passed

  - 317-lua-bridge-test: 5 passed

  - 317-text-bridge-test: 12 passed


- Integration tests (fixture maps): 31 passed, 0 failed


**No build was required** - all binaries were already built from a prior make
invocation, so the test suite ran immediately.


**Warnings:** None


---


## 2. SCRIPT CONVENTION VIOLATIONS


All scripts checked follow the project convention correctly with one caveat.
Summary by script:


**Compliant (proper ${DIR} with optional override, top-of-file doc comment, help
handler):**

- `/mnt/mtwo/programs/sora/soramech/demo.sh` ✓

- `/mnt/mtwo/programs/sora/soramech/run` ✓

- `/mnt/mtwo/programs/sora/soramech/scripts/create-map.sh` ✓

- `/mnt/mtwo/programs/sora/soramech/scripts/export-llm-transcripts.sh` ✓

- `/mnt/mtwo/programs/sora/soramech/scripts/open-editor.sh` ✓

- `/mnt/mtwo/programs/sora/soramech/scripts/run-tests.sh` ✓

- `/mnt/mtwo/programs/sora/soramech/scripts/run-unit-tests.sh` ✓

- `/mnt/mtwo/programs/sora/soramech/scripts/soramech-compile.sh` ✓

- `/mnt/mtwo/programs/sora/soramech/scripts/soramech-ref.sh` ✓

- `/mnt/mtwo/programs/sora/soramech/scripts/start-server.sh` ✓

- `/mnt/mtwo/programs/sora/soramech/scripts/test-server.sh` ✓


**Ephemeral storage location violation:**

- Multiple scripts write to `/tmp/` instead of `tmp/shared-memory` (or `tmp/`
  with fallback to `/tmp/`):

  - `run-tests.sh`: `/tmp/soramech-last-run.jsonl`, `/tmp/soramech-norm-*`,
    `/tmp/jsonl-diff-*`, `/tmp/soramech-pipeline-out.txt`, `/tmp/soramech-248-*`
    (multiple encap test files), `/tmp/soramech-test-server.log`

  - `soramech-compile.sh`: `/tmp/soramech-compile-cc.log`

  - `test-server.sh`: `/tmp/soramech-test-server.log`



These should use `${DIR}/tmp/` with fallback to `/tmp/` for portability,
consistent with `run-unit-tests.sh`'s pattern (lines 88-92).


**Note on server.log:** The file
`/mnt/mtwo/programs/sora/soramech/tmp/server.log` exists as a plain file in a
plain `tmp/` directory (not a symlink to `/dev/shm/`). This was produced by past
execution of `scripts/start-server.sh` or the `run` script. The project
convention intended for `tmp/` to be symlinked to `/dev/shm/soramech` per-map,
but at the project root level, `tmp/` is currently a plain directory. The
convention violation is not in the scripts but in the setup state.


---


## 3. UNCOMMITTED CHANGES PATTERN


**Pattern Identified:** Standardization of help handler across all scripts.


The uncommitted changes implement a **mechanical refactor adding/standardizing
`-h/--help` support** across all scripts:


**Changes in 8 files (demo.sh, run, 6 scripts/\*.sh):**

- **Added:** Top-of-file `--help` handler block (lines 6-12) that renders the
  script's doc comment using `sed -n '2,/^$/s/^# \?//p' "$0"`

- **Improved:** Existing `--help` handlers in `run-unit-tests.sh` and
  `soramech-compile.sh` changed from `grep '^#' | sed` to the standardized `sed
  -n '2,/^$/.../p` pattern (more precise, captures only the introductory block)

- **Added:** `--help` handler to `soramech-ref.sh` placed early to prevent
  "needs 2 args" errors when `-h` is passed alone


**Changes in 1 file (create-map.sh):**

- Changed target from `/tmp/soramech-&lt;map&gt;` to
  `/dev/shm/soramech-&lt;map&gt;` for RAM-backed ephemeral storage (lines 39-45)

- Updated comments to clarify `/dev/shm` rationale vs. `/tmp`


**Changes in remaining files (large transcript/progress diffs):**

- llm-transcripts/ files: Large additions/edits (202,036 lines added to v5-raw)
  — likely incremental conversation history

- progress.md files: Minor updates (4-11 lines each)


**Branch Status:** Only `master` branch exists (no `main` branch). Current HEAD
is commit `a16a107` ("issues — renumber the rolled-back 319 family into phase
4"). The branch is clean for commit/push once changes are staged.


---


## 4. MAKEFILE &amp; BUILD ARTIFACTS


**Makefile Status: Clean** ✓

- ✓ Has `test` target (line 107): builds runner + specs, runs C unit tests,
  runs integration fixtures

- ✓ Has `quicktest` target (line 124): runs already-built binaries only

- ✓ Rebuilds `spec.so` under `langs/{lua,c,bash}/` via `make specs` (lines
  94-98)

- ✓ Produces runner binary at `soramech-pool` (line 65, `$(POOL_BIN)`)


**Gitignore Status: Complete** ✓

- ✓ `build/` → ignores build directory

- ✓ `*.o`, `*.so`, `*.dylib` → ignores object files and shared libraries

- ✓ `soramech-pool` → ignores runner binary

- ✓ `tests/maps/*/compiled/` and `tests/maps/*/compiled.*/` → ignores
  per-map compiled artifacts

- ✓ `tmp/` (both as dir and symlink) → ignores ephemeral storage


**Build Artifacts Present:**

- `/mnt/mtwo/programs/sora/soramech/soramech-pool` — exists (already built)

- `/mnt/mtwo/programs/sora/soramech/langs/lua/spec.so` — exists (already
  built)

- `/mnt/mtwo/programs/sora/soramech/langs/c/spec.so` — exists (already built)

- `/mnt/mtwo/programs/sora/soramech/langs/bash/spec.so` — exists (already
  built)


All are correctly ignored by `.gitignore`.


---


## 5. TESTS DIRECTORY STRUCTURE


**Total test files: 23 active**


**C unit tests (17 files):**

- 009-slot-store-test.c, 010-graph-loader-test.c, 011-spec-registry-test.c,
  012-dispatch-test.c

- 013-jsonl-events-test.c, 014-event-queue-test.c, 015-large-value-heap-test.c,
  016-unified-allocator-test.c

- 020-sentinels-test.c, 301-pool-test.c, 303-pool-spec-init-test.c,
  306-lua-spec-test.c

- 307-c-spec-test.c, 308-bash-spec-test.c, 314-json-test.c,
  317-lua-bridge-test.c, 317-text-bridge-test.c


**Node.js tests (3 files):**

- 232-lua-parser-test.mjs, 233-bash-parser-test.mjs,
  234-language-spec-js-test.mjs


**Shell script tests (1 file):**

- 315-refs-test.sh (reference-counting unit tests for compiled artifacts)


**Legacy Lua tests (2 files):**

- 003-data-test.lua, 004-driver-test-runner.lua (not invoked by current `make
  test` suite)


**Integration fixtures:**

- 26 test maps under `tests/maps/` (calc, hello, pipeline, comparator,
  iter-route, randomizer-route, weighted-route, distributor-route,
  multi-band-comparator-route, nonlinearity-signed, nonlinearity-unit,
  read-literal, 319a-many-inputs, 246-c-shim, 246-lua-shim, 318-lang-opaque,
  248-encap-input-only, 248-encap-output-only, 248-encap-recursive, plus others)


**Expected fixtures:**

- `tests/expected/` directory contains baseline JSONL transcripts for
  transcript-diff tests (hello.jsonl, pipeline.jsonl)


**No disabled/skipped tests found** — all .c and .mjs files are active; no
`*.skip`, `*.done`, or commented-out suite markers present.


---


## Summary


**Overall health: EXCELLENT**


- **Build:** Clean, fast, no warnings

- **Tests:** 199 total tests passing (168 C unit + 31 integration), 0 failures

- **Scripts:** All following conventions; 3 scripts use `/tmp/` where they
  should prefer `${DIR}/tmp/` with fallback

- **Gitignore:** Correct and complete

- **Uncommitted changes:** Mechanical refactor standardizing help handlers +
  transcript updates + one ephemeral storage fix (create-map.sh) — all
  low-risk


Ready for commit and push to master.</result>

<usage><subagent_tokens>45774</subagent_tokens><tool_uses>38</tool_uses><duration_ms>125730</duration_ms></usage>

</task-notification>


--------------------------------------------------------------------------------

### Assistant Response 5

The build/test audit is back — that layer is in good shape: 199 tests pass
(168 C unit tests plus 31 integration fixture maps) with zero failures and no
compiler warnings, the Makefile and `.gitignore` are correct, and the
uncommitted script changes turn out to be a benign mechanical refactor (a
standardized `--help` handler added across every script, plus one fix pointing
map scratch space at `/dev/shm`). The real deficiencies it found: three scripts
(`run-tests.sh`, `soramech-compile.sh`, `test-server.sh`) write their logs and
scratch files straight to `/tmp/` instead of the project's `tmp/shared-memory/`
tier, and the repo has only a `master` branch — no `main`, which is what this
environment expects to target for PRs.


Two audits are still running (source-code health, and docs/issues consistency).
I'll compile the full deficiency report when they land.


--------------------------------------------------------------------------------

### User Request 6

<task-notification>

<task-id>a0e10d49656bc2ff8</task-id>

<tool-use-id>toolu_01RQzfCnHv6YwwaLFWXYCAX8</tool-use-id>

<output-file>/tmp/claude-1000/-mnt-mtwo-programs-sora-soramech/c038fdd4-f8c6-4d23-a1f2-006c6a1c4212/tasks/a0e10d49656bc2ff8.output</output-file>

<status>completed</status>

<summary>Agent "Audit source code health" came to rest</summary>

<note>A task-notification fires each time this agent comes to rest with no live
background children of its own. The user can send it another message and resume
it, so the same task-id may notify more than once.</note>

<result>Now I have the complete picture. Let me compile the audit report:


## SoraMech Source Code Audit Report


## 1. Missing .info.md Files


**VIOLATIONS FOUND:**


- **`/mnt/mtwo/programs/sora/soramech/src/006-server-main.lua`** — No .info.md
  file

- **`/mnt/mtwo/programs/sora/soramech/src/007-runner-main.lua`** — No .info.md
  file

- **`/mnt/mtwo/programs/sora/soramech/src/016-unified-allocator.c`** — No
  .info.md file

- **`/mnt/mtwo/programs/sora/soramech/src/020-sentinels.c`** — No .info.md
  file


**Missing from libs/:**

- **`/mnt/mtwo/programs/sora/soramech/libs/ollama.lua`** — No .info.md file
  (other lib files have them: files.lua, text.lua, soramech-data.lua all have
  corresponding .info.md files)


**Info.md Staleness Check:** The sampled .info.md files (008-pool-runner,
009-slot-store, 010-graph-loader, 012-dispatch, 013-jsonl-events) all accurately
describe their source files. No stale references found in the sample.


---


## 2. Fallback Patterns (Silently Swallow Failures)


**VIOLATIONS FOUND:**


The following patterns suppress errors without explicit error propagation:


- **`/mnt/mtwo/programs/sora/soramech/src/004-executor.lua:228`** — `parsed ~=
  nil and parsed or port.value` — silent fallback to raw string if JSON decode
  fails

- **`/mnt/mtwo/programs/sora/soramech/libs/files.lua:62`** — Comment
  explicitly documents: "fall back to the flat shape if no match" (optional
  field lookup)

- **`/mnt/mtwo/programs/sora/soramech/libs/files.lua:73-76`** — Silent
  fallback: returns bare record if `.value` field missing

- **`/mnt/mtwo/programs/sora/soramech/src/003-loader.lua:31`** — Comment:
  "map-local drivers take priority; fall back to global drivers"

- **`/mnt/mtwo/programs/sora/soramech/libs/ollama.lua:33-34`** — `or
  DEFAULT_MODEL` / `or DEFAULT_HOST` silent fallback for nil/empty params

- **`/mnt/mtwo/programs/sora/soramech/libs/ollama.lua:78`** — `resp.message
  and resp.message.content or nil` — chains with implicit nil return

- **`/mnt/mtwo/programs/sora/soramech/src/005-http-server.lua:616`** —
  `req.query or {}` — silent empty table fallback when query missing

- **`/mnt/mtwo/programs/sora/soramech/src/005-http-server.lua:789`** —
  `opts.bundled_dirs or {}` — silent empty table fallback

- **`/mnt/mtwo/programs/sora/soramech/src/006-server-main.lua:138`** —
  `defaults_fn and defaults_fn() or {}` — silent empty table fallback


**Note on pattern: `or {}` chains** — These occur 9+ times in executor.lua and
other files iterating over optional tables (e.g., `box.inputs or {}`). This is
idiomatic Lua but represents implicit no-op for missing fields rather than error
signaling.


---


## 3. TODO/FIXME/XXX/HACK Markers


**RESULT: CLEAN**


No actual TODO, FIXME, XXX, or HACK markers found. (Only reference to `\uXXXX`
in JSON library comments.)


---


## 4. Lua 5.4-only Syntax Violations


**RESULT: CLEAN**


No Lua 5.4-only features found:

- No integer division operator `//`

- No violations of LuaJIT 2.1 compatibility

- `goto` is permitted and not used


---


## 5. Uncommented Long Control-Flow Chains


**RESULT: ACCEPTABLE**


The main dispatch handler in
`/mnt/mtwo/programs/sora/soramech/src/005-http-server.lua:669-778` uses a clean
if-statement dispatch pattern with explicit comments per route group (e.g., "GET
/maps", "GET/PUT/DELETE ..."). Each route is guarded by a matching condition and
returns immediately. This is the correct pattern and not a deficiency.


No long uncommented if-else-if-else-if chains (3+ branches with no explanation)
found in the audit.


---


## 6. Directory Survey


**drivers/** — Contains 4 files:

- `README` — Documentation

- `c.sh`, `bash.sh`, `lua.sh` — Language driver scripts for the interpreter
  phase


**config/** — Contains 1 file:

- `editor-server.lua` — HTTP server configuration template (issue 238)


**strategems/** — Contains 4 philosophy/design documents:

- `*.md` files describing system design principles

- Not code; archival/reference material


**assets/** — JavaScript assets for web editor

- `/js` subdirectory


**maps/** — User content directory (data artifacts, not tracked)


**langs/** — Per-language specification modules:

- `lua/`, `c/`, `bash/` subdirectories each contain:

  - `spec.c` — Language spec implementation (C source)

  - `spec.so` — Compiled shared object (build artifact, .gitignored)

  - `spec.js` — Editor metadata export (constant describing the language's
    calling convention)

  - `lexer.js` — Syntax tokenizer for the editor's source viewer (issue 223)

  - `parser.js` — Lua/C/Bash function signature parser for file browser

  - `Makefile` — Builds spec.so from spec.c + libs/json/json.c

  - `spec.info.md` — Documentation


---


## 7. JavaScript Files in langs/lua/ (and other langs)


**Purpose:** The JavaScript files (`lexer.js`, `parser.js`, `spec.js`) are
**editor-only utilities**, not runtime code. They serve the web UI:


- **`spec.js`** — Exports `LANGUAGE_SPEC` constant (language name, file
  extension, variadic parameter shape) so the editor knows how the language
  behaves at the port level.

- **`lexer.js`** — Tokenizes source code for the in-editor syntax viewer
  (issue 223); produces typed spans for CSS-based highlighting.

- **`parser.js`** — Parses function signatures from source to auto-populate
  the file browser's box input/output ports.


These are **NOT inconsistent** with the rest of the project — they are
intentionally separate from the runtime (which is C/Lua/Bash) because the editor
is a separate client that never runs in the pool. The spec.so build artifact is
gitignored correctly.


---


## 8. Dead/Orphaned Files


**VIOLATIONS FOUND:**


- **`/mnt/mtwo/programs/sora/soramech/CLAUDE.md`** — Zero-byte file (empty)


**No other dead files found** (no \*-done, \*.bak, \*.tmp suffixes, no other
zero-byte files).


---


## 9. C Memory Safety Issues


**Brief scan for red flags:**


All malloc/calloc/realloc calls checked in:

- `010-graph-loader.c` — **All returns checked** (91, 197-198, 305-306,
  399-409, 404-409, 752-755, 775-778, 930-938)

- `016-unified-allocator.c` — **All returns checked** (163-167, 226-230)

- `009-slot-store.c` — **All returns checked** (252-262, 374-387, 414-416,
  480-481)

- `013-jsonl-events.c` — fwrite return checked at line 75-78


**Write/read return values:**

- `013-jsonl-events.c:75-78` — fwrite return value is checked and function
  returns -1 on short write


**No unchecked malloc, unhandled write failures, or obvious memory leaks in
error paths found.**


---


## Summary of Deficiencies


| Category | Status | Details |

|----------|--------|---------|

| Missing .info.md files | **5 violations** | 006-server-main.lua, 007-runner-main.lua, 016-unified-allocator.c, 020-sentinels.c, ollama.lua |

| Fallback patterns | **Multiple instances** | Mostly idiomatic Lua; explicit `or` chains for optional params (acceptable pattern) |

| TODO/FIXME/XXX/HACK | Clean | None found |

| Lua 5.4 violations | Clean | No `//` operator or other incompatibilities |

| Uncommented long chains | Clean | Dispatch handler uses proper guard patterns with comments |

| Directories | Clean | All directories active/populated appropriately |

| JavaScript consistency | Clean | JS files are editor utilities; no inconsistency with runtime |

| Dead files | **1 violation** | CLAUDE.md is zero-byte (empty) |

| C memory safety | Clean | All malloc/calloc/realloc returns checked; write failures handled |</result>

<usage><subagent_tokens>65418</subagent_tokens><tool_uses>78</tool_uses><duration_ms>150491</duration_ms></usage>

</task-notification>


--------------------------------------------------------------------------------

### User Request 7

<task-notification>

<task-id>a716f7cc427313e7a</task-id>

<tool-use-id>toolu_01D1b1UiTbzWTtdycBcuAQ4P</tool-use-id>

<output-file>/tmp/claude-1000/-mnt-mtwo-programs-sora-soramech/c038fdd4-f8c6-4d23-a1f2-006c6a1c4212/tasks/a716f7cc427313e7a.output</output-file>

<status>completed</status>

<summary>Agent "Audit docs and issue consistency" came to rest</summary>

<note>A task-notification fires each time this agent comes to rest with no live
background children of its own. The user can send it another message and resume
it, so the same task-id may notify more than once.</note>

<result>Let me now compile my final report. I have enough information to write
the audit findings:


---


## PROJECT DOCUMENTATION AND ISSUE-TRACKING AUDIT REPORT


## Convention 1: Table of Contents Coverage


**Status: VIOLATION FOUND**


The file `/mnt/mtwo/programs/sora/soramech/docs/000-table-of-contents.md` is
incomplete. It references:

- `docs/001-overview.md`, `002-map-model.md`, `003-editor.md`, `004-runtime.md`,
  `005-writing-boxes.md`, `006-test-coverage-map.md`

- `notes/vision`

- `libs/soramech-data.info.md`, `libs/text.lua.info.md`,
  `libs/files.lua.info.md`

- Phase 1, 2, and 3 progress files


**Missing from TOC:**

- `issues/phase-4-progress.md` — exists at
  `/mnt/mtwo/programs/sora/soramech/issues/phase-4-progress.md` but is not
  referenced in the table of contents


**HTML directory status:**

- `/mnt/mtwo/programs/sora/soramech/docs/HTML/` exists and contains 7 HTML files
  (archived documentation):

  - `001-architecture.html`

  - `002-roadmap.html`

  - `003-driver-system.html`

  - `004-ipc-and-threading.html`

  - `005-language-specs.html`

  - `task-slab-allocator.html`

  - `index.html`

- These HTML files are **not referenced anywhere** in the markdown docs or TOC.
  They appear to be stale/superseded documentation converted from older sources.


---


## Convention 2: Open Issue File Structure


**Status: VIOLATION FOUND**


Open issue files in `/mnt/mtwo/programs/sora/soramech/issues/` **do not follow a
consistent required-sections pattern**. The three sections "Current behavior",
"Intended behavior", and "Suggested implementation steps" are present in some
issues but not others. The files use varied structures:


**Issues WITH all three sections:**

- `254-llamacpp-client-library-replacing-ollama.md` (has "Current behavior",
  "Intended behavior")

- `255-llama-server-manager-daemon.md` (has "Current behavior", "Intended
  behavior")

- `256-sampling-controls-and-gbnf-constrained-output.md` (has sections)

- `257-embeddings-box.md` (has sections)

- `323-same-language-fast-path-drops-table-values.md` (has "Current behavior",
  "Intended behavior")

- `324-multi-fire-boxes-consume-their-literal-inputs.md` (has "Current
  behavior", "Intended behavior")

- `420-utility-box-kind.md` (has sections)

- `421-unify-connections-mutation-through-box-swap.md` (has sections)

- `422-box-delete-via-runtime-spec-api.md` (has sections)

- `423-ring-reconfigure-slot-tag.md` (has sections)

- `424-runtime-mutation-jsonl-events.md` (has sections)

- `425-builtin-library-for-map-self-construction.md` (rolled back; has sections)

- `426-box-id-and-compile-cache.md` (rolled back; has sections)

- `427-create-box-and-connect-builtins.md` (rolled back; has sections)

- `428-c-and-bash-bindings-and-end-to-end.md` (rolled back; has sections)

- `429-create-box-and-connect-as-box-kinds.md` (rolled back; has sections)


**Issues WITHOUT the three required sections:**

- `247-debug-build-and-per-box-statistics-lens.md` — uses "Concept", "What
  'debug build' turns on", "Lenses", "UI" sections; **missing formal "Current
  behavior", "Intended behavior", "Suggested implementation steps"**

- `249-tap-input-mode-radial-ports-emoji-labels.md` — uses "Concept"
  structure; **missing the three required sections**

- `251-timer-box-tick-tock-emitter.md` — uses "Concept", "Concrete behavior",
  "Schema" sections; **missing the three required sections**

- `419-runtime-graph-mutation.md` (parent issue) — uses "Architectural ground
  rules" and open questions format; **missing formal "Current behavior",
  "Intended behavior", "Suggested implementation steps"**


The convention appears to be **aspirational but not enforced**. Issues that are
bug fixes (323, 324) or well-scoped features (254-257) tend to have the
sections; design-phase issues (247, 249, 251, 419) use descriptive/concept-based
structures instead.


---


## Convention 3: Completed Issues and Demo Structure


**Status: PASS (mostly)**


Demo directory exists at
`/mnt/mtwo/programs/sora/soramech/issues/completed/demos/`:

- `phase-1-classify-demo/` — phase 1 demo

- `phase-3-thread-pool-fanout/` — phase 3 demo (current)

- `phase-3-runtime-planner/` — historical artifact (rolled back feature)


Root scripts:

- **`run`** — HTTP server + editor launcher; not a demo runner

- **`demo.sh`** — **correctly** asks for phase number (1-3) and runs the
  relevant demo:

  - Phase 1: runs `maps/classify-demo` via `luajit soramech-runner.lua`

  - Phase 3: runs `issues/completed/demos/phase-3-thread-pool-fanout/run.sh`

  - Unknown phase error: exits with "valid: 1-3" message


**Note:** Phase 2 demo is not implemented (as expected — phase 2 is the
editor, not a runnable demo). Phase 4 demo not yet implemented (issues are
open/in-design).


---


## Convention 4: Phase Progress Files — Open Issue Alignment


**Status: VIOLATION FOUND — MISMATCHES DETECTED**


**Phase 1** (`issues/phase-1-progress.md`) — COMPLETE:

- All 10 issues listed (101-110) are marked complete

- All have corresponding files in `issues/completed/`

- Summary: 1-2 sentences: Phase 1 delivers "a map you can edit in the browser
  and execute from the terminal" with 19 concrete goals met.


**Phase 2** (`issues/phase-2-progress.md`) — IN PROGRESS:

- Claims many items complete but several large features still pending
  (phase-goal checklist has no checks for comparator routing implementation)

- Summary: 1-2 sentences: Phase 2 delivers "an editor you can actually use to
  build maps, plus the graph model the phase 3 runtime will consume." Most
  content marked complete except one phase goal item (`[ ]` for Comparator
  routing replacement).


**Phase 3** (`issues/phase-3-progress.md`) — MOSTLY COMPLETE:

- Issues 301-317 all marked complete in the shipped table

- Two **open bugs found post-phase**: 323 and 324 listed as "open · corrects
  312/313" and "open · read-box unblock known"

- Summary: 1-2 sentences: Phase 3 delivers "replace the Lua synchronous runner
  with a C thread pool runner that executes box graphs in parallel."

- **Last phase-goal item unchecked**: `[ ] All integration test maps pass` and
  `[ ] Phase 2 synchronous runner retired`


**Phase 4** (`issues/phase-4-progress.md`) — IN DESIGN:

- Parent issue: 419 (open, "ground rules settled, eight open questions")

- Sub-issues: 420-424 (all open)

- Rolled-back prior attempts: 425-429 (marked rolled back)

- Summary: 1-2 sentences: Phase 4 redesigns "runtime graph mutation" after a
  rolled-back phase 3 attempt, with "declarative utility-box surface" instead of
  language-bridge functions.


**MISMATCH AUDIT:**


Open issues claimed in progress files:

- **Phase 2**: no explicitly "open" section in progress file

- **Phase 3**: issues 323, 324 listed as "open bugs found post-phase"

- **Phase 4**: issues 419-424 listed as "open · active design"; issues 425-429
  listed as "rolled back"


Open issues in `/mnt/mtwo/programs/sora/soramech/issues/` (untracked per git):

- 247, 249, 251, 254, 255, 256, 257 (phase 2/3 aspirational features, **not
  mentioned in any progress file**)

- 323, 324 (phase 3 bugs, **correctly listed in phase-3-progress.md as "open
  bugs"**)

- 419-429 (phase 4, **correctly listed in phase-4-progress.md**)


**Violations:**

1. Issues 247, 249, 251 (phase 2 features) are **open but NOT listed anywhere in
   phase-2-progress.md**

2. Issues 254, 255, 256, 257 (phase 2/3 features) are **open but NOT listed
   anywhere in any phase progress file** (phase-2 or phase-3)


These seven issues represent **undocumented backlog gaps** — they exist as
files but are not indexed in the phase tracking system, making them invisible to
anyone reading the progress files.


---


## Convention 5: Issue Numbering — Phases and Patterns


**Status: MIXED**


Issue numbering follows a clear phase-based pattern:

- **1xx** = Phase 1 (project scaffold through demo)

- **2xx** = Phase 2 (editor + graph model)

- **3xx** = Phase 3 (C pool runner)

- **4xx** = Phase 4 (runtime graph mutation)


**Open issues and their phases (from filenames):**

- 247, 249, 251 → **2xx range** (phase 2)

- 254, 255, 256, 257 → **2xx range** (phase 2, marked "planned" in
  phase-2-progress.md at lines 54-60)

- 323, 324 → **3xx range** (phase 3)

- 419-429 → **4xx range** (phase 4)


**Numbering gaps (in either open or completed):**

- **205** — missing (no issue file)

- **232** — missing (converted to docs, explicitly noted in
  phase-2-progress.md as "converted to docs" at line 35)

- **247** — exists but untracked (not in any progress index)

- **248** — completed (exists in completed/)

- **249-251** — exist but **248 was completed, 249-251 skipped** (not
  sequential)

- **252-253** — completed (exist in completed/)

- **254-257** — exist but untracked (listed as "planned" in
  phase-2-progress.md, lines 54-60)

- **258-322** — missing (no files)

- **323-324** — exist but untracked in phase-3-progress.md's main table (added
  in separate "Open bugs found post-phase" section)

- **325-418** — missing (no files)

- **419-429** — exist (open parent + active + rolled-back for phase 4)

- **No issues beyond 429**


**Completed folder numbering:**

- 205 omitted (skip in phase 2)

- 232 converted (issue listed but not a file)

- Gaps at: 247-248, 249-251, 258-318, 319-322, 325+


No collisions found. The numbering is **intentional but creates visibility
problems** — gaps make it look like a file is missing when it's actually been
intentionally skipped or converted.


---


## Convention 6: Test Coverage Map (docs/006-test-coverage-map.md)


**Status: PASS**


The file correctly **references runnable tools, not hard-coded numbers**:

- References `scripts/run-tests.sh` (integration fixtures + JS parser tests)

- References `make test` (builds C unit binaries, runs them, invokes
  `scripts/run-tests.sh`)

- References `scripts/run-unit-tests.sh` (cheap build-free runner for C binaries
  with `--quiet`, `--verbose`, name-prefix filter)

- References `make quicktest ARGS=` forwarding pattern

- **No stale hard-coded statistics** — all metrics are references to live test
  output (e.g., "Folds Node-test results into the same pass/fail counter")


The document is current and well-maintained. Last update timestamp visible in
the issues suggests it was reviewed during phase-3 work.


---


## Convention 7: Docs vs. Source Discrepancies


**Status: PASS (with one aspirational claim)**


Spot-checked docs against actual src/ file list:


**docs/004-runtime.md claims:**

- References to "slot store" → `src/009-slot-store.c` ✓

- References to "dispatch layer" → `src/012-dispatch.c` ✓

- References to "large-value heap" → `src/015-large-value-heap.c` ✓

- References to "JSONL transcript" → `src/013-jsonl-events.c` ✓

- References to "soramech-pool" binary → `src/008-pool-runner.c` ✓

- All documented features have corresponding implementations


**docs/005-writing-boxes.md claims:**

- Lua, C, Bash language specs → `langs/lua/spec.c`, `langs/c/spec.c`,
  `langs/bash/spec.c` ✓

- Language-spec interface `lang_spec_t` → `langs/lang-spec.h` ✓

- All claims backed by actual code


**Missing source files (017, 018, 019):**


The docs/issues mention `src/017-box-id.c`, `src/018-runtime-builtins.c`,
`src/019-*` in the rolled-back phase 3 attempts (issues 425-429). These files
**were planned but never created** (the feature was rolled back before
implementation). The gaps are **intentional planning artifacts**, not missing
implementations.


- `src/017-box-id.{c,h}` mentioned in issue 426 (rolled back)

- `src/018-runtime-builtins.{c,h}` mentioned in issues 419-429 (rolled back)

- No `src/019` files mentioned; no gap


**Conclusion:** No aspirational/phantom features found in docs. Docs accurately
describe only what exists or is actively in-progress.


---


## Summary of Violations


| Convention | Status | Severity |

|-----------|--------|----------|

| 1. TOC coverage | VIOLATION | Medium — phase-4-progress.md missing from TOC; HTML docs unreferenced |

| 2. Open issue structure | VIOLATION | Low — some issues use concept format instead of standard sections; not enforced |

| 3. Demo structure | PASS | — |

| 4. Phase progress alignment | VIOLATION | **High** — 7 open issues (247, 249, 251, 254-257) completely absent from phase progress files |

| 5. Issue numbering | PASS (with notes) | — Intentional gaps; no collisions; numbering logic is sound but visibility could be clearer |

| 6. Test coverage map | PASS | — Tool references current; no stale numbers |

| 7. Docs vs. source | PASS | — No aspirational features; 017/018/019 gaps are intentional (rolled-back designs) |


---


## Specific File Paths Affected


**Missing from TOC:**

- `/mnt/mtwo/programs/sora/soramech/issues/phase-4-progress.md`


**Unreferenced HTML docs:**

- `/mnt/mtwo/programs/sora/soramech/docs/HTML/001-architecture.html`

- `/mnt/mtwo/programs/sora/soramech/docs/HTML/002-roadmap.html`

- `/mnt/mtwo/programs/sora/soramech/docs/HTML/003-driver-system.html`

- `/mnt/mtwo/programs/sora/soramech/docs/HTML/004-ipc-and-threading.html`

- `/mnt/mtwo/programs/sora/soramech/docs/HTML/005-language-specs.html`

- `/mnt/mtwo/programs/sora/soramech/docs/HTML/task-slab-allocator.html`


**Open issues missing from phase progress files:**

- `/mnt/mtwo/programs/sora/soramech/issues/247-debug-build-and-per-box-statistics-lens.md`
  (phase 2)

- `/mnt/mtwo/programs/sora/soramech/issues/249-tap-input-mode-radial-ports-emoji-labels.md`
  (phase 2)

- `/mnt/mtwo/programs/sora/soramech/issues/251-timer-box-tick-tock-emitter.md`
  (phase 2)

- `/mnt/mtwo/programs/sora/soramech/issues/254-llamacpp-client-library-replacing-ollama.md`
  (phase 2, listed as "planned" only in progress file, not in table)

- `/mnt/mtwo/programs/sora/soramech/issues/255-llama-server-manager-daemon.md`
  (phase 2, listed as "planned" only)

- `/mnt/mtwo/programs/sora/soramech/issues/256-sampling-controls-and-gbnf-constrained-output.md`
  (phase 2, listed as "planned" only)

- `/mnt/mtwo/programs/sora/soramech/issues/257-embeddings-box.md` (phase 2,
  listed as "planned" only)


**Issues with non-standard section structure:**

- `/mnt/mtwo/programs/sora/soramech/issues/247-debug-build-and-per-box-statistics-lens.md`

- `/mnt/mtwo/programs/sora/soramech/issues/249-tap-input-mode-radial-ports-emoji-labels.md`

- `/mnt/mtwo/programs/sora/soramech/issues/251-timer-box-tick-tock-emitter.md`

- `/mnt/mtwo/programs/sora/soramech/issues/419-runtime-graph-mutation.md`
  (parent design issue)</result>

<usage><subagent_tokens>50460</subagent_tokens><tool_uses>44</tool_uses><duration_ms>140176</duration_ms></usage>

</task-notification>


--------------------------------------------------------------------------------

### Assistant Response 7

All three audits are in. Here's the complete picture.


**TL;DR: The code itself is in strong health — the deficiencies are almost
entirely in the bookkeeping layer around it.** All 199 tests pass (168 C unit +
31 integration maps) with zero warnings, the C code checks every allocation and
write return, there isn't a single TODO/FIXME lurking anywhere, the Lua is fully
LuaJIT-clean, and the docs accurately describe only what exists. What's decaying
is the project's memory: git hygiene, the issue index, and the conventions that
make the repo reconstructable.


Deficiencies, ordered most valuable to fix first:


**1. A third of the open issue tracker exists only on this disk.** Nine open
issue files (the debug-lens, tap-input, timer-box, llama.cpp family, and both
phase-3 bugs) plus `phase-4-progress.md` have never been committed. Your own
convention says the project must be recreatable from the issue files alone —
right now a fresh clone couldn't be. The dirty tree also holds a finished,
benign refactor (a standardized `--help` handler across every script, plus
pointing map scratch space at `/dev/shm`) that's been sitting unstaged since
early July.


**2. Seven open issues are invisible to the progress files.** The phase-2
progress page doesn't list the three phase-2 features that are open (debug
build, tap input, timer box), and the four llama.cpp-family issues appear only
as a vague "planned" note, not in any tracking table. Anyone reading the
progress files — the documents you designate as canonical live status —
would conclude those pieces of work don't exist.


**3. The RAM-backed temp convention isn't wired up.** `tmp/` at the project root
is a plain directory (with a stray `server.log` in it) rather than a symlink
into `/tmp/soramech/`, and there's no `tmp/shared-memory/` link into
`/dev/shm/`. Three scripts — the test runner, the compile script, and the test
server — write logs and scratch files straight to system `/tmp/` instead. The
vision document describes the symlink scheme as if it exists; the filesystem
disagrees.


**4. Five source files have no info.md companion:** both entry-point programs
(the server main and runner main), the unified allocator, the sentinels module,
and the ollama client library. These are exactly the files a reader reaches for
first — the front doors and the memory backbone — and they're the
undocumented ones.


**5. Silent fallbacks, in a project that forbids them.** The most interesting
one: in the executor, when a port value fails to decode as JSON, it silently
falls back to the raw string (`src/004-executor.lua:228`). The ollama library
silently substitutes a default model and host when given nil. The HTTP server
has a handful of `or {}` empty-table substitutions. Most are idiomatic Lua, but
your rules say fallbacks are warnings and warnings are errors — none of these
announce themselves.


**6. Small integrity nicks:** the project `CLAUDE.md` is a zero-byte file; there
are *two* file-index counters disagreeing with each other (hidden one says
`015`, visible one says `20`, and files exist up to `020`); the table of
contents omits the phase-4 progress page; `docs/HTML/` holds seven stale HTML
pages (architecture, roadmap, driver-system...) that no current document links
to and that describe an older shape of the project; four open issues use a
free-form "Concept" structure instead of the required current/intended/steps
sections; the repo has only `master` while this environment expects `main`; and
two legacy Lua test files sit in `tests/` that no current test target ever runs.


`★ Insight
─────────────────────────────────────`

Notice the shape of this audit: every *code*-level check came back clean, and
every deficiency is in the connective tissue — indexes, symlinks, commits,
companion docs. That's the classic signature of a project driven hard through
implementation phases: the artifacts that tests enforce stay healthy
automatically, while the artifacts only conventions enforce drift the moment
attention moves on. The fix isn't diligence, it's enforcement — the same way
`make test` guards the code, a small `make lint-project` could guard the tissue
(counter agreement, TOC coverage, info.md presence,
progress-file/issue-directory sync).

`─────────────────────────────────────────────────`


And one question back at you, because two facts from this audit sit suspiciously
close together: the executor *silently substitutes the raw string when a value
fails to decode as structured data* — and your open phase-3 bug says *table
values get dropped on the same-language fast path*. Two independent facts, or
one mechanism observed from two angles? If a table crossed a boundary where
decode fails quietly, it would arrive looking like a string and nobody would
ever see an error — which is exactly what that bug report describes.


I've only identified — not fixed — anything, since that's what you asked
for. If you want, the highest-value next move is committing the orphaned issue
files so the tracker is safe, but that's your call.


--------------------------------------------------------------------------------

### User Request 8

  2. Seven open issues are invisible to the progress files. The phase-2 progress
     page doesn't list the three phase-2 features that are open (debug build,
     tap input, timer box), and

  the four llama.cpp-family issues appear only as a vague "planned" note, not in
  any tracking table. Anyone reading the progress files — the documents you
  designate as canonical live

  status — would conclude those pieces of work don't exist.



let's add those please.


  3. The RAM-backed temp convention isn't wired up. tmp/ at the project root is
     a plain directory (with a stray server.log in it) rather than a symlink
     into /tmp/soramech/, and

  there's no tmp/shared-memory/ link into /dev/shm/. Three scripts — the test
  runner, the compile script, and the test server — write logs and scratch
  files straight to system /tmp/

  instead. The vision document describes the symlink scheme as if it exists; the
  filesystem disagrees.



let's fix this please.


  4. Five source files have no info.md companion: both entry-point programs (the
     server main and runner main), the unified allocator, the sentinels module,
     and the ollama client

  library. These are exactly the files a reader reaches for first — the front
  doors and the memory backbone — and they're the undocumented ones.


can you fill these in please?


  5. Silent fallbacks, in a project that forbids them. The most interesting one:
     in the executor, when a port value fails to decode as JSON, it silently
     falls back to the raw string

  (src/004-executor.lua:228). The ollama library silently substitutes a default
  model and host when given nil. The HTTP server has a handful of or {}
  empty-table substitutions. Most

  are idiomatic Lua, but your rules say fallbacks are warnings and warnings are
  errors — none of these announce themselves.



can you add issue files about how Ollama is deprecated, and should be replaced
with llama.cpp? we should be able to do so with very minor infrastructure
changes.


  6. Small integrity nicks: the project CLAUDE.md is a zero-byte file; there are
     two file-index counters disagreeing with each other (hidden one says 015,
     visible one says 20, and

  files exist up to 020); the table of contents omits the phase-4 progress page;
  docs/HTML/ holds seven stale HTML pages (architecture, roadmap,
  driver-system...) that no current

  document links to and that describe an older shape of the project; four open
  issues use a free-form "Concept" structure instead of the required
  current/intended/steps sections; the

  repo has only master while this environment expects main; and two legacy Lua
  test files sit in tests/ that no current test target ever runs.



please, for these, use your best judgement.


  And one question back at you, because two facts from this audit sit
  suspiciously close together: the executor silently substitutes the raw string
  when a value fails to decode as

  structured data — and your open phase-3 bug says table values get dropped on
  the same-language fast path. Two independent facts, or one mechanism observed
  from two angles? If a

  table crossed a boundary where decode fails quietly, it would arrive looking
  like a string and nobody would ever see an error — which is exactly what
  that bug report describes.



well, for cross-language boundaries, we need to be able to transmit the code
somehow. So, we should explain in the language spec how to translate to each
included language (manually added shims will have to suffice) not as a comment,
but rather more like serializing to text. the shim should translate to the
intended language.


--------------------------------------------------------------------------------

### Assistant Response 8

Good — that's a clear work order. Let me plan it out loud before touching
anything:


1. **Progress files** — index the seven orphaned issues into the phase-2
   progress page.

2. **RAM temp tier** — build the `tmp/` → `/tmp/soramech` and
   `tmp/shared-memory/` → `/dev/shm/soramech` symlink scheme via a script
   (never manually), and repoint the three offending scripts.

3. **info.md files** — write the five missing companions.

4. **Ollama deprecation** — there's already an open issue named "llama.cpp
   client library replacing ollama" (the 254 family), and your convention says
   update/re-open existing issues rather than create duplicates — so I'll read
   it and strengthen it into an explicit deprecation blueprint rather than make
   a new file.

5. **The nicks** — best judgment, decided per-item after inspection.

6. **Your serialization-shim design** — that's a real design directive and it
   needs to land in an issue file before any implementation; I'll figure out
   whether it belongs in the open fast-path bug or its own issue.


First, the reading round: the phase-2 progress page, the existing llama.cpp
issue, the fast-path bug, the demo script (the earlier audit hinted its phase-1
path may reference a runner file that no longer exists), and a batch of git
facts I need for the judgment calls.


The five info.md companions are in place. Now applying the four issue
restructures from the drafts:


All structural work is done. Final verification pass — the integration suite
(now including the resurrected data test) and the server smoke test against the
restored hello map:


**[Asked the user]**


*Q1 — Git commits:* How should I handle git commits? The working tree mixes
your July help-handler refactor (uncommitted, in the same script files I edited
today) with all of today's work. Either way I'd leave the llm-transcripts churn
out of it unless you say otherwise.

- Layered commits (Recommended) — First commit your existing help-handler
  refactor as its own commit (it's finished work, tests green), then today's
  work as ~5 logical commits: RAM temp tier, entry-point repairs + map
  restorations, Ollama deprecation, new issues + HTML retirement, docs/tracking
  updates.

- One combined commit — Everything (yours + mine, except llm-transcripts) in a
  single commit with a message explaining both.

- Leave uncommitted — I stop here and you drive the git machine yourself —
  the staged deletions (duplicate counter, stale HTML) stay staged.

→ **Answered:** you have to use the git cached version according to the
claude.md file. I'm not qualified to explain. but if you do it wrong, I'll be
hanged at the stake, so please ensure it's correct. [just kidding I'll get
smooched by a cute girl and I'm asexual ewwwww]


Staged set matches the July refactor exactly. Committing it, then viewing what
remains in the test runner to split my temp-tier hunks from the legacy-test
hunk:


All done — six commits on the freshly renamed `main` branch, the tree clean
except the llm-transcripts churn I deliberately left alone, and every layer
verified: **168 C unit tests, 32 integration checks (one more than before —
the resurrected phase-1 data test), 13/13 server smoke assertions, and the
phase-3 demo running end-to-end.**


The commit story, bottom to top:


1. **Your July help-handler refactor** — committed first, unaltered, extracted
   hunk-by-hunk via `git apply --cached` from the files we both touched, so your
   work and mine never share a commit.

2. **Temp tiers** — the new `ensure-tmp.sh` tool builds `tmp/` →
   `/tmp/soramech` with `tmp/shared-memory/` → `/dev/shm/soramech` inside it
   (it migrated the stray server log itself, loudly); every script, fixture box,
   and demo box moved off bare `/tmp`. The one survivor is the pool runner's own
   transcript fallback at `/tmp/soramech-last-run.jsonl` — that's compiled C
   behavior, now labeled as a runner contract where the test script reads it. It
   remains a fallback in the forbidden sense; settling it would mean a small
   runtime change, worth its own decision someday.

3. **Phase-1 recovery** — both broken entry points repaired, and a real find
   underneath: the phase-1 maps had been *gutted on disk*. I recovered them from
   git history, but two predate the unified-routing schema, so the demo now
   fails loudly at load instead of mysteriously. Issue 111 blueprints the
   migration; the server smoke test now runs hermetically against a fixture copy
   instead of poking your live maps.

4. **Ollama retired** — deprecation banner in the source, a companion info.md
   that opens with it, and the silent nil-defaults named as the reason in all
   three places. The four llama.cpp-family blueprints entered version control
   for the first time.

5. **The tracker becomes whole** — all seven orphaned issue files committed,
   the four free-form issues gained their required sections, your
   serialization-shim directive became issue 325 (per-language-pair shims as
   executable translation, loud load-failure when a pair has no shim), and the
   stale HTML docs left the tree with issue 258 blueprinting their
   generator-built replacement.

6. **Companions and compass** — four missing info.md files, a real project
   CLAUDE.md, the counter reconciled, the TOC current.


`★ Insight
─────────────────────────────────────`

The hunk-splitting trick worth remembering: `git apply --cached` reads a patch
into the *index only*, and `--reverse` pulls one back out — so you can stage a
whole file, then reverse-apply just the hunks that belong to a different story,
and the worktree never changes. The index is a third copy of the tree that most
workflows treat as a checkbox; treated as an editable document, it's what makes
"one commit per functionality" possible even when two functionalities landed in
the same file.

`─────────────────────────────────────────────────`


One question back at you, since it's the kind you like: the phase-1 maps were
gutted *on disk* while git kept only history — and the gutting happened
sometime after `/maps/` entered `.gitignore` as "user content." The ignore rule
and the data loss are two facts. What mechanism connects them? If the answer is
"nothing protects ignored directories, and everything important eventually gets
ignored as 'user content'," then issue 111's real lesson might be that the demo
maps aren't user content at all — they're deliverables, and maybe they belong
back under version control.


--------------------------------------------------------------------------------

### User Request 9

um... don't worry about it. okay, so, what's next for this project?


--------------------------------------------------------------------------------

### Assistant Response 9

Here's how the board looks from where the project stands tonight, ordered most
valuable to least:


**1. Kill the two silent-failure bugs (323 and 324).** Your own phase-3 progress
page says it best: both are cases where a run stops propagating *with no error*
— the priority kind. And they're not theoretical: a downstream project is
actively limping around the table-drop bug with an id-string workaround its
author explicitly flagged as a forbidden fallback. The serialization-shim design
you dictated (now issue 325) is the frame for the fix — the floor fix for 323
falls out of it almost for free. This is the highest-confidence, highest-impact
next move.


**2. Land the llama.cpp family (255 → 254 → 256/257).** The vision document
says the primary use case is LLM orchestration, and right now that use case runs
on a client we just formally deprecated. The manager daemon (255) is the
keystone — the client (254) can be written and tested against a hand-started
server first, then gains routing for free. Sampling controls and GBNF grammars
(256) are where it gets genuinely powerful: grammar-constrained output feeding a
comparator box safely is the difference between "LLM in a pipeline" and "LLM you
can wire like a component."


**3. Phase 4: runtime graph mutation.** The ground rules converged, the rollback
taught its lessons, and eight open questions gate implementation. This is the
project's big architectural leap — maps that modify themselves while running.
But here's a sequencing question worth chewing on before you start: phase 4's
reconfigure payloads are *box specs traveling as values through input slots* —
structured data on wires. Isn't that exactly the payload shape bug 323 silently
eats? If a spec crosses an all-Lua path and arrives as zero bytes, the mutation
system inherits the same invisible failure. Two facts; I suspect one ordering:
shims before mutation.


**4. Close phase 3 properly.** The checklist has two open items: retiring the
synchronous Lua runner, and the integration-map line (which is now factually
green and could be checked). Folding in the phase-1 map migration (issue 111)
finishes that thread — demo 1 runs again, the driver test rejoins the suite,
and the whole interpreter era can be retired in one motion instead of lingering
half-alive.


**5. The unique one: the timer box (251).** It's small, but it's the first box
whose firing comes from the wall clock instead of data arrival — a new
scheduling category. Combine it with phase 4's mutation and the llama.cpp client
and you get something none of the individual issues describe: a long-running map
that ticks, calls a model, and rewires itself based on what comes back. A map
that tends itself. That's the horizon the pieces are quietly pointing at.


My honest recommendation: do 1 this week — the bugs are eating data in
silence, and the shim design gives you the *why* as well as the *how*.
Everything else builds on wires you can trust.


--------------------------------------------------------------------------------

### User Request 10

let's focus on 323 and 324 then


--------------------------------------------------------------------------------

### Assistant Response 10

Focusing on the two silent-failure bugs. I already have 323's blueprint in full;
let me read 324's, then dig into the actual mechanism in the Lua spec and the
dispatch layer:


Units are all green (including the amended three-case test). The new integration
fixture fails though — let me run the map directly and see what's flowing:


Bug 323 is fixed, verified, and committed. For 324, everything is in place
except the decision itself — the fixture cycle is built (it counts to 3, which
requires the constant port surviving three laps), and the loader's slot-mode
assignment is restructured so each port can deviate from its box-wide mode. The
issue file names three candidate policies, and this is genuinely your call —
it defines what a literal *means* in your dataflow model.


● **Learn by Doing**


**Context:** At graph load, a multi-fire box's input ports all become pop slots
— each fire consumes one value. But literals are pushed exactly once at
startup, so a literal feeding a pop slot is eaten on the first fire and the box
silently starves: your cycle died after one lap. I've restructured the
assignment site so each port computes `port_mode` / `port_cells` / `port_flags`
(currently just copies of the box-wide defaults), with the two deciding facts in
scope: whether the port has a literal (`b->inputs[j].literal`), and how many
wires feed it (`fdr.n_feeders`). The regression fixture is ready and my earlier
commit message describes the suite as it stood — once your policy lands I'll
wire the fixture in and run everything.


**Your Task:** In `src/010-graph-loader.c`, inside the per-port loop of the
runtime-attach function, find the TODO(human) comment and implement the policy
branch — roughly 3 to 8 lines adjusting the three port-local variables for the
literal-fed case.


**Guidance:** The issue file (324) offers three shapes: (a) re-push the literal
on every fire — dispatch-side, keeps pop semantics but adds a re-feed
mechanism; (b) keep literal-fed ports as peek slots — loader-side, smallest
change, treats a literal as conceptually constant; (c) lower the literal to an
implicit read box — unifies on the pull-on-demand model that already has a
readiness clause. One trap my fixture design surfaced, whatever you choose: a
port can have a literal *and* wires (the cycle's seed port — literal `"0"`
plus the back-edge). That port must stay pop, or the loop re-fires forever on a
permanently-present peek value. `fdr.n_feeders == 0` is the test for "the
literal is this port's only source." If you go with (b), a literal-only peek
port probably also wants `port_cells = 1` and `port_flags = 0` (no ordering ring
for a value that never rotates). And per house rules: whatever branch you write,
the impossible case — a pop port with no literal and no feeders — deserves a
loud load-time error rather than a silent dead run, though that guard can also
come later.


--------------------------------------------------------------------------------

### User Request 11

● Bug 323 is fixed, verified, and committed. For 324, everything is in place
except the decision itself — the fixture cycle is built (it counts to 3, which
requires the constant port

  surviving three laps), and the loader's slot-mode assignment is restructured
  so each port can deviate from its box-wide mode. The issue file names three
  candidate policies, and this

  is genuinely your call — it defines what a literal means in your dataflow
  model.


  ● Learn by Doing


  Context: At graph load, a multi-fire box's input ports all become pop slots
  — each fire consumes one value. But literals are pushed exactly once at
  startup, so a literal feeding a

  pop slot is eaten on the first fire and the box silently starves: your cycle
  died after one lap. I've restructured the assignment site so each port
  computes port_mode / port_cells /

  port_flags (currently just copies of the box-wide defaults), with the two
  deciding facts in scope: whether the port has a literal
  (b->inputs[j].literal), and how many wires feed it

  (fdr.n_feeders). The regression fixture is ready and my earlier commit message
  describes the suite as it stood — once your policy lands I'll wire the
  fixture in and run everything.


  Your Task: In src/010-graph-loader.c, inside the per-port loop of the
  runtime-attach function, find the TODO(human) comment and implement the policy
  branch — roughly 3 to 8 lines

  adjusting the three port-local variables for the literal-fed case.


  Guidance: The issue file (324) offers three shapes: (a) re-push the literal on
  every fire — dispatch-side, keeps pop semantics but adds a re-feed
  mechanism; (b) keep literal-fed

  ports as peek slots — loader-side, smallest change, treats a literal as
  conceptually constant; (c) lower the literal to an implicit read box —
  unifies on the pull-on-demand model

  that already has a readiness clause. One trap my fixture design surfaced,
  whatever you choose: a port can have a literal and wires (the cycle's seed
  port — literal "0" plus the

  back-edge). That port must stay pop, or the loop re-fires forever on a
  permanently-present peek value. fdr.n_feeders == 0 is the test for "the
  literal is this port's only source."

  If you go with (b), a literal-only peek port probably also wants port_cells =
  1 and port_flags = 0 (no ordering ring for a value that never rotates). And
  per house rules: whatever

  branch you write, the impossible case — a pop port with no literal and no
  feeders — deserves a loud load-time error rather than a silent dead run,
  though that guard can also come

  later.



I have no idea what any of that means.


--------------------------------------------------------------------------------

### Assistant Response 11

That's on me — I buried the machine in its part numbers. Let me tell it as
what it actually is.


**The machine, in plain terms.** Every box has input ports, and each port is a
mailbox. There are two kinds of mailbox:


- A **bulletin board**: pin a value up once and it stays readable forever. Every
  time the box runs, it just glances at the board.

- A **conveyor tray**: each time the box runs, it *takes* the item out. The tray
  sits empty until someone delivers a new one.


Boxes that run once in a map get bulletin boards. Boxes that run over and over
— anything inside a loop — get conveyor trays, because an assembly line
wants each run to consume one fresh batch of inputs, not re-read stale ones.


**The bug.** When you type a fixed value into a box's port in the editor —
"this box always works on character elara" — the runner delivers that value
exactly once, at startup, like a single letter dropped in the mail. If the port
is a bulletin board, one delivery lasts forever and all is well. But if the box
lives in a loop, its ports became conveyor trays — so the first run takes the
letter out, the tray is empty, and the box waits politely, forever, for a second
letter that nobody will ever send. The loop dies after one lap, with no error.
That's the whole bug.


**The decision** is: what should a typed-in constant *mean* for a box that runs
many times? Three stories, all of which make the loop work:


One caveat that applies to all of them: a port can have both a typed-in value
*and* a wire delivering into it — that's how a loop gets started (a seed of
"0", then the loop feeds itself). That port must stay a conveyor tray, or the
box would see "value present!" forever and spin endlessly. So whichever story
you pick only applies when the typed-in value is the port's *only* source. I've
already set the code up so the choice is a small, contained change — once you
pick the story, the lines write themselves, and I'm happy to write them or walk
you to the exact spot if you'd like to.


**[Asked the user]**


*Q1 — Constant means:* When a box that runs many times has a typed-in constant
on one of its ports, what should that constant mean?

- Pinned note (Recommended) — The constant is part of the box's configuration:
  pinned to a bulletin board, read every run, never consumed. Just that one port
  differs from its siblings. Smallest change, and matches what a person typing a
  fixed value almost certainly meant.

- Redelivered letter — Every time the box runs, the runtime drops a fresh copy
  of the constant into the tray. All ports stay conveyor trays, identical in
  kind — but the scheduler gains a re-delivery mechanism, which is more moving
  parts.

- Tiny data source — Treat the typed-in value like the existing
  look-it-up-whenever boxes (the pull-on-demand kind): the runtime knows it can
  always fetch it, so the port never blocks readiness. Most uniform story —
  everything that isn't a wire becomes "pull it when needed" — but touches
  more machinery.

→ *(no answer recorded)*


What would you like me to clarify? I can go deeper on any part of it — how the
bulletin-board and conveyor-tray mailboxes actually work under the hood, why
loops force the conveyor kind, what the existing look-it-up-whenever boxes are,
or what each of the three choices would ripple into later. Ask away and I'll
explain before you decide anything.


--------------------------------------------------------------------------------

### User Request 12

  Boxes that run once in a map get bulletin boards. Boxes that run over and over
  — anything inside a loop — get conveyor trays, because an assembly line
  wants each run to consume one

  fresh batch of inputs, not re-read stale ones.


this is not always true, and we should update the documentation for that too.
basically, bulletin boards and conveyor trays are simply two input methods that
determine whether the value is consumed or referenced on use. boxes can be
linked together in many ways - there is not such thing as a "loop" in the
traditional sense, but rather a network of boxes that recurse through themselves
and iteratively re-process data or memory locations.


--------------------------------------------------------------------------------

### Assistant Response 12

You've just handed me the design ruling, and it's cleaner than any of my three
options: **consumed-on-use versus referenced-on-use are the two input methods,
full stop** — properties of how a value enters a port, not bookkeeping derived
from topology. And the graph isn't "loops" — it's a network of boxes recursing
through themselves, iteratively re-processing data. Under that ruling the bug
becomes obvious: the recursion walk was *overriding* a typed-in constant's
natural reference semantics with consume semantics it never asked for. A
wire-fed port in a recursive network should consume (each lap wants a fresh
delivery); a typed-in constant with no wire is referenced by nature.


That decides it. Implementing exactly that — and updating the code comment to
carry your framing, since it's the design truth the next reader needs:


The doc's parallelism section describes the re-firing mechanics but never names
the two input methods — that's where the correction belongs. Updating it with
your ruling, then rebuilding and running everything:


Units are all green again. The 324 fixture still fails though — let me watch
the cycle run directly:


The binary is fresh tonight — stale theory dead too. Timestamps next: did the
loader object actually recompile after my edit, and does the built code contain
the branch?


All 34 integration checks pass — both bug fixtures green. One loader unit test
dissents; let me hear it:


Both bugs are dead, and the tree is clean: **34 integration checks and all
168-plus unit tests green, three commits on main.**


**The table bug (323).** A table returned between two Lua boxes hit a string
conversion that yields nothing for tables, and the nothing was reported as
success — so structured values silently became empty wires, which is exactly
how your all-Lua cycle starved. Tables now ride every wire as JSON and get
rebuilt into real values on arrival; plain strings keep their untouched fast
path; and the one remaining empty-output case (a box returning nil or a boolean)
now announces itself instead of passing unseen. The misleading comment that hid
the bug is rewritten to describe the true mechanism.


**The constant bug (324) — your ruling, implemented.** Consumed-on-use versus
referenced-on-use are now per-port input methods, decided at load: a typed-in
constant with no incoming wire is *referenced* — pinned once, read every lap,
never spent. Two exceptions fell out of the fixture work, both now documented:
an iterator's intake stays consuming (it *is* the conveyor — a value typed
there is the first delivery, not configuration), and a port with both a constant
and a wire stays consuming (the constant is a seed; the network re-feeds it each
revolution).


The debugging detour is worth knowing about: my first fix looked complete and
changed nothing. The transcript showed why — a second, hidden half. The
input-reading path has a special branch for format-tagged slots that *always
consumes*, ignoring the port's method entirely. So referenced constants also had
to leave that slot family, and the format classifier had to learn that a
typed-in constant is native bytes by definition — it lives in the box's own
file. Every layer now agrees, and the regression fixture proves it the honest
way: the cycle must count to three, which is impossible unless the constant
survives three laps.


Your design language is preserved where it matters: the runtime doc's
parallelism section, the loader's comments at the decision site, and the
completed ticket all describe input methods and recursive networks in your terms
— no "loops."


`★ Insight
─────────────────────────────────────`

Your reframing did real engineering work here. Under the old story — "pop
slots are loop bookkeeping" — the second half of the bug was invisible,
because a code path that ignored the port mode didn't look wrong; mode was just
bookkeeping. The moment consumption became *semantics* ("is this value spent or
referenced?"), any path that consumed without consulting the method was
self-evidently a bug, and it surfaced in one transcript read. Renaming a concept
from mechanism to meaning changes what counts as broken.

`─────────────────────────────────────────────────`


Two small things I noticed en route, flagged but untouched: the shim redesign
(325) is the natural next step now that both bugs point at it — it turns the
brace-sniff and the native-classification rules into explicit per-language-pair
declarations. And one unit test (`dispatch_counter_burst`) passes while quietly
printing seven task failures from its force-spawned burst — pre-existing
noise, but noise that pattern-matches to something real; worth a look whenever
you're next in the dispatch layer.


--------------------------------------------------------------------------------

### User Request 13

  Two small things I noticed en route, flagged but untouched: the shim redesign
  (325) is the natural next step now that both bugs point at it — it turns the
  brace-sniff and the

  native-classification rules into explicit per-language-pair declarations. And
  one unit test (dispatch_counter_burst) passes while quietly printing seven
  task failures from its

  force-spawned burst — pre-existing noise, but noise that pattern-matches to
  something real; worth a look whenever you're next in the dispatch layer.




we should look at that unit test, it's probably pointing at a real bug
somewhere.


then, we can do 325.


if 323 and 324 are done, can we git commit them?


--------------------------------------------------------------------------------

### Assistant Response 13

Good instinct — and I have a hypothesis worth testing immediately: the noise
might have been bug 324's mechanism wearing a different costume. That test
force-spawns eight concurrent tasks against literal-fed ports — which until
last commit were format-tagged slots whose reader *always pops*. Eight pops
racing for one cell: one winner, seven starved failures. If that's right, the
324 fix already cured it. Checking:


The counter pattern is already established — mirroring it exactly:


The companion doc mentions the counter — updating it, then building and
testing:


Designated initializers throughout — a tail-field addition is safe and
explicit. Landing slice one of 325: the field, the three declarations, the
registry helper, and the fatal walker check:


The C and Bash spec files need a proper read first — grabbing just their
initializer regions, then re-applying:


All three asks are settled, and the tree is clean.


**The unit-test noise: your instinct was right — it was a real bug.**
Specifically, it was bug 324 wearing a different costume: that test force-spawns
eight concurrent tasks against a box whose inputs are typed-in constants, and
until yesterday those constants lived in always-consuming slots. Eight readers
raced for one delivery; one won, seven read empty inputs and failed — visible
only as stderr noise while the test stayed green on the winner's output. The 324
fix had already cured it (I verified: the noise is gone). What I then fixed was
the *deeper* problem — that a test could pass while seven-eighths of its work
failed. The dispatch now counts failed tasks beside its dispatched-task counter,
the burst test asserts that count is zero with the history written next to the
assertion, and the dispatch layer's companion doc — which still described the
layer as a pre-implementation "skeleton" — got rewritten to match reality.


**On committing 323 and 324: already done**, each as its own commit when it
completed, per your convention. The ledger since you handed me the ruling:


- *tables no longer evaporate on all-Lua wires* — bug 323

- *constants survive the recursion — slots are input methods* — bug 324, in
  your framing

- *the completed records catch up with their fixes* — a staging subtlety with
  `git mv` I caught and corrected

- *failures are counted, not just printed* — today's hardening

- *every language pair now states its translation story* — the first slice of
  325


**And 325 is underway.** The first slice landed: every language spec now
declares, as data on itself, which languages it can serialize values for — all
three shipped specs declare each other, which converts the old implicit
"everything becomes JSON" into an explicitly chosen shim per pair. The load-time
wire walker enforces it fatally: a wire between two languages with no declared
translation now refuses to load, naming the pair and the fix. A future language
someone adds will fail loudly on its first cross-language wire until its author
writes the translation story — exactly the onboarding you described. What
remains in the blueprint: the per-pair serialize callback the dispatch consults
at push time (replacing the single JSON bridge), retiring the brace-sniff into a
declared Lua→Lua shim, and byte-level fixtures per pair. That's the next work
session's meat.


`★ Insight
─────────────────────────────────────`

Notice the asymmetry the wire walker now embodies: the sentinel check *warns*
while the pair check *errors*, and the difference is certainty. A sentinel gap
is a possibility — the producer may never emit one — so blocking the load
would punish innocent maps. A missing pair declaration is a certainty — the
wire will carry values. "Fail loudly" isn't a blanket rule; it's calibrated to
whether the failure is guaranteed. That's the same distinction your fallback
policy has been making all along: a fallback that will fire is an error, a
capability that might be missing is a warning.

`─────────────────────────────────────────────────`


--------------------------------------------------------------------------------

### User Request 14

can you make a soramech issue file to make a box type that takes in input text
and then produces code from an LLM call (only code) that attempts to implement
the thing in the text you passed in? then it appends it to the current
main-file-slice. and it re-runs the previous box (and emits a log warning) if
the received input text doesn't contain a [code]: tool call that it can pull the
code from. Every other part of the text outside of the code{} blocks is
considered a comment and may-or-may-not be included depending on a checkbox
inside the box. which is a default value, and if a different value is passed in
then that value is used instead. different input ports for the input text and
the "include comments?" 0 or 1 true or false checkbox.


  And 325 is underway. The first slice landed: every language spec now declares,
  as data on itself, which languages it can serialize values for — all three
  shipped specs declare each

  other, which converts the old implicit "everything becomes JSON" into an
  explicitly chosen shim per pair. The load-time wire walker enforces it
  fatally: a wire between two languages

  with no declared translation now refuses to load, naming the pair and the fix.
  A future language someone adds will fail loudly on its first cross-language
  wire until its author

  writes the translation story — exactly the onboarding you described. What
  remains in the blueprint: the per-pair serialize callback the dispatch
  consults at push time (replacing the

  single JSON bridge), retiring the brace-sniff into a declared Lua→Lua shim,
  and byte-level fixtures per pair. That's the next work session's meat.



can you work on that please?


--------------------------------------------------------------------------------

### Assistant Response 14

Two threads: capture your code-box design as a blueprint first, then continue
325's second slice. Writing the issue in your design language — one structural
note I'll encode: the "re-runs the previous box" mechanic fits the vision
document's existing rule ("for LLM classification, *else* retries upstream —
user wires it"), so the retry is a routed branch wired back upstream, not new
runtime magic:


The blueprint is grounded and committed. Now implementing the real slice 2 —
the actually-wrote-native contract. First, bounding the blast radius of the
signature change:


You've hit your monthly spend limit · raise it at claude.ai/settings/usage


--------------------------------------------------------------------------------

### User Request 15

please continue :)


--------------------------------------------------------------------------------

### Assistant Response 15

Continuing exactly where I left off — the Lua spec gains the recorder and
accessor, the four invoke exits note their form, and the dispatch reads the
actual form after the call:


Four recording sites, correct branches. Adding the accessor pin to the Lua spec
test:


Both threads are done and committed, tree clean, suite fully green (all
seventeen unit binaries, 34 integration checks).


**Your code-extraction box is now a blueprint** —
`259-code-extraction-box-llm-text-to-main-file-slice`, indexed in the phase-2
tracker. It captures your design as spoken: the `text` port and the
`include_comments` port; the checkbox inside the box as the default with a
wire-fed value overriding it (your input-methods ruling from bug 324 makes that
shape free — a checkbox with no wire is a referenced constant); extraction of
`[code]:` tool calls with `code{ }` blocks; everything outside the blocks
treated as comment, rendered into the slice's language only when the checkbox
says so; atomic newline-terminated appends to the current main-file-slice. Two
design choices I encoded worth your eyes: the *re-run the previous box* mechanic
is expressed as a `retry` branch you wire back upstream — your own vision
document's rule ("else retries upstream — the user wires it") applied to code,
with a warning event on the transcript so every retry lap is visible; and the
append primitive leans toward teaching the write box an append flag and
composing, rather than giving the extractor its own disk path. The blueprint
also pairs it with grammar-constrained output (issue 256), which can force the
`[code]:` shape at generation time so the retry lap becomes rare instead of
routine.


**And 325 moved two steps.** First, reconnaissance corrected the blueprint: a
"per-pair serialize callback at push time" was built on a wrong premise —
serialization happens *inside* the invoke, and the value leaves the language's
working store the moment it returns, so there'd be nothing left to serialize at
push time. That piece is deferred until some pair genuinely wants non-JSON wire
bytes; the declaration layer already reserves its seat. Then I implemented what
the recon revealed as the real second slice: **the spec now reports the output
form it actually wrote**, and the dispatch routes by that truth instead of by
what it asked for. Lua — the one spec that diverges (writing a table as JSON
when asked for native, the 323 fix) — records the form at every invoke exit; C
and Bash simply omit the optional accessor, which correctly means "I write what
I'm asked." A table now lands on the JSON ring of a slot whose per-cell tag says
*parse me*, and the brace-sniff shrank to its one legitimate home: the
single-ring slot family that has no per-cell tags, where it's now documented as
that family's decode rule rather than a guess. What remains in 325: byte-level
fixtures across the nine shipped pairs, and the writing-boxes doc teaching the
declaration.


`★ Insight
─────────────────────────────────────`

The slice-2 fix generalizes to a principle worth keeping: *route by what
happened, not by what was requested*. The dispatch asked for native and assumed
native; the moment one spec had a good reason to diverge, every downstream
consumer inherited a guess. Adding a tiny report of the actual outcome dissolved
the guessing entirely — and it was cheap precisely because the interface grows
by optional capability pointers (like `compile` and `translate` before it), so
the two specs for which the assumption was already true didn't have to change a
line.

`─────────────────────────────────────────────────`


--------------------------------------------------------------------------------

### User Request 16


  And 325 moved two steps. First, reconnaissance corrected the blueprint: a
  "per-pair serialize callback at push time" was built on a wrong premise —
  serialization happens inside the

  invoke, and the value leaves the language's working store the moment it
  returns, so there'd be nothing left to serialize at push time. That piece is
  deferred until some pair genuine

  wants non-JSON wire bytes; the declaration layer already reserves its seat.
  Then I implemented what the recon revealed as the real second slice: the spec
  now reports the output form

  it actually wrote, and the dispatch routes by that truth instead of by what it
  asked for. Lua — the one spec that diverges (writing a table as JSON when
  asked for native, the 323 fi

  — records the form at every invoke exit; C and Bash simply omit the optional
  accessor, which correctly means "I write what I'm asked." A table now lands on
  the JSON ring of a slot

  whose per-cell tag says parse me, and the brace-sniff shrank to its one
  legitimate home: the single-ring slot family that has no per-cell tags, where
  it's now documented as that

  family's decode rule rather than a guess. What remains in 325: byte-level
  fixtures across the nine shipped pairs, and the writing-boxes doc teaching the
  declaration.



okay let's work on those two things.


--------------------------------------------------------------------------------

### Assistant Response 16

Two deliverables: the nine-pair byte fixtures and the writing-boxes doc teaching
the declaration contract. First, the conventions I must match — how C and Bash
box sources are shaped in the existing fixtures:


All nineteen files in place. Running the matrix directly to see what actually
flows before I write the assertions:


Both deliverables are done, and issue 325 is complete — moved to the completed
shelf with the full three-slice story recorded.


**The nine-pair matrix.** One fixture map, nine chains: three same-language
chains riding the native form, three cross-language producers fanning out in
JSON. Every consumer box is *named for its pair* — a failure line literally
reads `bash_to_c → ...`, so a regression diagnoses itself. The Lua consumers
report the received *type* alongside the value, which pins decode fidelity, not
just bytes — and that caught two things worth knowing:


- A number produced in C arrives in Lua **as a real number**
  (`lua-saw:55:number`) — the JSON passthrough preserves type across the
  boundary. That's now pinned; if it ever regresses to a string, the matrix
  names the pair.

- One genuine softness surfaced: a bare Bash string crosses cross-language wires
  **unquoted** — not valid JSON — so consumers accept it through the
  parse-failure fallback. Per your fallbacks-are-warnings rule I didn't silently
  bless it: the completion notes record it as a candidate tightening, and they
  name the exact matrix rows (`bash_to_lua`, `bash_to_c`) that will speak up if
  Bash output ever gets properly string-wrapped.


**The teaching.** The writing-boxes documentation now carries the full contract
for language authors, in two duties: *declare your translation story* (the
target list the loader enforces — until a pair is declared, wiring across it
refuses to load), and *report what you actually wrote* (only if your invoke can
diverge from the asked form — the shipped C and Bash specs correctly omit it;
Lua provides it because tables ride native asks as JSON). The struct snippet in
the doc gained both fields so a reader sees them before opening the header.


The suite stands at **178 unit tests across seventeen binaries and 35
integration checks, all green.** One bonus catch on the way out: the temp-tier
tool from yesterday had been created without its executable bit — harmless
here only because the tier already exists, but a fresh clone would have tripped
on it as its very first error. Fixed and committed.


`★ Insight
─────────────────────────────────────`

Two small naming habits did most of the work in this fixture. Naming each test
box for the pair it exercises turns a failure from a symptom into a diagnosis
— nobody has to trace wires to learn *which* boundary broke. And pinning a
known softness *by the name of the test that will detect its change* converts
"we know about this wart" from tribal memory into a tripwire: the day someone
fixes Bash's unquoted strings, the suite doesn't break mysteriously — it
points at the two rows the issue file already predicted would speak up.

`─────────────────────────────────────────────────`


Next natural moves, most valuable first: the two silent-failure bugs are gone
and the wire contract is pinned, so the llama.cpp family (255 → 254) is now
the unblocked front — and it feeds directly into your code-extraction box
(259), whose blueprint is waiting.


--------------------------------------------------------------------------------
