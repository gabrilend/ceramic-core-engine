# Conversation Summary: 290ee4ef-01c5-4b8a-b553-7b2820f46658

Generated on: 2026-08-22 11:40:53

--------------------------------------------------------------------------------

### User Request 1

hello, can you examine the project and see where we're at? Evaluate any open
issues to see if they're already implemented, and give me a quick roadmap for
how to get to v1.0


--------------------------------------------------------------------------------

### Assistant Response 1

I'll start by getting the lay of the land.


I explored the tree, built it, ran the tests, ran the example, and read all
eight open tickets against what's actually in the code. Here's what I found.


## Where the project is


**The engine is finished and it works.** `make test` builds and passes all 33
test programs. `make example` builds from clean and runs the four-station graph
in the readme. Nothing is broken.


The scale, so the rest of this has a sense of proportion:


| | |

|---|---|

| Engine source (the `src/` tree plus the thread pool in `libs/`) | ~10,200 lines of C and headers |

| Tooling — the generator, the map parser and writer, the site builder, the renumberer | ~5,650 lines |

| Test programs | 33, all passing |

| Completed blueprints | 74 |

| Open tickets | 8 |

| Numbered runtime guarantees | 54 |


Phases one through seven — the thread pool, stations and the push path, the
build-time generator, constants on ports, comparators and iterators, the map
file, and the diagnostics — are all closed. Phase seven has two tickets still
genuinely open and phase eight has three, one of which is partly built.


The most recent stretch of work was substantial: the box registry was dissolved
so a map addresses a box by the file it lives in, a map became something the
engine compiles into construction calls rather than interprets, boxes can be
compiled and placed into a program *while it runs*, and a running program can be
written to disk and picked back up with the values still sitting in its buffers
and each iterator pointing where it had got to.


---


## The eight open tickets, checked against the code


## Already done, or done enough to close


**"The demos after the pull path" is finished by deletion.** It described
repairing three demo programs that used the pull mechanism after that mechanism
was removed. All seven demos were then deleted outright rather than repaired.
There is no subject left. Its analysis of which demonstrations were load-bearing
is still worth keeping, but the ticket has nothing to implement — it can move
to the completed directory as-is.


**"An index is a position in the reading order" is done except for a judgement
call you own.** The rule is stated in the table of contents where a reader
arrives, and the renumbering tool exists and does the whole job — it takes a
desired order, refuses anything missing or duplicated, renames through git so
both names survive in history, rewrites every reference, and stages renames
behind placeholders so two files swapping numbers don't collide. What is left is
not code. It is deciding whether to actually move about fifty files so that
build tools stop sitting between engine files, the generator stops appearing
forty places after the thing it generates, and the station tests move back next
to the station code. The ticket itself says this is very low priority and blocks
nothing.


## Partly built


**The documentation site is standing but half-dressed.** The Lua generator runs
as part of a full build so stale pages cannot ship, walks every documentation
directory downward rather than reading a list, sweeps out any page no source
produces, and emits about seventy cross-linked pages with a table of contents on
every one. Three of the five promised interactive pieces are live — the ring
buffer with a capacity slider, the readiness check that fires on the click
filling the last slot, and the iterator dealing in scrambled order. Missing: the
wirable three-port comparator, the steppable sleep-and-termination race, links
that land mid-page instead of at page tops, and the "look more artistic than a
stylesheet applied to markdown" pass the ticket asks for.


**The browser workbench draws and remembers, and cannot yet hand you a file.**
You can place stations, name them, pick plain/comparator/iterator, type a box
function name, add and remove ports, and drag wires. Several drawings sit side
by side on a shelf in the browser's own storage, saved on every edit, restored
when the browser reopens, and nothing is sent anywhere. Steps four through eight
— validating wires as they are drawn, typing constants into ports, downloading
the map, reading a map back into the canvas, and the token simulator — are
untouched.


**I found a stale blocker there worth telling you about.** The ticket says
getting the map writer into the page is blocked because *this machine has no
WebAssembly linker*. That is no longer true. Zig is installed at `/usr/bin/zig`,
and `zig cc` bundles a WebAssembly linker. I compiled the map parser, the map
writer, and the text helper into a 718 KB WebAssembly module just now to check
— it links clean; the only complaint was that a library has no entry point,
which a stub answers. So the one thing standing between the canvas and a
downloadable map file is buildable today, and there is no need for the
JavaScript reimplementation the ticket spends three sections refusing to create.


## Not started


**The slideshow and the transcript library.** Nothing built. Worth noting: this
ticket says its prerequisite is the generator learning to walk directories
downward, and that landed — so the prerequisite is met. The transcripts are
absent from the site for one concrete reason: the generator's file-finding
command carries an explicit exclusion for the transcript directory. Removing
that one exclusion is where this starts.


**"A program you can watch."** Nothing in the engine emits a trail. I grepped
the source; there is no ring in shared memory, no build flag, nothing.


**"The viewer."** Not started, and correctly so — it waits on the trail
existing.


## One thing that is worse than a ticket


**The project's front door is empty.** `./run-demo` exits with "No demos
directory" and a message explaining that the seven paged demos were deleted and
their replacements are not built yet. The readme tells a visitor that phases one
through seven stand; the launcher tells them there is nothing to run. `make
example` covers this well for a first-minute impression, but the phase-by-phase
demonstration the whole project structure promises is currently absent. That is
the ticket called "the demos you can steer," and only its first step — the
deletion — has been taken.


---


## A roadmap to v1.0


**There is no definition of v1.0 anywhere in the project.** I grepped the
documents, the notes, and the tickets; the roadmap document deliberately
contains no schedule and the phases are described as clusters of functionality
rather than milestones. So the definition below is mine, proposed, and you
should push back on it.


**Proposed: v1.0 is the point where somebody who is not you can build a working
program with this and never have to ask you a question.** That makes the engine
already done and puts every remaining piece of work in the same category — the
approach to the thing, rather than the thing.


By that definition, in order of most value first:


**1. Refill the front door — the steerable panels.** One live control panel
per phase, three regions, levers the reader moves, and a log of what the engine
did about it. This is the single biggest gap between what the project *is* and
what a visitor *sees*, and it is the only remaining item that shows off the
three capabilities that make this engine unusual: writing a value from outside
is an event, the shape can be edited while it runs, and code can arrive after
the program started. Every one of those has no subject until somebody outside
the program acts, which is exactly why the old paged demos deserved deleting.


**2. Get the map writer into the workbench, then finish the round trip.** Build
the parser and writer to WebAssembly with zig, then take steps four through
seven together — validate as you draw, type constants into ports, download the
map plus its layout companion, read one back in. Finishing these makes the
canvas the thing the readme claims it will be: draw a picture, leave with a file
that runs. Right now it is a drawing toy. The wasm step is the keystone and it
just became cheap.


**3. Finish the documentation site.** The two missing interactive pieces, the
mid-page links, and the artistic pass. The steppable termination race is the one
that earns its keep most — that mechanism is genuinely hard to hold in your
head and trivial to see once animated.


**4. Put the transcripts on the site as a book.** Delete one exclusion from the
generator's file-finding command, then page them, fold the machine-generated
blocks, and cross-link them against the issues. This is the smallest remaining
piece and the one your own conventions lean on hardest — the transcripts are
where the arguments survive, and nothing currently reaches them.


**5. The slideshow.** Live-drawn screens, one argument rather than a table of
contents, sharing the simulator built in step two rather than a second one. This
is the most expensive item on the list and the last thing a v1.0 needs, because
it serves a reader who has not decided to care yet.


Below the line, and I'd argue explicitly out of v1.0: **the trail and the
viewer.** Watching a running program from another process is a genuinely good
feature and it is the only remaining item that requires touching the engine —
a ring in shared memory, a build flag, and a rule that a slow reader loses
events rather than holding the program up. Everything above it is about making
the finished engine reachable. Adding a feature to the engine in the same
release you are trying to make the engine approachable is how the
approachability slips.


**Also worth folding into whichever release: the renumbering.** Not because
anything breaks, but because "filenames read in one order across the whole
project" is a claim the readme makes, and four places currently break it.


---


## Open questions I need you to answer


These surfaced during the read-through and I have not decided any of them for
you.


1. **Is my definition of v1.0 the right one?** The alternative reading is that
   v1.0 means the engine is stable and the tooling is explicitly early — which
   would make v1.0 something you could tag this afternoon after moving one
   superseded ticket to completed. That is a legitimate position and it changes
   the whole ordering above.


2. **Does the trail-and-viewer work belong inside v1.0 or after it?** I put it
   after. If you'd rather the engine be feature-complete at 1.0 and never
   touched again, it belongs in.


3. **Do you want the renumbering done, and if so do you want to argue about the
   order first?** The ticket says the sequence should be argued about before
   fifty files move, and it lists four places where the order stops reading
   well. I can write the proposed order out for you to mark up, but I'm not
   going to move anything on my own judgement.


4. **The workbench's WebAssembly blocker — do you want that ticket corrected
   now?** It records a machine limitation that no longer holds, and its note is
   the reason step two is described as blocked. A ticket that says "blocked"
   when it isn't stays blocked by nobody rechecking.


I have not changed anything. The only file modified in the tree is today's
transcript, which was already that way when I started.


--------------------------------------------------------------------------------
