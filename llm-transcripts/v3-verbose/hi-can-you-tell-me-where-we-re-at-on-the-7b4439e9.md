<!--
Conversation transcript — generated by
scripts/export-llm-transcripts.lua.

id          : 7b4439e9-9a4e-40f1-99fb-94c47dfb29ca
slug        : hi-can-you-tell-me-where-we-re-at-on-the
first prompt: hi, can you tell me where we're at on the issue files? check the most recently edited, and tell me what parts we implemented without updating the issue ticket.
first ts    : 2026-05-22T05:11:51.402Z
last ts     : 2026-05-22T06:30:54.736Z
entry count : 59
verbosity   : v3 verbose
-->

---

# Transcript

## User

hi, can you tell me where we're at on the issue files? check the most recently edited, and tell me what parts we implemented without updating the issue ticket.

## Assistant

_Tools:_

- `Bash: ls -lt /mnt/mtwo/programs/sora/soramech/issues/*.md | head -20`

### Tool result

```
-rw-r--r-- 1 ritz ritz 23650 May 21 19:26 /mnt/mtwo/programs/sora/soramech/issues/307-c-language-spec.md
-rw-r--r-- 1 ritz ritz 23688 May 21 19:16 /mnt/mtwo/programs/sora/soramech/issues/312-same-language-wire-fast-path.md
-rw-r--r-- 1 ritz ritz  9291 May 21 18:49 /mnt/mtwo/programs/sora/soramech/issues/313-research-whole-program-same-language-merge.md
-rw-r--r-- 1 ritz ritz 11501 May 21 17:11 /mnt/mtwo/programs/sora/soramech/issues/249-tap-input-mode-radial-ports-emoji-labels.md
-rw-r--r-- 1 ritz ritz 19789 May 21 17:09 /mnt/mtwo/programs/sora/soramech/issues/248-encapsulated-map-as-box.md
-rw-r--r-- 1 ritz ritz 13340 May 21 15:48 /mnt/mtwo/programs/sora/soramech/issues/318-sentinel-primitives-in-cross-language-json.md
-rw-r--r-- 1 ritz ritz  7150 May 21 15:40 /mnt/mtwo/programs/sora/soramech/issues/247-debug-build-and-per-box-statistics-lens.md
-rw-r--r-- 1 ritz ritz 14305 May 21 15:40 /mnt/mtwo/programs/sora/soramech/issues/246-custom-translation-specs-per-port.md
-rw-r--r-- 1 ritz ritz  5047 May 21 12:28 /mnt/mtwo/programs/sora/soramech/issues/245-wire-color-by-cross-language-classification.md
-rw-r--r-- 1 ritz ritz  5199 May 21 03:06 /mnt/mtwo/programs/sora/soramech/issues/phase-3-progress.md
-rw-r--r-- 1 ritz ritz 22477 May 20 22:14 /mnt/mtwo/programs/sora/soramech/issues/304-task-dispatch-layer.md
-rw-r--r-- 1 ritz ritz  7775 May 20 22:09 /mnt/mtwo/programs/sora/soramech/issues/244-data-box-pull-on-demand.md
-rw-r--r-- 1 ritz ritz  3608 May 20 15:00 /mnt/mtwo/programs/sora/soramech/issues/240-routing-kind-randomizer.md
-rw-r--r-- 1 ritz ritz  4154 May 20 15:00 /mnt/mtwo/programs/sora/soramech/issues/241-routing-kind-weighted.md
-rw-r--r-- 1 ritz ritz  3470 May 20 15:00 /mnt/mtwo/programs/sora/soramech/issues/242-routing-kind-distributor.md
-rw-r--r-- 1 ritz ritz  4860 May 20 15:00 /mnt/mtwo/programs/sora/soramech/issues/243-routing-multi-band-comparator.md
-rw-r--r-- 1 ritz ritz  4596 May 20 15:00 /mnt/mtwo/programs/sora/soramech/issues/phase-2-progress.md
-rw-r--r-- 1 ritz ritz 19231 May 20 13:58 /mnt/mtwo/programs/sora/soramech/issues/317-spec-json-bridge-for-data-boxes.md
-rw-r--r-- 1 ritz ritz 12924 May 20 13:58 /mnt/mtwo/programs/sora/soramech/issues/232-test-coverage-map.md
-rw-r--r-- 1 ritz ritz 14917 May 20 13:58 /mnt/mtwo/programs/sora/soramech/issues/311-integration-tests-and-run-output.md
```

### Tool result

```
1	# 307 — C language spec implementation
2	
3	## Status
4	open
5	
6	## Current behavior
7	C boxes today run via `drivers/c.sh`, which compiles the source on
8	demand (mtime-based recompile to a binary), then spawns the binary as
9	a subprocess and reads stdout. Process spawn cost dominates; each call
10	is its own freshly-launched executable. The phase 2 driver compiles
11	to a standalone binary, not a `.so`.
12	
13	## Concept
14	The C language spec implements `lang_spec_t` (issue 303) for C. It is
15	the second reference implementation, and the first one that exercises
16	the `compile` callback. After compile, invocation is in-process via
17	`dlopen` / `dlsym` — the user's compiled function is called directly
18	through a function pointer, with no subprocess and no IPC.
19	
20	This is the "Option 1 — LuaJIT FFI" model from
21	`docs/004-ipc-and-threading.md`, applied to C: same-process call,
22	nanosecond-overhead invocation, native-typed access. The only thing
23	the C spec adds over the Lua spec is the compile step.
24	
25	## Per-worker state
26	
27	`init` returns a small struct holding the dlopen-handle cache:
28	
29	```c
30	typedef struct {
31	    void  *so_handles[MAX_BOXES];   // per-file .so handle, indexed by file id
32	    void  *fn_ptrs   [MAX_BOXES];   // per-(file, fn) cached function pointer
33	    int    n_cached;
34	    // (a hash table could replace these arrays if MAX_BOXES grows large)
35	} c_handle_t;
36	
37	void *c_init(int worker_idx);
38	void  c_teardown(void *handle);    // dlclose every cached handle
39	```
40	
41	Each worker thread has its own cache. The same `.so` will end up
42	loaded once per worker, since `dlopen` with the same path on the same
43	process returns a refcounted handle (the underlying code segment is
44	shared across all callers — the per-worker `dlopen` is just a
45	refcount bump after the first).
46	
47	## Compile callback
48	
49	The C spec's `compile` callback receives the box's metadata in
50	addition to the source and output paths, so it knows the function's
51	signature and can generate the wrapper:
52	
53	```c
54	int c_compile(const char *src_path, const char *out_path, const box_t *box);
55	```
56	
57	(This is a small extension to `lang_spec_t` in issue 303 — `compile`
58	gains a `box_t` pointer. Lua's compile is NULL, so unaffected.)
59	
60	The callback:
61	1. Reads `box->inputs`, `box->returns`, `box->headers`, `box->link_libs`,
62	   `box->cflags` from the box metadata.
63	2. Generates a wrapper `.c` file (see "Wrapper generation" below) in
64	   the map's `compiled/bin/` directory next to where the `.so` will
65	   land.
66	3. Forks and execs the system `cc` with:
67	   - `-shared -fPIC -O2 -Wall` (defaults)
68	   - The user's `cflags`, if any
69	   - The wrapper `.c` and the user's `src_path` (if `ref` is set)
70	   - `-o out_path`
71	   - `-l<lib>` for each entry in `box->link_libs`
72	4. Waits for the compiler, returns 0 on success, 1 on failure.
73	
74	If the resulting `.so` is newer than the user's source and the
75	wrapper, compile is skipped (mtime check, same as phase 2).
76	
77	The compiler is `cc` — the system default. `gcc` on most Linux,
78	`clang` on macOS. No SoraMech-specific compiler dependency.
79	
80	### cflags in box JSON
81	The `cflags` field on the box JSON is a string of additional compiler
82	flags (`-O0 -g`, `-mavx2`, etc.). It is set by the user in the
83	inspector — a "Compile flags" text input shown when a C-language box
84	is selected. The editor saves it as part of the box's JSON; reads
85	preserve the field; saves write it back. Nothing overwrites it.
86	
87	## Box file convention: any C function, no boilerplate
88	
89	**The rule, stated as a rule:** a box author writes code as if
90	SoraMech does not exist. The function they write must be a
91	function their non-SoraMech compiler can compile, their
92	non-SoraMech editor can lint, their non-SoraMech test harness
93	can call. No SoraMech-prefixed macros. No SoraMech-shaped
94	return types. No `#include "soramech.h"`. Nothing in the
95	author's source betrays that the function will ever cross
96	SoraMech's wire.
97	
98	SoraMech bends to the language. The language never bends to
99	SoraMech. This is non-negotiable across every spec; it's the
100	reason for the spec interface in the first place.
101	
102	Users do not write a SoraMech-specific C signature. They reference
103	any C function — whether one they wrote themselves, or one from a
104	library (zlib, OpenCV, libcurl, anything they have headers for) —
105	and the C spec generates a thin wrapper that bridges SoraMech's
106	bytes-in / bytes-out ABI to the function's natural typed C
107	signature.
108	
109	### What the user provides
110	Either:
111	- A `.c` file containing the function, referenced by `ref` in the
112	  box JSON; or
113	- A library function from a header, no `.c` file of their own — the
114	  `ref` field is omitted, the function name and headers are
115	  declared in the box JSON, and the library is named in
116	  `link_libs`.
117	
118	### Box JSON for a user-written function
119	
120	```json
121	{
122	  "id": "classify",
123	  "kind": "call",
124	  "lang": "c",
125	  "ref": "classify.c",
126	  "fn": "classify",
127	  "inputs": [
128	    {"name": "text",   "type": "string"},
129	    {"name": "weight", "type": "int"}
130	  ],
131	  "returns": "int"
132	}
133	```
134	
135	The user's `.c` file is naturally written:
136	
137	```c
138	// maps/<name>/src/classify.c
139	int classify(const char *text, int weight) {
140	    if (text[0] >= '0' && text[0] <= '9') return weight * 1;
141	    return weight * 2;
142	}
143	```
144	
145	No header to include, no macro, no `int classify(SM_BOX_ARGS)`.
146	Just a function with a typed signature.
147	
148	### Box JSON for a library function (no user source)
149	
150	```json
151	{
152	  "id": "compress",
153	  "kind": "call",
154	  "lang": "c",
155	  "fn": "compress2",
156	  "headers": ["<zlib.h>"],
157	  "link_libs": ["z"],
158	  "inputs": [
159	    {"name": "src",   "type": "bytes"},
160	    {"name": "level", "type": "int"}
161	  ],
162	  "returns": "bytes"
163	}
164	```
165	
166	The wrapper includes `<zlib.h>`, calls `compress2`, and the linker
167	pulls in `libz` via `-lz`. No user-written `.c` file.
168	
169	### Wrapper generation
170	
171	The compile callback emits a wrapper `.c` for each C box. The
172	wrapper has a fixed entry-point name (`sm_box_entry`) that the spec
173	always `dlsym`s. It declares the user's function (extern), unpacks
174	SoraMech's input bytes into typed C arguments per the box's `inputs`
175	declaration, calls the user's function, and writes the return value
176	into `sm_out_buf` per the box's `returns` declaration.
177	
178	For `classify` above, the generated wrapper is roughly:
179	
180	```c
181	// generated: classify_wrap.c
182	extern int classify(const char *, int);
183	
184	int sm_box_entry(const void **inputs, const int *sizes, int n,
185	                 void *out_buf, int out_cap, int *out_size) {
186	    const char *text   = (const char *)inputs[0];
187	    int         weight = atoi((const char *)inputs[1]);
188	    int         result = classify(text, weight);
189	    *out_size = snprintf(out_buf, out_cap, "%d", result);
190	    return 0;
191	}
192	```
193	
194	The wrapper is mechanical — same template for every box, varying
195	only in the input-marshalling lines and the return-encoding line.
196	The compile step writes it to `compiled/bin/<box-id>_wrap.c` and
197	compiles `<box-id>_wrap.c` plus the user's source (if any) into a
198	single `.so`.
199	
200	This means the C spec implements typed function calls without
201	libffi (which would do the marshalling at runtime via the C ABI
202	metadata). The same effect is achieved at compile time with
203	straightforward generated code, no runtime dependency.
204	
205	## Input marshalling (in the generated wrapper)
206	
207	The wrapper converts SoraMech's `(bytes, size)` input pairs into the
208	typed C arguments the user's function expects, based on the
209	declared input types in the box JSON:
210	
211	| Declared type | Wrapper action                                                  |
212	|---------------|-----------------------------------------------------------------|
213	| `string`      | `(const char *)inputs[i]`                                       |
214	| `int`         | `atoi((const char *)inputs[i])`                                 |
215	| `long`        | `strtoll(...)`                                                  |
216	| `double`      | `strtod(...)`                                                   |
217	| `bool`        | `strcmp("true", inputs[i]) == 0`                                |
218	| `bytes`       | `(const void *)inputs[i]`, paired with `sizes[i]` for the size  |
219	| `json`        | `cJSON_ParseWithLength(inputs[i], sizes[i])` → `cJSON *`        |
220	
221	The `json` case uses the same vendored cJSON the graph loader uses
222	(issue 305) — one JSON library shared across the runner. When a box
223	input is declared `"type": "json"`, the wrapper passes a `cJSON *`
224	to the user's function. The user navigates the tree with cJSON's
225	API (`cJSON_GetObjectItem`, `cJSON_GetArrayItem`, etc.) and the
226	wrapper frees the parsed tree after the function returns.
227	
228	If a parse fails (a `json` input that is not valid JSON, an `int`
229	input that does not parse as an integer), the wrapper aborts the
230	program with the box id and the input position (per issue 303).
231	
232	## Output encoding (in the generated wrapper)
233	
234	The wrapper takes the user function's return value and serializes
235	it into `sm_out_buf`, based on the declared `returns` type:
236	
237	| Declared `returns` | Wrapper action                                              |
238	|--------------------|-------------------------------------------------------------|
239	| `string`           | the user returns `const char *`; `strcpy` + set `out_size` |
240	| `int`              | `snprintf("%d", ret)`                                       |
241	| `long`             | `snprintf("%lld", ret)`                                     |
242	| `double`           | `snprintf("%.17g", ret)`                                    |
243	| `bool`             | write `"true"` or `"false"`                                 |
244	| `bytes`            | the user returns `(void *, int)` via an out-parameter pair (see below) |
245	| `json`             | the user returns `cJSON *`; `cJSON_PrintUnformatted` → bytes |
246	| `void`             | `*out_size = 0` (zero-byte output)                          |
247	
248	For `bytes` returns, the user function's signature has the form
249	`int my_fn(... const char *out_buf, int out_cap, int *out_size)` —
250	extra parameters that the wrapper passes through from
251	`sm_out_buf` / `sm_out_capacity` / `sm_out_size`. This handles
252	library functions like `compress` that write their output via an
253	out-pointer rather than a return value. The box JSON declares
254	`"returns": "bytes"` and the wrapper threads the buffer through.
255	
256	If the encoded value exceeds `sm_out_capacity`, the wrapper aborts
257	(per issue 303). For boxes whose output capacity is not known at
258	compile time, see "Variable-size outputs" below — the wrapper
259	routes through the large-value heap.
260	
261	## Variable-size outputs
262	
263	For boxes whose output capacity is not known at compile time (issue
264	302's large-value heap): the box JSON declares `output_capacity = 0`
265	and the wrapper allocates from the large-value heap on each call.
266	For `bytes` and `json` returns, the wrapper transparently picks the
267	heap path when the box is variable-size — the user's function is
268	unchanged either way. The handle (pointer + size into the
269	large-value heap) is what ends up in `sm_out_buf`.
270	
271	For string returns, similarly: if the returned string exceeds the
272	declared fixed `output_capacity`, the wrapper falls back to the
273	large-value heap. This decision is made at compile time based on the
274	box JSON, not at runtime.
275	
276	## Thread safety of user code
277	
278	The same `.so` is loaded by every worker thread that may call into
279	it. **Static variables and globals declared in the user's C source
280	are shared mutable state across all workers** unless explicitly
281	made `const`.
282	
283	A note on `static` in C: it does not mean immutable. `static` at
284	file scope means "this symbol has internal linkage, only visible
285	inside this `.c` file." `static` inside a function means "this
286	variable persists across calls." Either way, the variable itself is
287	mutable — `static int counter = 0; counter++;` is perfectly legal
288	and does what it says, just with internal linkage. Immutable in C
289	is `const`.
290	
291	So:
292	- `static int counter = 0;` at file scope — racy, shared across workers.
293	- `static int x = 0;` inside a function body — racy, shared across workers calling that function.
294	- `const int max = 100;` — safe, immutable.
295	- `static const int max = 100;` — safe, immutable.
296	
297	Box functions must be reentrant — no mutable shared state without
298	explicit locking. The C spec does not enforce this; the rule lives
299	in `docs/005-language-specs.md` (to be written) for box authors to
300	read.
301	
302	### Library functions that need global init
303	
304	Many real C libraries require a one-time setup call before any of
305	their functions work — `curl_global_init` for libcurl,
306	`OPENSSL_init_crypto` for OpenSSL, etc. A SoraMech box that calls
307	into such a library cannot just be wired in cold; the library's
308	state has to exist before the call.
309	
310	Two patterns handle this without changing the spec interface:
311	
312	1. **Init box wired as an entry point.** The user creates a small C
313	   box whose function is the library's init call (e.g.
314	   `curl_global_init`). They mark it as an entry box and wire it
315	   such that any box using libcurl is downstream of it. The init
316	   runs once at startup before any consumer fires. Process-global
317	   library state — which is what most C libraries actually use — is
318	   set up by the time anyone needs it. Pairs with a teardown box
319	   wired as a terminal node if needed.
320	
321	2. **Per-worker init via spec extension** (future). For libraries
322	   that need per-worker setup (each thread gets its own handle),
323	   the C spec gains an optional per-box `worker_init_fn` field in
324	   the box JSON. The spec calls that function once per worker
325	   thread during pool startup, before any box invoke. Not in scope
326	   yet — pattern (1) covers the common case.
327	
328	The init-box approach is the recommended starting point. The
329	spec-extension path is the escape hatch when (1) does not fit.
330	
331	A future fallback exists: `dlmopen`, a Linux variant of `dlopen`
332	that loads each library into its own isolated "namespace," giving
333	each worker its own copy of the library's symbols (statics
334	included). Two threads using two different namespaces would not
335	share static state. Less portable than `dlopen` (Linux-only), so
336	not the default — flagged here as the escape hatch if static-
337	variable races become a real problem.
338	
339	## Build
340	
341	```
342	langs/c/
343	    spec.c              ← lang_spec_t implementation
344	    soramech-c.h        ← user-facing header (shipped to maps via compile step)
345	    Makefile            ← builds spec.so
346	    spec.so             ← built artifact (gitignored)
347	```
348	
349	Spec Makefile target:
350	```make
351	spec.so: spec.c
352		$(CC) -shared -fPIC -O2 -Wall -o $@ $< -ldl
353	```
354	
355	Linking against `libdl` for `dlopen` / `dlsym` is the only
356	dependency. No interpreter, no helper library beyond libc.
357	
358	### What `dlopen` and `dlsym` do
359	- `dlopen("path/to/file.so", flags)` loads a shared library at
360	  runtime. The OS loader maps the library's code into the process's
361	  memory and returns a handle. Same idea as Windows'
362	  `LoadLibrary`.
363	- `dlsym(handle, "name")` looks up a symbol — a function or a
364	  variable — by name inside an already-loaded library, and returns
365	  a pointer to it. Same idea as Windows' `GetProcAddress`.
366	
367	Together they are how a program loads and calls code that wasn't
368	linked at compile time. Plugin systems are the classic use case.
369	Here, every C box compiles to its own `.so`; the spec `dlopen`s the
370	`.so` once per worker, `dlsym`s `sm_box_entry` once per file, and
371	calls the resulting function pointer per invocation.
372	
373	The user's C box files are compiled by the `compile` callback at map
374	load time, not by this Makefile. The Makefile here builds only the
375	spec itself.
376	
377	## Error handling
378	
379	- `dlopen` failure (missing `.so`, link errors): print path + `dlerror()`
380	  to stderr, return 1, dispatch layer aborts.
381	- `dlsym` failure (missing function): print file/fn + `dlerror()`,
382	  return 1, abort.
383	- Compile failure: `cc` exit nonzero, print compiler stderr (already
384	  routed there by `cc`), return 1, abort.
385	- User function returns nonzero: propagate that as the spec's return,
386	  abort.
387	
388	No retry, no fallback. Per issue 303.
389	
390	## Open questions
391	
392	(none currently — earlier questions resolved as follows:)
393	
394	- Per-box `cflags`: yes, add the field to the box JSON. The
395	  inspector shows a "Compile flags" text input when a C-language
396	  box is selected (phase 2 inspector work).
397	- Typed signatures: handled via compile-time wrapper generation
398	  (see "Box file convention" above). No libffi runtime dependency.
399	- Static-variable isolation via `dlmopen`: deferred. Current rule
400	  is "C box authors write reentrant code"; if that breaks down in
401	  practice, fall back to per-worker `dlmopen` namespaces (Linux-only).
402	
403	## Suggested implementation sequence
404	
405	1. Set up `langs/c/` directory with a Makefile that builds an empty
406	   `spec.so` exporting a stub `soramech_lang_spec`.
407	2. Implement `init` and `teardown` with the dlopen-handle cache.
408	3. Implement `compile` — fork/exec `cc` with the standard flags.
409	   Smoke test: compile a single hello-world `.c` file.
410	4. Implement `invoke` — dlsym, direct call, propagate return.
411	5. Write `soramech-c.h` with the `SM_BOX_ARGS` macro and the basic
412	   input/output helpers.
413	6. Smoke test: a C box that takes one string input, writes a string
414	   output, runs end-to-end through the pool.
415	7. Add the typed input helpers (`sm_in_int`, `sm_in_double`, etc.).
416	8. Add the typed output helpers.
417	9. Test against `maps/driver-test` (C portions).
418	10. Add variable-size output support (`sm_out_alloc`, `sm_out_handle`).
419	11. Document in `docs/005-language-specs.md` once it exists, with a
420	    note on thread safety.
421	
422	## Relevant files
423	
424	- `drivers/c.sh` — phase 2 C driver, retired by this spec
425	- `langs/lang-spec.h` — interface header (issue 303)
426	- `issues/303-language-runtime-spec.md` — spec contract this implements
427	- `issues/304-task-dispatch-layer.md` — dispatch layer that calls
428	  `invoke`
429	- `issues/302-wire-value-slot-store.md` — large-value heap
430	- `issues/222-compile-button-and-assets-directory.md` — where compiled
431	  `.so`s live (`compiled/bin/`)
432	- `issues/306-lua-language-spec.md` — sister spec; same shape, no
433	  compile step
434	- `maps/driver-test` — multi-language test map including C boxes
435	
436	## Implementation log
437	
438	### Compile + invoke — 2026-05-12
439	
440	`langs/c/spec.c`:
441	- `compile(src, out)` forks gcc with `-shared -fPIC -O2 -Wall`.
442	- `init` allocates a 32-entry dlopen cache; `teardown` walks it.
443	- `invoke` looks up the .so in the cache or dlopens it, dlsyms
444	  the named function, and calls it through a fixed signature:
445	  `int fn(const void **inputs, const int *sizes, int n,
446	  void *out_buf, int out_capacity, int *out_size)`.
447	
448	Convention: C box functions all match that signature. The
449	optional typed-wrapper mode (per-box signature parsing +
450	generated marshaller) is deferred until a box wants types that
451	diverge from the byte-array shape.
452	
453	`tests/maps/hello/src/echo.c` ships two fixture functions; five
454	tests in `tests/307-c-spec-test.c` compile and invoke them, plus
455	the missing-symbol and bad-source failure modes.
456	
457	## Remaining work (to unblock issue 312's deferred output side)
458	
459	1. ~~**JSON-input parsing (primitive level).** Done 2026-05-21.~~
460	2. **JSON-output writing.** When `output_native == 0`, the C
461	   spec writes a JSON serialization of the return value rather
462	   than the user function's raw bytes. The wrapper knows the
463	   return type, so it can construct the JSON shape. Symmetric
464	   to (1); pending typed-wrapper generation so the spec knows
465	   what shape to serialize. **Still open.**
466	3. ~~**`compile` callback signature extension.** Done 2026-05-21.~~
467	4. ~~**Per-box cflags / link_libs.** Done 2026-05-21.~~ headers
468	   field is parsed but not yet consumed by the spec — it lights
469	   up when wrapper generation lands.
470	
471	## Implementation log (continued)
472	
473	### JSON-input acceptance (primitive level) — 2026-05-21
474	
475	`langs/c/spec.c` gains `try_strip_json_primitive`, called per
476	input port when `input_native[i] == 0`. Behavior:
477	
478	- JSON string `"hello"` → user function receives the unquoted
479	  bytes `hello` (5 bytes, no quotes).
480	- JSON number / bool / null → passthrough; the JSON
481	  representation is already the form the C function's
482	  `atol` / `strncmp` / etc. expect.
483	- JSON arrays / objects → passthrough (raw bytes); proper
484	  projection needs the typed-wrapper generation that's still
485	  ahead in items 2-4 above.
486	- Parse failure → passthrough. Matches Lua's input-side
487	  parse-with-fallback so producers that don't yet emit real
488	  JSON keep working.
489	
490	The C spec's `invoke` reuses a stack-allocated scratch matrix
491	(`scratch[C_MAX_INPUTS][4096]`) — the substituted bytes live
492	inside the invoke's frame so the lifetime question is trivial,
493	and no heap allocation happens on the call path. The C box
494	function still uses the same fixed signature
495	(`int fn(const void **, const int *, int, void *, int, int *)`);
496	the JSON unwrap is transparent to it.
497	
498	This slice unblocks issue 312's slice 4.5 — Lua now writes JSON
499	for cross-language outputs, and a Lua → C wire with a primitive
500	payload (string or number) flows correctly end-to-end. The
501	pipeline integration test (`tests/maps/pipeline`) exercises this
502	path: Lua doubles a number, C adds one, Bash exclaims, end-to-end.
503	
504	`tests/307-c-spec-test.c` gains three tests:
505	`invoke_json_string_input` (quotes stripped),
506	`invoke_json_number_input` (raw passthrough on JSON number),
507	`invoke_json_falls_back_on_non_json` (raw passthrough on parse
508	failure). `langs/c/Makefile` now folds in `libs/json/json.c`
509	the same way the Lua spec does.
510	
511	### compile callback signature + per-box build hints — 2026-05-21
512	
513	The spec interface's `compile` callback grew a third parameter,
514	`const box_t *box`. Calls with a real box record (compile-button
515	pipeline, future typed-wrapper generation) get access to the
516	box's identity and compile hints; the internal lazy-compile path
517	in `maybe_lazy_compile` passes `NULL` because it operates on a
518	raw `.c` ref with no box context. `langs/lang-spec.h` carries a
519	forward `typedef struct box box_t;` so specs that don't read the
520	fields don't need the loader header pulled in.
521	
522	`box_t` (in `src/010-graph-loader.h`) grew four optional fields,
523	all parsed from the box JSON when present:
524	
525	- `cflags` — a single string of additional flags appended to gcc.
526	  Whitespace-split into argv tokens at compile time.
527	- `link_libs[]` — array of library names. Each becomes a
528	  `-l<name>` argv entry. The pointer array is malloc'd and freed
529	  by `graph_destroy`; the strings inside live in the JSON arena.
530	- `headers[]` — array of header names. Parsed and stored; not yet
531	  consumed by the spec. Lights up with wrapper generation.
532	- `n_link_libs` / `n_headers` — counts.
533	
534	`langs/c/spec.c`'s `c_compile` walks the box's `cflags` and
535	`link_libs` into a bounded argv (96 slots), forks gcc, and waits.
536	The previous fixed-flags-only execlp call is replaced by execvp
537	so the variable arg list can flow through. Failure modes (cflags
538	too long, too many tokens, argv overflow, oom) all return -1
539	with a stderr line naming the box id.
540	
541	Two new end-to-end tests in `tests/307-c-spec-test.c`:
542	
543	- `compile_with_cflags` — a probe source whose return depends on
544	  `#ifdef SORAMECH_FLAG_ON`. Builds twice: once with no hints
545	  (returns "off"), once with `cflags="-DSORAMECH_FLAG_ON"`
546	  (returns "on"). Proves the cflags string reaches gcc.
547	- `compile_with_link_libs` — a source that calls `sqrt(16.0)`
548	  from `<math.h>`. Builds with `link_libs=["m"]`, invokes,
549	  receives "4". Proves the `-l<name>` translation works.
550	
551	The compile pipeline (`scripts/soramech-compile.sh`) currently
552	ignores the box's cflags / link_libs when its informational
553	pre-compile of C boxes runs — the runtime lazy-compile is the
554	authoritative path. Cleaning that up so the pre-compile uses the
555	same flags is a small follow-on; out of scope for this slice.
556	
```

## Assistant

_Tools:_

- `Bash: git log --oneline -20 /mnt/mtwo/programs/sora/soramech/issues/307-c-language-spec.md /mnt/mtwo/programs/sora/soramech...`

## Assistant

_Tools:_

- `Read: /mnt/mtwo/programs/sora/soramech/issues/307-c-language-spec.md`

### Tool result

```
c7beb53 307 — the C language spec, gcc compile and dlopen cache
d27e1af 307 — sharpen the language-agnostic rule, record compile + invoke log
0dd33de Phase 3 foundation — build system scaffolding and the slot store
f642680 Specify phase 3 thread pool runtime; align docs and graph model
```

## Assistant

_Tools:_

- `Bash: git diff --stat HEAD /mnt/mtwo/programs/sora/soramech/langs/c/spec.c /mnt/mtwo/programs/sora/soramech/langs/c/Makefil...`

### Tool result

```
 langs/c/Makefile        |  10 +-
 langs/c/spec.c          | 318 ++++++++++++++++++++++++++++++++++++--
 langs/lang-spec.h       |  56 ++++++-
 src/010-graph-loader.c  |  97 ++++++++++--
 src/010-graph-loader.h  |  26 +++-
 tests/307-c-spec-test.c | 399 +++++++++++++++++++++++++++++++++++++++++++++++-
 6 files changed, 865 insertions(+), 41 deletions(-)
```

## Assistant

_Tools:_

- `Bash: git log --oneline --since="2026-05-19" --name-only | head -80`

### Tool result

```
6ecfd94 302 — one heap for every byte that carries a value
Makefile
issues/completed/302-wire-value-slot-store.md
issues/phase-3-progress.md
src/008-pool-runner.c
src/009-slot-store.c
src/009-slot-store.h
src/009-slot-store.info.md
src/012-dispatch.c
tests/009-slot-store-test.c
a342599 the structural shell — dispatch as attempt loop, data boxes as defaults
docs/001-architecture.md
docs/HTML/task-slab-allocator.html
issues/244-data-box-pull-on-demand.md
issues/304-task-dispatch-layer.md
2d5780a 233 — unified routing schema for branching boxes
assets/js/002-boxes.js
assets/js/004-inspector.js
assets/js/005-app.js
assets/js/006-wires.js
issues/240-routing-kind-randomizer.md
issues/241-routing-kind-weighted.md
issues/242-routing-kind-distributor.md
issues/243-routing-multi-band-comparator.md
issues/completed/233-unified-routing-schema.md
issues/phase-2-progress.md
src/001-schema.lua
src/004-executor.lua
a2d9df4 239 — literal-bound input ports render as a nodule, name in the gutter
assets/js/002-boxes.js
issues/completed/239-literal-port-shrinks-to-nodule-with-name-on-left.md
issues/phase-2-progress.md
a46be72 217 — variadic toggle gates on the language, not on the function
assets/js/004-inspector.js
issues/completed/217-variadic-ports-and-language-spec-shape.md
issues/phase-2-progress.md
d558452 314 — the project's own small C JSON parser and writer
issues/completed/314-c-json-parser.md
issues/phase-3-progress.md
9f5c118 310 — priority parameter wired through the spawn surface
issues/completed/310-priority-queue.md
issues/phase-3-progress.md
fdbe39e 309 — top-level build and the portable-artifact pipeline
issues/completed/309-build-system.md
issues/phase-3-progress.md
77b9462 308 — bash boxes through a persistent socket server, no per-call spawn
issues/completed/308-bash-language-spec.md
issues/phase-3-progress.md
1b0a024 303 — pluggable language-runtime spec, equal footing for every language
issues/completed/303-language-runtime-spec.md
issues/phase-3-progress.md
a4f7c39 301 — thread-pool lifecycle and per-worker initialization
issues/completed/301-pool-lifecycle-and-worker-init.md
issues/phase-3-progress.md
c70646d 231 — language-specific signature parsers move into langs/
issues/completed/231-move-signature-parsers-into-langs.md
```

## Assistant

_Tools:_

- `Bash: git diff HEAD /mnt/mtwo/programs/sora/soramech/langs/c/spec.c | head -400`

### Tool result

```
diff --git a/langs/c/spec.c b/langs/c/spec.c
index 1ec6491..5a26565 100644
--- a/langs/c/spec.c
+++ b/langs/c/spec.c
@@ -21,6 +21,8 @@
  */
 
 #include "lang-spec.h"
+#include "json.h"
+#include "010-graph-loader.h"
 
 #include <dlfcn.h>
 #include <stdio.h>
@@ -68,21 +70,135 @@ static void c_teardown(void *handle)
 }
 /* }}} */
 
-/* {{{ c_compile() — gcc -shared -fPIC */
-static int c_compile(const char *src_path, const char *out_path)
+/* {{{ split_cflags() — split a cflags string on whitespace
+ *
+ * gcc wants each token as a separate argv entry. The user's cflags
+ * is a single string like "-O0 -g -mavx2" — we split it in place
+ * (modifying `buf`) and stash the token pointers into `tokens`.
+ * Returns the number of tokens; -1 on overflow. Tokens point into
+ * `buf` and are valid for as long as `buf` is. */
+static int split_cflags(char *buf, char **tokens, int max_tokens)
+{
+    int n = 0;
+    char *p = buf;
+    while (*p) {
+        while (*p == ' ' || *p == '\t') p++;
+        if (!*p) break;
+        if (n >= max_tokens) return -1;
+        tokens[n++] = p;
+        while (*p && *p != ' ' && *p != '\t') p++;
+        if (*p) *p++ = '\0';
+    }
+    return n;
+}
+/* }}} */
+
+/* {{{ c_compile() — gcc -shared -fPIC, plus per-box hints from box JSON
+ *
+ * When `box` is non-NULL the box's cflags / link_libs hints are
+ * appended to the gcc command. Issue 307 items 3+4. When `box` is
+ * NULL the call is the "no box context" path (e.g. the internal
+ * lazy-compile when the dispatch hands the spec a raw `.c` ref) —
+ * the default flags are still applied, no extras. */
+static int c_compile(const char *src_path, const char *out_path,
+                     const box_t *box)
 {
     if (!src_path || !out_path) return -1;
+
+    /* Bounded argv: 16 fixed slots for "-shared -fPIC -O2 -Wall
+     * -o out src" plus headroom, 32 for cflags tokens, 32 for
+     * link-lib `-l` pairs. Anything larger is unusual and the
+     * caller can break up the work into multiple boxes. */
+    enum { ARGV_CAP = 96, CFLAG_TOK_CAP = 32 };
+    const char *argv_view[ARGV_CAP];
+    int n = 0;
+    argv_view[n++] = "gcc";
+    argv_view[n++] = "-shared";
+    argv_view[n++] = "-fPIC";
+    argv_view[n++] = "-O2";
+    argv_view[n++] = "-Wall";
+
+    /* Per-box cflags: split on whitespace into individual tokens. */
+    char  cflags_buf[1024];
+    char *cflag_tokens[CFLAG_TOK_CAP];
+    int   n_cflag_tokens = 0;
+    if (box && box->cflags && box->cflags[0]) {
+        size_t len = strlen(box->cflags);
+        if (len >= sizeof cflags_buf) {
+            fprintf(stderr, "c spec: cflags too long for box '%s'\n", box->id);
+            return -1;
+        }
+        memcpy(cflags_buf, box->cflags, len + 1);
+        n_cflag_tokens = split_cflags(cflags_buf, cflag_tokens, CFLAG_TOK_CAP);
+        if (n_cflag_tokens < 0) {
+            fprintf(stderr, "c spec: too many cflag tokens for box '%s'\n",
+                    box->id);
+            return -1;
+        }
+        for (int i = 0; i < n_cflag_tokens; i++) {
+            if (n >= ARGV_CAP) {
+                fprintf(stderr, "c spec: argv overflow for box '%s'\n", box->id);
+                return -1;
+            }
+            argv_view[n++] = cflag_tokens[i];
+        }
+    }
+
+    argv_view[n++] = "-o";
+    argv_view[n++] = out_path;
+    argv_view[n++] = src_path;
+
+    /* Per-box link libs: each becomes a `-l<name>` argv entry. The
+     * combined "-l" + name lives in a small per-lib heap allocation
+     * — n_link_libs is small so the cost is negligible. */
+    char *lflag_buf[ARGV_CAP];
+    int   n_lflags = 0;
+    if (box && box->link_libs && box->n_link_libs > 0) {
+        for (int i = 0; i < box->n_link_libs; i++) {
+            const char *libname = box->link_libs[i];
+            if (!libname || !*libname) continue;
+            size_t buflen = strlen(libname) + 3;   /* "-l" + name + NUL */
+            char *s = malloc(buflen);
+            if (!s) {
+                for (int j = 0; j < n_lflags; j++) free(lflag_buf[j]);
+                fprintf(stderr, "c spec: oom assembling link_libs for '%s'\n",
+                        box->id);
+                return -1;
+            }
+            snprintf(s, buflen, "-l%s", libname);
+            if (n >= ARGV_CAP) {
+                for (int j = 0; j < n_lflags; j++) free(lflag_buf[j]);
+                free(s);
+                fprintf(stderr, "c spec: argv overflow for box '%s'\n", box->id);
+                return -1;
+            }
+            lflag_buf[n_lflags++] = s;
+            argv_view[n++] = s;
+        }
+    }
+
+    if (n >= ARGV_CAP) {
+        for (int j = 0; j < n_lflags; j++) free(lflag_buf[j]);
+        return -1;
+    }
+    argv_view[n] = NULL;
+
     pid_t pid = fork();
-    if (pid < 0) return -1;
+    if (pid < 0) {
+        for (int j = 0; j < n_lflags; j++) free(lflag_buf[j]);
+        return -1;
+    }
     if (pid == 0) {
-        /* Child: replace ourselves with gcc. */
-        execlp("gcc", "gcc",
-               "-shared", "-fPIC", "-O2", "-Wall",
-               "-o", out_path, src_path, (char *)NULL);
+        /* Child: replace ourselves with gcc. execvp wants
+         * (char *const *), and we built (const char *) entries —
+         * the cast is safe because execvp doesn't modify them. */
+        execvp("gcc", (char *const *)(void *)argv_view);
         _exit(127);
     }
     int status = 0;
-    if (waitpid(pid, &status, 0) < 0) return -1;
+    int wait_rc = waitpid(pid, &status, 0);
+    for (int j = 0; j < n_lflags; j++) free(lflag_buf[j]);
+    if (wait_rc < 0) return -1;
     if (!WIFEXITED(status) || WEXITSTATUS(status) != 0) {
         fprintf(stderr, "c spec: gcc failed for '%s' → '%s' (status=%d)\n",
                 src_path, out_path, status);
@@ -122,7 +238,9 @@ static const char *maybe_lazy_compile(const char *file_path,
     unsigned long h = hash_path(file_path);
     int n = snprintf(so_buf, so_cap, "/tmp/soramech-c-%lx.so", h);
     if (n < 0 || (size_t)n >= so_cap) return NULL;
-    if (c_compile(file_path, so_buf) != 0) return NULL;
+    /* No box context on this internal lazy-compile path; the caller
+     * passed us a raw `.c` ref and we just need a default build. */
+    if (c_compile(file_path, so_buf, NULL) != 0) return NULL;
     return so_buf;
 }
 /* }}} */
@@ -149,13 +267,142 @@ static void *get_or_load(c_handle_t *h, const char *path)
 }
 /* }}} */
 
+/* {{{ apply_typed_json_output() — issue 307 item 2
+ *
+ * The user's C function has already written its raw bytes into
+ * out_buf and set *out_size. When the dispatch asked for JSON
+ * output (output_native == 0) we rewrap those bytes per the box's
+ * declared return type so the cross-language consumer receives
+ * a parseable JSON value.
+ *
+ * Returns 0 on success, -1 on buffer-overflow / encoding error.
+ *
+ * - `string`: wrap in JSON quotes with proper escaping for
+ *    quotes, backslashes, control characters, etc. The bytes
+ *    might not be NUL-terminated, so we copy them into a
+ *    NUL-terminated scratch before feeding json_writer_string.
+ * - `int` / `double` / `bool` / `null` / `json` / `bytes`:
+ *    passthrough. The user's bytes are already a valid JSON
+ *    fragment ("42" is a JSON number; "true" is a JSON bool;
+ *    user-declared "json" returns mean the function chose its
+ *    own JSON serialization). For `bytes` proper base64 wrapping
+ *    is a follow-on; raw passthrough keeps the door open for
+ *    typed-wrapper generation that can do it right.
+ * - `void`: empty output regardless of what the user wrote.
+ * - NULL / unknown: passthrough (safest default; matches the
+ *    pre-typed behavior). */
+static int apply_typed_json_output(char *out_buf, int out_capacity,
+                                   int *out_size, const char *returns)
+{
+    if (!out_size) return 0;
+    if (!returns)  return 0;    /* unknown type → passthrough */
+
+    if (strcmp(returns, "void") == 0) {
+        *out_size = 0;
+        return 0;
+    }
+    if (strcmp(returns, "string") == 0) {
+        /* Stash the user's bytes (small bound — typical primitive
+         * outputs fit in a few KB; if they overflow this scratch,
+         * the JSON-encoded form wouldn't fit out_buf either). */
+        char scratch[8192];
+        int n = *out_size;
+        if (n < 0 || n >= (int)sizeof scratch) return -1;
+        memcpy(scratch, out_buf, (size_t)n);
+        scratch[n] = '\0';
+
+        json_writer_t w;
+        json_writer_init(&w, out_buf, out_capacity);
+        json_writer_string(&w, scratch);
+        int written = json_writer_finish(&w);
+        if (written < 0) return -1;
+        *out_size = written;
+        return 0;
+    }
+    /* int / double / bool / null / json / bytes / anything else:
+     * passthrough. Already valid JSON or the spec has no better
+     * shape without typed wrapper generation. */
+    return 0;
+}
+/* }}} */
+
+/* {{{ try_strip_json_primitive() — issue 307 JSON-input acceptance
+ *
+ * Attempts to parse `src` (length `src_size`, not necessarily NUL-
+ * terminated) as JSON. If it's a JSON string, writes the unquoted
+ * content into `scratch` and returns 1 with `*out_data` pointing at
+ * the scratch buffer and `*out_size` set to the unquoted length.
+ * If it's a JSON number / bool, leaves the bytes alone and returns
+ * 0 (the JSON representation of these is the same shape the C
+ * function expects — "42" parses with atol, "true"/"false" with
+ * strcmp, "null" with strcmp). Returns 0 in all "use the raw bytes"
+ * cases including parse failures and non-primitive JSON. The C
+ * function only sees the substituted bytes when this returns 1.
+ *
+ * This is the symmetric of Lua's input-side parse-with-fallback —
+ * cross-language producers that emit JSON (Lua, future Python /
+ * Node etc.) get their primitive strings unwrapped here. JSON
+ * arrays / objects fall through; handling those needs typed
+ * wrappers (the deferred 307 work). */
+static int try_strip_json_primitive(const void *src, int src_size,
+                                    char *scratch, int scratch_cap,
+                                    const void **out_data, int *out_size)
+{
+    if (src_size <= 0) return 0;
+
+    /* json_parse needs a NUL-terminated buffer. Copy into a small
+     * heap allocation rather than a fixed-size stack array because
+     * input sizes vary; the allocator's per-call cost is negligible
+     * compared to the cross-language wire-format work. */
+    char *nul_buf = (char *)malloc((size_t)src_size + 1);
+    if (!nul_buf) return 0;
+    memcpy(nul_buf, src, (size_t)src_size);
+    nul_buf[src_size] = '\0';
+
+    json_arena_t *arena = json_arena_create();
+    if (!arena) { free(nul_buf); return 0; }
+
+    int err_offset = 0;
+    const char *err_msg = NULL;
+    json_node_t *node = json_parse(arena, nul_buf, &err_offset, &err_msg);
+    free(nul_buf);
+
+    if (!node) { json_arena_destroy(arena); return 0; }
+
+    json_kind_t kind = json_kind(node);
+    int rc = 0;
+    if (kind == JSON_STRING) {
+        const char *s = json_string_value(node);
+        int len = (int)strlen(s);
+        if (len < scratch_cap) {
+            memcpy(scratch, s, (size_t)len);
+            scratch[len] = '\0';
+            *out_data = scratch;
+            *out_size = len;
+            rc = 1;
+        }
+    }
+    /* Other primitives (NUMBER / BOOL / NULL): raw bytes already
+     * match what the C function expects. Containers (ARRAY /
+     * OBJECT): no clean unwrap without typed wrappers — let the
+     * raw bytes pass through and surface whatever the function
+     * does with them. */
+
+    json_arena_destroy(arena);
+    return rc;
+}
+/* }}} */
+
 /* {{{ c_invoke() */
 static int c_invoke(void *handle,
+                    const box_t *box,
                     const char  *file_path,    /* path to a .so */
                     const char  *fn_name,
                     const void **input_data,
                     const int   *input_sizes,
+                    const int   *input_native,
                     int          n_inputs,
+                    int          output_native,
                     void        *out_buf,
                     int          out_capacity,
                     int         *out_size)
@@ -183,8 +430,57 @@ static int c_invoke(void *handle,
                 fn_name, file_path, err ? err : "symbol not found");
         return -1;
     }
-    return u.fn(input_data, input_sizes, n_inputs,
-                out_buf, out_capacity, out_size);
+
+    /* Issue 307 JSON-input acceptance: when input_native says the
+     * bytes are JSON (i.e. the producer is in a different language),
+     * try to unwrap JSON primitives so the user's C function sees
+     * the underlying value. Substituted bytes live in per-input
+     * scratch buffers on the stack — the call returns before any
+     * of them go out of scope, so a pointer table referencing them
+     * is safe to hand to the user function. Inputs whose flag says
+     * native, or whose JSON parse fails, or whose JSON shape is a
+     * container that can't be unwrapped without typed wrappers,
+     * pass through unchanged. */
+    enum { C_MAX_INPUTS = 16, C_SCRATCH_SIZE = 4096 };
+    if (n_inputs > C_MAX_INPUTS) {
+        fprintf(stderr, "c spec: invoke '%s' has %d inputs (cap %d)\n",
+                fn_name, n_inputs, C_MAX_INPUTS);
+        return -1;
+    }
+    const void *eff_data[C_MAX_INPUTS];
+    int         eff_size[C_MAX_INPUTS];
+    char        scratch[C_MAX_INPUTS][C_SCRATCH_SIZE];
+    for (int i = 0; i < n_inputs; i++) {
+        eff_data[i] = input_data[i];
+        eff_size[i] = input_sizes[i];
+        if (input_native && input_native[i] == 0) {
+            try_strip_json_primitive(input_data[i], input_sizes[i],
+                                     scratch[i], C_SCRATCH_SIZE,
+                                     &eff_data[i], &eff_size[i]);
+            /* try_strip_* leaves eff_data / eff_size untouched on
+             * the "use raw bytes" path — no else branch needed. */
+        }
+    }
+
+    int rc = u.fn(eff_data, eff_size, n_inputs,
+                  out_buf, out_capacity, out_size);
+    if (rc != 0) return rc;
+
+    /* Output-side typed JSON wrap. When the dispatch wants JSON
+     * (output_native == 0) and the box declared a return type, the
+     * user's raw bytes get reshaped — strings become JSON-quoted,
+     * primitives pass through. Without a declared return type the
+     * spec leaves the bytes alone (current behavior). */
+    if (!output_native && box && box->returns) {
+        if (apply_typed_json_output(out_buf, out_capacity,
+                                    out_size, box->returns) != 0) {
+            fprintf(stderr, "c spec: JSON-encode return for '%s' failed "
+                            "(declared %s)\n",
+                    box->id ? box->id : fn_name, box->returns);
+            return -1;
+        }
+    }
+    return 0;
 }
 /* }}} */
```

## Assistant

_Tools:_

- `Bash: git diff HEAD /mnt/mtwo/programs/sora/soramech/tests/307-c-spec-test.c | grep -E "^\+.*test_|^\+.*TEST" | head -30`

### Tool result

```
+/* {{{ test_invoke_json_string_input() — issue 307 JSON-input acceptance */
+static int test_invoke_json_string_input(void)
+/* {{{ test_invoke_json_number_input() — passthrough on primitive */
+static int test_invoke_json_number_input(void)
+/* {{{ test_invoke_json_falls_back_on_non_json() — robustness */
+static int test_invoke_json_falls_back_on_non_json(void)
+/* {{{ test_compile_with_cflags() — issue 307 items 3+4
+static int test_compile_with_cflags(void)
+/* {{{ test_compile_with_link_libs() — issue 307 items 3+4
+static int test_compile_with_link_libs(void)
+/* {{{ test_invoke_string_output_jsonifies() — issue 307 item 2
+static int test_invoke_string_output_jsonifies(void)
+/* {{{ test_invoke_string_output_native_passthrough() — issue 307 item 2
+static int test_invoke_string_output_native_passthrough(void)
+/* {{{ test_invoke_int_output_passthrough() — issue 307 item 2
+static int test_invoke_int_output_passthrough(void)
```

## Assistant

_Tools:_

- `Bash: git diff HEAD /mnt/mtwo/programs/sora/soramech/langs/lang-spec.h | head -80`

### Tool result

```
diff --git a/langs/lang-spec.h b/langs/lang-spec.h
index 6642f60..9b7260e 100644
--- a/langs/lang-spec.h
+++ b/langs/lang-spec.h
@@ -32,20 +32,59 @@
 extern "C" {
 #endif
 
+/* Forward declaration. `box_t` lives in src/010-graph-loader.h and
+ * carries the full per-box record (input declarations, language,
+ * compile-time hints like `cflags` / `link_libs` / `headers`).
+ * Spec implementations include the loader header when they want to
+ * read those fields; the bare forward declaration here keeps the
+ * spec interface stand-alone for callers that only need the typedef
+ * names. */
+typedef struct box box_t;
+
 /* {{{ Shared signatures */
 /* Invoke signature — shared by `invoke`, `invoke_native`, and
  * `invoke_json`. The dispatch layer picks which one to call based
- * on the wire-format classification in issue 312. */
+ * on the wire-format classification in issue 312.
+ *
+ * Per-input format flag (`input_native[i]`): non-zero iff the
+ * bytes in `input_data[i]` are this spec's native form; zero iff
+ * they are JSON from a cross-language producer. The spec
+ * dispatches internally — its decoder for that input picks the
+ * native or JSON path based on the flag.
+ *
+ * Per-output format flag (`output_native`): non-zero iff the
+ * dispatch wants the spec to write native bytes to `out_buf`,
+ * zero iff it wants JSON. The spec produces one form per call;
+ * cross-language fan-out is handled by the dispatch layer, which
+ * converts via `native_to_json` once and pushes the JSON to every
+ * cross-language consumer.
+ *
+ * On non-dual-ring slots (single-language inputs or single-language
+ * outputs only), the dispatch passes NULL for `input_native` and
+ * 0 for `output_native`; the spec falls back to its default
+ * decode (typically JSON, which Lua's existing bridge handles).
+ * `input_native` may be NULL even when `n_inputs > 0` — specs
+ * must check before dereferencing. */
 typedef int (*lang_invoke_fn)(void *handle,
+                              const box_t *box,
                               const char *file_path,
                               const char *fn_name,
                               const void **input_data,
                               const int   *input_sizes,
+                              const int   *input_native,
                               int          n_inputs,
+                              int          output_native,
                               void        *out_buf,
                               int          out_buf_capacity,
                               int         *out_size);
 
+/* `box` is the box record being invoked, or NULL when the caller has
+ * no box context (e.g. test fixtures driving the spec directly).
+ * Specs that need declared return type, compile hints, or future
+ * sentinel-emission decisions read fields off `box`. Specs that
+ * don't need it ignore the parameter. The pointer is valid only for
+ * the duration of the call. */
+
 /* Wire-format bridge: convert one direction of native ↔ JSON.
  * Used when a value crosses a language boundary. Spec sets one
  * or both; if NULL, the dispatch layer treats native and JSON as
@@ -85,9 +124,18 @@ typedef struct lang_spec {
     /* compile: optional. Called once per source file at map-load
      * (or at editor compile-button time). For languages that don't
      * compile (Lua, Bash), leave this NULL. For C, it runs
-     * gcc -shared -fPIC -o out_path src_path. Returns 0 on
-     * success, nonzero on failure. */
-    int   (*compile)(const char *src_path, const char *out_path);
+     * gcc -shared -fPIC -o out_path src_path, plus any per-box
+     * compile flags / link libraries / headers the box declared.
+     *
+     * `box` is the box record for the box being compiled, or NULL
+     * if the caller is compiling a source file with no box context
+     * (e.g. the C spec's internal lazy-compile path that maps a raw
+     * `.c` ref to a `/tmp/<hash>.so` on the fly). When non-NULL,
+     * the spec may read box->cflags / box->link_libs / box->headers
```

## Assistant

The most-recently-touched ticket is `issues/307-c-language-spec.md` (19:26). Comparing it to the uncommitted code (spec.c has 318 lines of unstaged additions, lang-spec.h ~56, tests ~399), there's a chunk of done work the ticket doesn't reflect:

**Item 2 — JSON-output writing.** The "Remaining work" list still says *"Still open."* But `langs/c/spec.c` already has `apply_typed_json_output()` wired into `c_invoke`. When the dispatch passes `output_native == 0` and the box declares `returns`:
- `string` → re-wrapped with proper JSON quoting/escaping via `json_writer_string`
- `void` → emitted as empty
- `int` / `double` / `bool` / `json` / `bytes` → passthrough (already valid JSON)
Three new tests prove it: `invoke_string_output_jsonifies`, `invoke_string_output_native_passthrough`, `invoke_int_output_passthrough`. So item 2 is at least partially done at the primitive level and should be moved out of "Remaining work".

**The `invoke` signature widened past what the ticket documents.** The ticket's compile-callback section says *"compile gains a `box_t` pointer"* — but `lang-spec.h` shows `invoke` also grew **three** new params: `const box_t *box`, `const int *input_native`, and `int output_native`. The ticket's "Input marshalling" / "Output encoding" tables describe behavior, but never mention that the dispatch now hands the spec a per-input native/JSON classification and a per-call output classification. That's a real interface change worth recording — probably as a new section ("Invoke signature") rather than tucked into item 2.

**Stale fragments inside the ticket.** "Per-worker state" still shows `void *c_init(int worker_idx)` and the old `c_compile(src, out)` / no-`box` `invoke` shape. Those should be updated to match the live header.

`★ Insight ─────────────────────────────────────`
- The ticket's "Done 2026-05-21" entries are partial: they cover the *input* side of JSON bridging in detail, but leave a sibling "output" implementation in the codebase that the doc still claims is open. This is the classic shape of half-recorded work — the easier half got written up while the harder half was still being thought through, then both shipped together.
- The dual `input_native[]` / `output_native` flags are a small but load-bearing schema change: every spec now must check the flag *per input*, not just per call. That's the kind of detail that has to live in `lang-spec.h` comments **and** in 307's prose, otherwise the next spec author (Bash, future Python) will miss it.
`─────────────────────────────────────────────────`

A question back, since you asked one: looking at `apply_typed_json_output`, `bytes` is documented as *"passthrough; proper base64 wrapping is a follow-on"* — but raw bytes aren't valid JSON in general. Do you want me to log that as a separate "item 2b" on 307, or roll it into 318 (sentinel primitives in cross-language JSON), which already covers non-primitive wire shapes?

## User

  A question back, since you asked one: looking at apply_typed_json_output, bytes is documented as "passthrough; proper base64 wrapping is a
  follow-on" — but raw bytes aren't valid JSON in general. Do you want me to log that as a separate "item 2b" on 307, or roll it into 318
  (sentinel primitives in cross-language JSON), which already covers non-primitive wire shapes?


put it in 318

## Assistant

_Tools:_

- `Read: /mnt/mtwo/programs/sora/soramech/issues/318-sentinel-primitives-in-cross-language-json.md`

### Tool result

```
1	# 318 — Sentinel primitives in the cross-language JSON wire
2	
3	## Status
4	open
5	
6	## Concept
7	
8	JSON's structure covers six kinds of value cleanly — object,
9	array, number, string, bool, null — and the cross-language ring
10	in the dual-ring slot (issue 312) carries JSON for any wire that
11	crosses a language boundary. The structural cases that don't fit
12	those six kinds — Lua closures, libcurl handles, C++ class
13	instances, file handles, function pointers, large byte buffers
14	where serialization would be expensive — need somewhere to live
15	in the JSON ring without forcing the wire format to grow new
16	top-level shapes.
17	
18	**Sentinel primitives** are JSON object shapes with reserved key
19	names that mean "this isn't a structural value, it's a reference
20	to something the consumer must reconstruct using language-specific
21	machinery."
22	
23	Three kinds, each solving a different structural gap:
24	
25	```json
26	{"$ref":              {"ptr": "0x...",  "len": 1048576,
27	                       "type": "bytes"}}
28	{"$function_pointer": {"addr": "0x...", "signature": "int(void*,void*)"}}
29	{"$lang_opaque":      {"lang": "lua",   "tag": 12345,
30	                       "shape": "coroutine"}}
31	```
32	
33	Sentinels are **emitted by the producer's spec** when its language
34	has a value JSON can't represent structurally. They are
35	**reconstructed by the consumer's spec** using whatever runtime
36	machinery that language provides (LuaJIT FFI, ctypes for Python,
37	direct cast for C, etc.). Wires whose consumer cannot reconstruct
38	a sentinel kind **fail at compile time** — the editor catches the
39	mismatch when the user wires a function-pointer output into a
40	Bash input.
41	
42	## Why these three, not more
43	
44	The three sentinels carve up the structural gap by ownership:
45	
46	- **`$ref`** — bytes in a shared heap. The producer wrote the
47	  bytes to the large-value heap (issue 302 / `src/015-large-value-heap.c`)
48	  and emits a pointer-and-length reference. The bytes are
49	  immutable for the wire's lifetime; refcount tracks consumers.
50	  Solves: "I have a megabyte to share; copying it through the
51	  slot would be wasteful."
52	- **`$function_pointer`** — a callable, identified by a raw
53	  address and a signature interpretable in C-function-pointer
54	  syntax. The address is process-local. The signature is the
55	  contract between producer and consumer. Solves: "I have a
56	  function I want my consumer to be able to call directly."
57	- **`$lang_opaque`** — a language-private value. The producer
58	  tags it with the language name and a worker-private reference
59	  the producer's spec can resolve back. Solves: "I have a value
60	  with no general structural representation; only my own
61	  language knows what it is."
62	
63	Anything that doesn't fit one of the three either (a) has a
64	JSON-structural representation and shouldn't be a sentinel, or
65	(b) is genuinely unrepresentable and the wire is wrong, not the
66	format.
67	
68	## Producer protocol
69	
70	Each language spec's `native_to_json` walks its native values and
71	emits the appropriate sentinel when it encounters a non-structural
72	value. The spec author writes the emit logic; it's not generic.
73	
74	### Lua
75	
76	`encode_value` in `langs/lua/spec.c:188` currently hard-errors on
77	function / userdata / thread / lightuserdata. The extension:
78	
79	- **Function** (`LUA_TFUNCTION`) — if the function is a C function
80	  registered with FFI, emit `$function_pointer` with its FFI
81	  signature; if it's a Lua function, emit `$lang_opaque` with
82	  `lang: "lua"` and a tag the spec can use to resolve the
83	  function back later (a registry index, for example).
84	- **Userdata** with an FFI ctype — emit `$function_pointer` if
85	  the type is a function pointer; `$ref` if the type is a sized
86	  buffer; `$lang_opaque` otherwise.
87	- **Coroutine** (`LUA_TTHREAD`) — `$lang_opaque` with
88	  `shape: "coroutine"`.
89	- **Large strings or tables** — at the spec's discretion, the
90	  producer may write the value to the large-value heap and emit
91	  `$ref` instead of inlining the bytes. Decision is per-call,
92	  driven by size threshold or output annotation.
93	
94	### C
95	
96	The C spec's generated wrapper (issue 307) knows the box's
97	declared output type. The wrapper emits the right sentinel
98	shape based on the type:
99	
100	- A declared `function_pointer` return — `$function_pointer`
101	  with the type's signature.
102	- A declared `bytes` return whose size exceeds the inline
103	  threshold — `$ref` with the bytes written to the heap.
104	- A declared opaque struct — `$lang_opaque` if the struct's
105	  shape is C-only.
106	
107	### Bash
108	
109	Bash has no native values besides strings; sentinels don't apply
110	on the emit side. A Bash producer always emits structural JSON
111	strings.
112	
113	## Consumer protocol
114	
115	Each spec's `json_to_native` walks the JSON tree and, when it
116	sees a sentinel key, reconstructs the value using its language's
117	machinery. The reconstruction is the spec author's responsibility.
118	
119	### Lua
120	
121	```c
122	// inside lua_json_to_native, on seeing {"$function_pointer": {...}}:
123	const char *sig = json_string_value(obj, "signature");
124	uintptr_t   addr = json_int_value(obj, "addr");
125	lua_getglobal(L, "ffi");
126	lua_getfield(L, -1, "cast");
127	lua_pushstring(L, sig);                 // "int (*)(void*, void*)"
128	lua_pushinteger(L, (lua_Integer)addr);
129	lua_call(L, 2, 1);
130	// stack top: an FFI callable Lua can invoke as compare_fn(a, b)
131	```
132	
133	`$ref` reconstruction in Lua copies the bytes (`lua_pushlstring(ptr, len)`)
134	since Lua's value model needs values in Lua's heap. The
135	producer's refcount handles the lifetime.
136	
137	`$lang_opaque` with `lang: "lua"` resolves the tag through the
138	worker's per-worker registry; with `lang != "lua"` it's a wire
139	compile error.
140	
141	### C
142	
143	Direct casts. `$function_pointer` becomes a typed function
144	pointer the wrapper can invoke. `$ref` is a `(void*, size_t)`
145	pair the wrapper hands to the user's function. `$lang_opaque`
146	with `lang: "c"` is a typed-struct pointer; other langs fail.
147	
148	### Bash
149	
150	Bash cannot reconstruct any sentinel except possibly `$ref`
151	(by reading the bytes from the file the marshaller staged at the
152	process boundary). `$function_pointer` and `$lang_opaque` fail
153	unless the producer chose the cross-process escape hatch (see
154	below).
155	
156	## Compile-time validation
157	
158	The compile step walks every wire and checks:
159	
160	- If the producer's declared output type can emit sentinel kind
161	  K (per the producer language's spec capability), then
162	- The consumer's language must be able to reconstruct sentinel
163	  kind K (per the consumer language's spec capability).
164	- If not, **the compile fails** with the specific wire and the
165	  specific sentinel kind named.
166	
167	This is editor-time feedback. The user fixes the wire
168	(reroute it, change the consumer's language, attach a custom
169	translation per issue 246) before the program ever runs.
170	
171	The per-spec capability is a static fact each spec declares.
172	Adding a new language K declares K's emit set and reconstruct
173	set; the compile validator consults the declaration.
174	
175	## Cross-process escape hatch for `$function_pointer`
176	
177	Bash (and any other process-isolated language) can't dereference
178	an in-process function-pointer address. Two options when a
179	function pointer's wire targets such a consumer:
180	
181	1. **Fail at compile time** (the default). Clear, honest.
182	2. **Producer-side compiled binary** (opt-in). When the C spec
183	   detects that a function-pointer output is wired to a
184	   cross-process consumer, it compiles the function into a
185	   standalone executable in `compiled/cross-process/<box>__<port>.bin`.
186	   The sentinel becomes:
187	
188	   ```json
189	   {"$function_pointer": {"binary": "compiled/cross-process/...",
190	                          "signature": "int(int,int)"}}
191	   ```
192	
193	   Bash invokes the binary with `$(./bin args...)` per call. Cost:
194	   process spawn per invocation (1-10 ms). The escape hatch is
195	   opt-in per box output — the user explicitly chooses the cost
196	   in the inspector.
197	
198	The default behaviour is option 1 because hiding the spawn cost
199	would mislead the user about runtime behaviour. The user picks
200	their language; the cost follows from the choice. Option 2 is
201	the escape hatch for cases where the user genuinely needs
202	C-from-Bash and accepts the cost.
203	
204	## Relationship to custom translation specs (issue 246)
205	
206	Sentinels are the **spec author's tool** for representing
207	non-structural values. Custom translations are the **user's
208	tool** for interpreting them (or any other value) when the
209	default reconstruction isn't what they want.
210	
211	The escalation path:
212	
213	1. Default spec encode / decode handles JSON-structural values
214	   transparently.
215	2. When the producer's value can't be encoded structurally, the
216	   spec author's emit logic produces the right sentinel.
217	3. When the consumer's spec can reconstruct the sentinel
218	   generically (FFI cast, ctypes, direct pointer), the default
219	   reconstruction runs.
220	4. When the user wants a different interpretation than the
221	   default (treat a function pointer as a method on a class
222	   instance, dereference a `$ref` and parse its bytes as a
223	   custom format, etc.), they attach a per-port custom
224	   translation that overrides the default reconstruction.
225	
226	So sentinels and custom translations are layered: sentinels
227	provide the *bytes that arrive at the wire* for non-structural
228	values; custom translations decide *what to do with those bytes*
229	when the default isn't enough.
230	
231	## Where in the dispatch this lives
232	
233	Sentinels live inside the JSON ring of the dual-ring slot. The
234	slot store (issue 312) doesn't know about them — it just carries
235	bytes. The producer's `native_to_json` emits them when writing
236	to the JSON ring; the consumer's `json_to_native` recognizes
237	and reconstructs them when reading from the JSON ring.
238	
239	Inside the native ring, sentinels don't appear — native values
240	are encoded in the language's own binary form, which can
241	represent its own callables / opaque values directly without
242	needing a JSON-side hack.
243	
244	## Relevant files
245	
246	- `langs/lang-spec.h` — bridges (`native_to_json`,
247	  `json_to_native`) are where sentinel handling lives per spec.
248	- `langs/lua/spec.c:188` (`encode_value`) — emit-side extension
249	  point. Currently hard-errors on function / userdata / thread;
250	  this issue replaces those errors with sentinel emission.
251	- `langs/lua/spec.c:370` (`decode_node`) — reconstruct-side
252	  extension point. Add sentinel recognition before generic
253	  object handling.
254	- `langs/c/spec.c` — C spec's `native_to_json` / `json_to_native`
255	  (currently NULL); both grow when this issue lands.
256	- `langs/bash/spec.c` — Bash spec; only `$ref` is reconstructable.
257	- `src/015-large-value-heap.c` — `$ref` storage.
258	- `scripts/soramech-compile.sh` — compile-time wire validation
259	  walks producer/consumer sentinel capabilities.
260	- `issues/312-same-language-wire-fast-path.md` — dual-ring shape;
261	  sentinels ride inside its JSON ring.
262	- `issues/246-custom-translation-specs-per-port.md` — user-side
263	  override layered on top of sentinel reconstruction.
264	- `docs/001-architecture.md` — wire format section; gains a brief
265	  sentinel reference.
266	
267	## Suggested implementation sequence
268	
269	1. **Define the canonical JSON shapes**. Three sentinel kinds,
270	   exact key names, value-object schemas. Document in
271	   `docs/001-architecture.md`.
272	2. **Lua emit**: replace the hard-errors in `encode_value` with
273	   sentinel emission per the producer protocol above. Start with
274	   `$function_pointer` for FFI callables and `$lang_opaque` for
275	   Lua functions / coroutines.
276	3. **Lua reconstruct**: extend `decode_node` to recognize the
277	   three sentinels and reconstruct them.
278	4. **Per-spec capability declarations**. Each spec declares its
279	   emit set (sentinel kinds it can produce) and reconstruct set
280	   (sentinel kinds it can consume). Lives next to the
281	   `lang_spec_t` declaration.
282	5. **C spec sentinels**. Emit via the generated wrapper based on
283	   declared output types; reconstruct via direct cast.
284	6. **Bash spec sentinels**. Only `$ref` on the reconstruct side
285	   (read bytes from the marshaller's staged file).
286	7. **Compile-time validation**. Walk every wire; check the
287	   producer's emit set against the consumer's reconstruct set;
288	   fail with a clear message on mismatch.
289	8. **Cross-process escape hatch for `$function_pointer`**.
290	   Opt-in flag in the box output declaration; the C spec emits a
291	   standalone binary instead of (or alongside) the in-process
292	   address.
293	
294	## Open questions
295	
296	- **`$ref` lifetime across iterator-fed slots**. A producer's
297	  output is referenced by an N-cell ring of consumer slots; each
298	  cell holds the `$ref`, each pop decrements. Already supported
299	  by the large-value heap's refcount, but worth verifying the
300	  test coverage when sentinels land.
301	- **Sentinel inside sentinel.** A `$ref` whose bytes are
302	  themselves JSON containing a `$function_pointer` is legal but
303	  weird. Document the recursive case; probably falls out of the
304	  walker for free.
305	- **Versioning the sentinel format**. The reserved key names
306	  (`$ref`, `$function_pointer`, `$lang_opaque`) and the
307	  value-object schemas are part of the compile-time contract.
308	  Changing them is breaking. Worth a one-line stability note
309	  somewhere.
310	- **Cross-process `$ref` semantics**. In-process `$ref` is a raw
311	  pointer + length. Cross-process (Bash via UDS), the marshaller
312	  stages the bytes at the socket boundary. Decide whether the
313	  Bash-side `$ref` reconstruction reads from a file path or
314	  inlines the bytes in the request. The latter is simpler; the
315	  former scales to bigger values.
316	
```

## Assistant

_Tools:_

- `Edit: /mnt/mtwo/programs/sora/soramech/issues/318-sentinel-primitives-in-cross-language-json.md`

### Tool result

```
The file /mnt/mtwo/programs/sora/soramech/issues/318-sentinel-primitives-in-cross-language-json.md has been updated successfully.
```

## Assistant

Logged the `bytes`-passthrough gap as a fourth open question on 318, framed as "this resolution belongs here, not on 307." Two paths are sketched: base64-wrap small payloads into a JSON string, or route through the existing `$ref` heap path for larger ones — both lean on machinery 318 already owns.

`★ Insight ─────────────────────────────────────`
- This is the right place because 307's job is "make C boxes run"; the wire-format-correctness question is a cross-spec concern that the *consumer* also has to know about. Putting it on 318 keeps the responsibility next to the producer/consumer capability declarations that will eventually validate it.
- The note hints at a small compile-time gain: if specs declare their *bytes-emit shape* (raw vs base64 vs `$ref`) alongside their sentinel sets, the validator can warn at editor time rather than producing broken JSON at runtime. Cheap signal, much friendlier than a JSON parse explosion three boxes downstream.
`─────────────────────────────────────────────────`
