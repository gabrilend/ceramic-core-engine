# Transcript

## User

Hi, can you make me a list of all the responsibilities the language specs have? Be sure to check the open issues in addition to the current behavior. I am concerned about what's planned, not what's implemented, because I am planning for what happens after the plans are implemented.

## Assistant

_Tools:_

- `Bash: ls /mnt/mtwo/programs/sora/soramech/langs/ /mnt/mtwo/programs/sora/soramech/issues/ | head -100`

### Tool result

```
/mnt/mtwo/programs/sora/soramech/issues/:
232-test-coverage-map.md
240-routing-kind-randomizer.md
241-routing-kind-weighted.md
242-routing-kind-distributor.md
243-routing-multi-band-comparator.md
244-data-box-pull-on-demand.md
245-wire-color-by-cross-language-classification.md
246-custom-translation-specs-per-port.md
247-debug-build-and-per-box-statistics-lens.md
248-encapsulated-map-as-box.md
249-tap-input-mode-radial-ports-emoji-labels.md
304-task-dispatch-layer.md
311-integration-tests-and-run-output.md
312-same-language-wire-fast-path.md
313-research-whole-program-same-language-merge.md
315-compiled-artifact-reference-counting.md
317-spec-json-bridge-for-data-boxes.md
318-sentinel-primitives-in-cross-language-json.md
completed
phase-1-progress.md
phase-2-progress.md
phase-3-progress.md

/mnt/mtwo/programs/sora/soramech/langs/:
bash
c
lang-spec.h
lua
```

## Assistant

_Tools:_

- `Read: /mnt/mtwo/programs/sora/soramech/langs/lang-spec.h`

### Tool result

```
1	/* langs/lang-spec.h — public interface for SoraMech language specs.
2	 *
3	 * What it is, in a sentence: the C contract that every language
4	 * spec (Lua, C, Bash, anything user-added) implements. The pool
5	 * runner discovers specs by dlopen()ing langs/<name>/spec.so at
6	 * startup and reading the exported soramech_lang_spec symbol.
7	 *
8	 * Designed in issue 303. The init barrier (issue 301) calls init()
9	 * for every spec on every worker before unblocking task dispatch;
10	 * the dispatch action (issue 304) calls invoke() per task; the
11	 * compile-button pipeline (issue 309) calls compile() per source
12	 * file for languages whose compile pointer is non-NULL.
13	 *
14	 * Errors: a nonzero return from any callback is fatal. The dispatch
15	 * layer prints the error context and aborts. There is no recovery,
16	 * no retry, no per-task failed state. This is intentional.
17	 *
18	 * Issue 312 extends this contract with optional fast-path
19	 * callbacks: `invoke_native` and `invoke_json` are specialised
20	 * variants the dispatch layer prefers when wire classification
21	 * says they apply; `native_to_json` and `json_to_native` are
22	 * bridges used at language boundaries. Specs that don't set them
23	 * fall back to `invoke` for every call.
24	 */
25	
26	#ifndef SORAMECH_LANG_SPEC_H
27	#define SORAMECH_LANG_SPEC_H
28	
29	#include <stddef.h>
30	
31	#ifdef __cplusplus
32	extern "C" {
33	#endif
34	
35	/* Forward declaration. `box_t` lives in src/010-graph-loader.h and
36	 * carries the full per-box record (input declarations, language,
37	 * compile-time hints like `cflags` / `link_libs` / `headers`).
38	 * Spec implementations include the loader header when they want to
39	 * read those fields; the bare forward declaration here keeps the
40	 * spec interface stand-alone for callers that only need the typedef
41	 * names. */
42	typedef struct box box_t;
43	
44	/* {{{ Shared signatures */
45	/* Invoke signature — shared by `invoke`, `invoke_native`, and
46	 * `invoke_json`. The dispatch layer picks which one to call based
47	 * on the wire-format classification in issue 312.
48	 *
49	 * Per-input format flag (`input_native[i]`): non-zero iff the
50	 * bytes in `input_data[i]` are this spec's native form; zero iff
51	 * they are JSON from a cross-language producer. The spec
52	 * dispatches internally — its decoder for that input picks the
53	 * native or JSON path based on the flag.
54	 *
55	 * Per-output format flag (`output_native`): non-zero iff the
56	 * dispatch wants the spec to write native bytes to `out_buf`,
57	 * zero iff it wants JSON. The spec produces one form per call;
58	 * cross-language fan-out is handled by the dispatch layer, which
59	 * converts via `native_to_json` once and pushes the JSON to every
60	 * cross-language consumer.
61	 *
62	 * On non-dual-ring slots (single-language inputs or single-language
63	 * outputs only), the dispatch passes NULL for `input_native` and
64	 * 0 for `output_native`; the spec falls back to its default
65	 * decode (typically JSON, which Lua's existing bridge handles).
66	 * `input_native` may be NULL even when `n_inputs > 0` — specs
67	 * must check before dereferencing. */
68	typedef int (*lang_invoke_fn)(void *handle,
69	                              const box_t *box,
70	                              const char *file_path,
71	                              const char *fn_name,
72	                              const void **input_data,
73	                              const int   *input_sizes,
74	                              const int   *input_native,
75	                              int          n_inputs,
76	                              int          output_native,
77	                              void        *out_buf,
78	                              int          out_buf_capacity,
79	                              int         *out_size);
80	
81	/* `box` is the box record being invoked, or NULL when the caller has
82	 * no box context (e.g. test fixtures driving the spec directly).
83	 * Specs that need declared return type, compile hints, or future
84	 * sentinel-emission decisions read fields off `box`. Specs that
85	 * don't need it ignore the parameter. The pointer is valid only for
86	 * the duration of the call. */
87	
88	/* Wire-format bridge: convert one direction of native ↔ JSON.
89	 * Used when a value crosses a language boundary. Spec sets one
90	 * or both; if NULL, the dispatch layer treats native and JSON as
91	 * identical byte streams (the current uniform-byte default).
92	 *
93	 * `handle` is the worker's per-language handle (the same one
94	 * `invoke` receives) — bridges may need it to reach a runtime-side
95	 * scratch area: Lua reads/writes via the lua_State stack, C may
96	 * keep a typed-pointer table, Bash may need its sub-process. Specs
97	 * that don't need it ignore the argument.
98	 *
99	 * Stack-protocol convention for `native_to_json`: the producer
100	 * leaves its native value on top of its runtime's working store
101	 * (Lua: top of stack); the bridge consumes it. For `json_to_native`:
102	 * the bridge pushes the reconstructed value onto the same store.
103	 * `src` / `dst` carry only the JSON side. */
104	typedef int (*lang_bridge_fn)(void *handle,
105	                              const void *src, int src_size,
106	                              void *dst, int dst_capacity, int *dst_size);
107	/* }}} */
108	
109	/* {{{ lang_spec_t */
110	typedef struct lang_spec {
111	    const char *name;       /* "lua", "bash", "c", "python", ... */
112	    const char *file_ext;   /* ".lua", ".sh", ".c", ".py"        */
113	
114	    /* init: once per worker thread at pool startup. Returns an
115	     * opaque handle stored in the worker context; the dispatch
116	     * layer passes this handle back to invoke() for every call. */
117	    void *(*init)(int worker_idx);
118	
119	    /* teardown: once per worker at pool shutdown. Frees the handle
120	     * and any per-worker resources (e.g. lua_State, dlopen caches,
121	     * persistent socket connections). */
122	    void  (*teardown)(void *handle);
123	
124	    /* compile: optional. Called once per source file at map-load
125	     * (or at editor compile-button time). For languages that don't
126	     * compile (Lua, Bash), leave this NULL. For C, it runs
127	     * gcc -shared -fPIC -o out_path src_path, plus any per-box
128	     * compile flags / link libraries / headers the box declared.
129	     *
130	     * `box` is the box record for the box being compiled, or NULL
131	     * if the caller is compiling a source file with no box context
132	     * (e.g. the C spec's internal lazy-compile path that maps a raw
133	     * `.c` ref to a `/tmp/<hash>.so` on the fly). When non-NULL,
134	     * the spec may read box->cflags / box->link_libs / box->headers
135	     * to customise the compile. Returns 0 on success, nonzero on
136	     * failure. */
137	    int   (*compile)(const char *src_path, const char *out_path,
138	                     const box_t *box);
139	
140	    /* invoke: per box call. Inputs are arrays of (bytes, size) pairs,
141	     * already read from slots by the dispatch layer. Returns the
142	     * function's output by writing to out_buf and setting *out_size.
143	     * Returns 0 on success, nonzero (fatal) on error.
144	     *
145	     * This is the canonical entry point; every spec implements it.
146	     * The dispatch layer falls back to it when the specialised
147	     * paths below are not set. */
148	    lang_invoke_fn invoke;
149	
150	    /* invoke_native — optional fast path used when every wire
151	     * adjacent to this box (input and output) stays inside this
152	     * spec's language. Bytes on the wire are this language's
153	     * native serialisation (Lua tables via msgpack, C struct
154	     * memcpy, etc.) and the encode/decode round trip is skipped.
155	     * NULL means the spec hasn't specialised; dispatch falls
156	     * back to `invoke`. Issue 312. */
157	    lang_invoke_fn invoke_native;
158	
159	    /* invoke_json — optional universal-interop path. Bytes on the
160	     * wire are JSON. NULL means the spec hasn't specialised;
161	     * dispatch falls back to `invoke`. Issue 312. */
162	    lang_invoke_fn invoke_json;
163	
164	    /* Bridges between native and JSON wire forms. Used when a
165	     * value crosses a language boundary — the producer writes
166	     * native, the dispatch layer converts via the producer's
167	     * native_to_json, hands the JSON bytes to the consumer's
168	     * json_to_native. NULL means the spec hasn't specialised;
169	     * dispatch treats native and JSON as identical bytes. Issue
170	     * 312. */
171	    lang_bridge_fn native_to_json;
172	    lang_bridge_fn json_to_native;
173	} lang_spec_t;
174	/* }}} */
175	
176	#ifdef __cplusplus
177	}
178	#endif
179	
180	#endif /* SORAMECH_LANG_SPEC_H */
181	
```

## Assistant

_Tools:_

- `Bash: grep -l -i "spec\|language" issues/*.md 2>/dev/null`

### Tool result

```
issues/240-routing-kind-randomizer.md
issues/243-routing-multi-band-comparator.md
issues/244-data-box-pull-on-demand.md
issues/241-routing-kind-weighted.md
issues/304-task-dispatch-layer.md
issues/317-spec-json-bridge-for-data-boxes.md
issues/249-tap-input-mode-radial-ports-emoji-labels.md
issues/242-routing-kind-distributor.md
issues/246-custom-translation-specs-per-port.md
issues/318-sentinel-primitives-in-cross-language-json.md
issues/313-research-whole-program-same-language-merge.md
issues/248-encapsulated-map-as-box.md
issues/phase-2-progress.md
issues/232-test-coverage-map.md
issues/247-debug-build-and-per-box-statistics-lens.md
issues/245-wire-color-by-cross-language-classification.md
issues/phase-3-progress.md
issues/312-same-language-wire-fast-path.md
issues/315-compiled-artifact-reference-counting.md
issues/311-integration-tests-and-run-output.md
issues/phase-1-progress.md
```

## Assistant

_Tools:_

- `Agent: Survey language spec responsibilities`

### Tool result

```
Perfect. Now I have all the information needed. Let me synthesize the spec contract changes from the issues:

## Language Spec Contract Changes from Planned Issues

Based on the 13 issues reviewed, here are the NEW responsibilities that will be added to or CHANGED in the `lang_spec_t` contract:

**Issue 246 — Custom Translation Specs Per Port**
- **Custom decode on input ports**: The dispatch gains a per-port branch that calls a user-supplied custom translation function before passing input to `invoke`. The spec remains unchanged but must accept an additional `input_native` parameter that the custom shim also receives—specs need to handle cases where custom shims have already decoded the input and mark it as native via the flag.

**Issue 312 — Same-Language Wire Fast Path**
- **`input_native[]` array in invoke signature** (already reserved in header): The spec now *must* dispatch internally on `input_native[i]` per input. No longer can assume all inputs are one format.
- **`output_native` flag** (already reserved): The spec must produce either native or JSON bytes in one form per call, selected by this flag.
- **`native_to_json` and `json_to_native` bridges are now *required*** (not optional): Every spec must implement both or stub with hard-error. These drive cross-language wire translation and data-box semantics.

**Issue 317 — Language Spec JSON Bridge for Data Boxes**
- **Data box language execution**: Specs gain responsibility for executing `.lua`/`.c`/`.sh` files in data boxes and returning native values that `native_to_json` then converts. The dispatch will call the spec to load and run the file, not just push literal bytes.

**Issue 318 — Sentinel Primitives in Cross-Language JSON**
- **Sentinel emission on `native_to_json`**: The spec must emit three sentinel kinds (`$ref`, `$function_pointer`, `$lang_opaque`) when native values cannot be structurally represented in JSON. Lua must replace hard-errors on closures/userdata with sentinel emission.
- **Sentinel reconstruction on `json_to_native`**: The spec must recognize and reconstruct sentinels, raising compile-time errors when the consumer language cannot handle the sentinel kind.
- **Per-spec capability declarations**: Each spec must declare which sentinel kinds it can emit and which it can reconstruct.

**Issue 248 — Encapsulated Map as Box**
- **New language "map"**: A new spec (`langs/map/spec.so`) joins the registry with `lang: "map"` boxes. Its `init` loads sub-maps, `invoke` runs the sub-graph and packs outputs into the null-terminated size-index format, and `compile` either inlines or validates the sub-graph.

**Issue 313 — Whole-Program Same-Language Merge**
- **Merged-module loading (Lua, future C/Bash)**: The spec gains `load_via_merged()` on the cache-miss path—when `__merged__.<ext>` exists alongside the source file, the spec loads the merged module once per worker and indexes it by basename. Direct function calls inside the merged region bypass the spec's `invoke` path entirely (dispatch optimization, not spec responsibility, but specs need awareness of the merge).

All other issues (245, 247, 249, 244, 304, 315, 311) touch the editor, runtime dispatch, or tooling—they do not change the spec contract itself.
```

## Assistant

# Language Spec Responsibilities — The Planned End State

Below is the consolidated list, blending today's `lang-spec.h` contract with what the open issues will require. I've grouped by responsibility, not by issue.

## A. Lifecycle & registration
- **Identify itself**: provide `name` (`"lua"`, `"c"`, ...) and `file_ext` so the loader can dlopen `langs/<name>/spec.so` and resolve source files.
- **Per-worker init**: stand up the runtime (lua_State, dlopen cache, bash subprocess) once per worker thread, return an opaque handle.
- **Per-worker teardown**: free everything the handle owns at pool shutdown.

## B. Source → executable
- **Compile a source file** (optional, NULL if not applicable) into an artifact the spec can later invoke, honoring per-box `cflags` / `link_libs` / `headers` when a `box_t` is passed.
- **Lazy/no-box compile** for ad-hoc references (the C spec's `/tmp/<hash>.so` path).

## C. The three invoke variants (issue 312)
- **`invoke` (canonical)**: per-call entry; reads each input's `input_native[i]` flag and dispatches internally to its native or JSON decoder; writes output in the form requested by `output_native`.
- **`invoke_native` (optional fast path)**: all neighboring wires are same-language; bytes are this spec's native serialisation, no encode/decode round-trip.
- **`invoke_json` (optional universal path)**: all wires are JSON.

## D. Wire-format bridges (issues 312, 318)
- **`native_to_json`**: drain the top of the runtime's working store, emit JSON bytes. Becomes *required*, not optional, once 312 lands.
- **`json_to_native`**: parse JSON bytes, push reconstructed value onto the runtime's working store.
- **Sentinel emission**: when a native value isn't structurally JSON-representable, emit one of `$ref`, `$function_pointer`, `$lang_opaque` instead of erroring.
- **Sentinel reconstruction**: recognise those sentinels on the inbound side and rebuild a runtime value, or raise a compile-time error if this language can't host that kind.
- **Capability declaration**: each spec declares which sentinel kinds it can emit and which it can reconstruct, so cross-language wires can be validated before run.

## E. Data-box execution (issue 317)
- **Run a source file as a value producer**: load `.lua` / `.c` / `.sh` from a data box, execute it, hand back a native value, and let `native_to_json` finish the trip onto the wire.

## F. Custom per-port translation (issue 246)
- **Cooperate with user-supplied port shims**: when a shim has pre-decoded an input, `invoke` must trust the `input_native[i]` flag the shim set, rather than re-decoding.

## G. Encapsulated-map spec (issue 248)
- A new `langs/map/spec.so` joins the registry as a peer language. Its `invoke` runs a sub-graph; its `compile` either inlines the sub-graph or validates it. (This is a new spec, not a new responsibility for existing ones — but it widens what "a language spec" means.)

## H. Whole-program merged loading (issue 313, research)
- **Awareness of `__merged__.<ext>`**: on cache miss, if a merged module exists alongside source, load it once per worker and index by basename. Calls inside the merged region bypass `invoke` entirely — the spec must be okay with the dispatch layer reaching past it.

## Not the spec's job (clarifying the boundary)
The following open issues touch the surrounding system but do **not** add spec responsibilities: 245 (wire color in editor), 247 (debug build / stats lens), 249 (tap input UI), 244 (pull-on-demand data boxes — dispatch concern), 304 (dispatch action itself), 311 (integration tests), 315 (artifact refcounting — done outside the spec).

`★ Insight ─────────────────────────────────────`
- The spec contract is migrating from "decode JSON, run, encode JSON" toward "be a translator between a runtime's native value store and a shared JSON wire." After 312+318 land, **the bridges are the heart of the spec**, not `invoke` — `invoke` becomes a thin shell that delegates to the bridges based on flags.
- Sentinel capability declarations (H) are the first piece of *meta* responsibility: the spec describes what it *can* do, not just *what to do*. That shape — declared capabilities consulted at graph-load — is likely to recur (e.g. future "can stream", "can run pure", "can be merged").
- Issue 248's `map` spec quietly establishes that "language" is just "anything that satisfies the `lang_spec_t` contract." Once a non-textual, non-compiled thing is a spec, the contract itself becomes the abstraction boundary of SoraMech — which is the layer you'll want to design *next* against.
`─────────────────────────────────────────────────`

A natural question to chew on while you plan: once the bridges carry the real work and `invoke` is mostly a flag-router, is there still a reason for three separate invoke entry points, or does that collapse into one `invoke(flags...)` with the fast paths becoming an internal optimisation the spec — not the dispatch — chooses?

## User

  C. The three invoke variants (issue 312)

  - invoke (canonical): per-call entry; reads each input's input_native[i] flag and dispatches
  internally to its native or JSON decoder; writes output in the form requested by output_native.
  - invoke_native (optional fast path): all neighboring wires are same-language; bytes are this
  spec's native serialisation, no encode/decode round-trip.
  - invoke_json (optional universal path): all wires are JSON.


can you tell me why we have invoke, and then invoke_(native|json)? why not drop the canonical invoke, and always route to the correct of the other two types? Is there a valid reason for that extra option?

  - Sentinel emission: when a native value isn't structurally JSON-representable, emit one of
  $ref, $function_pointer, $lang_opaque instead of erroring.

my understanding for the function pointers, and other similar datum, was that we were going to take the function that is pointed at (and all of it's dependencies, if any) and compile it, on-the-fly if necessary but preferrably during the map's compile stage. Then, we were going to ask the language spec to write a little wrapper that runs a piece of code from another language - essentially every language can ask the OS to execute a binary with certain arguments, so if we can write into each spec how specifically to generate such wrappers, then we can successfully call other functions and such in other languages.

  E. Data-box execution (issue 317)

  - Run a source file as a value producer: load .lua / .c / .sh from a data box, execute it, hand
  back a native value, and let native_to_json finish the trip onto the wire.


Hmmmmm..... how is that different than a call box? Running a function, returning a number... I dunno feels pretty call-y to me.

  F. Custom per-port translation (issue 246)

  - Cooperate with user-supplied port shims: when a shim has pre-decoded an input, invoke must
  trust the input_native[i] flag the shim set, rather than re-decoding.


for this, it should be just as simple as moving a function pointer on the box that points to the default spec, to point to the custom specific spec. Is that right? Is that how we implement this part of the behavior?

  G. Encapsulated-map spec (issue 248)

  - A new langs/map/spec.so joins the registry as a peer language. Its invoke runs a sub-graph;
  its compile either inlines the sub-graph or validates it. (This is a new spec, not a new
  responsibility for existing ones — but it widens what "a language spec" means.)


I'm not sure what this means, can you help me out?

## Assistant

Good probing questions — let me answer each, and a few I'll bounce back because the design call is yours.

## 1. Why `invoke` alongside `invoke_native` / `invoke_json`?

The two fast paths handle the *uniform* cases: every input native, or every input JSON. They don't handle a third case that genuinely exists: **a single call where some inputs are native and others are JSON** (a Lua box reading from one Lua producer and one C producer). That's the case the canonical `invoke` covers, by routing per-input using the `input_native[i]` flag.

You *could* eliminate it — but only by mandating that the dispatch layer always normalise inputs to one form before the call (e.g. "if any input is JSON, convert everything to JSON, call `invoke_json`"). That's a real design choice with a real cost: you'd be doing one needless `native_to_json` per non-matching input on every mixed call.

`★ Insight ─────────────────────────────────────`
- `invoke` isn't really a third *variant* — it's the **general form** that the other two are *specialisations* of. The two fast paths exist because they let the spec skip per-input branching and per-call format checks in the hot loop.
- A simpler contract would be: spec implements only `invoke`, and the dispatch layer hints "all native" or "all JSON" via the flags. The spec internally chooses the cheap path. That trades a few branches for one fewer entry point.
─────────────────────────────────────────────────

**Question back:** is the hot-loop branch cost worth a second entry point to you, or would you rather collapse to one `invoke(flags…)` and let the spec author decide internally?

## 2. Function pointers — your model vs. issue 318's sentinel

You're describing something quite different from 318, and frankly more concrete. Let me play it back:

> "Compile the pointed-at function and its dependencies at map compile time. Then ask each language spec for a recipe: how does *your* language shell out to a binary with arguments? Generate a wrapper from that recipe."

That's a **`generate_foreign_call_wrapper(target_lang, fn_signature)`** responsibility — each spec knows how *it* shells out (Lua: `io.popen`, C: `execve`, Bash: literal subshell), and the compiler stamps wrappers at boundary crossings. The wire just carries the path to the compiled binary plus arguments.

Issue 318 currently says the spec emits a `$function_pointer` sentinel and the consumer reconstructs it. That's much vaguer — it doesn't say *how* the consumer turns the sentinel back into something callable.

`★ Insight ─────────────────────────────────────`
- Your design moves the work to compile time and makes it visible (a wrapper is a real file you can read); the sentinel design defers it to runtime and hides it in the bridge. The former is more debuggable and aligns with the "specs are translators" framing already emerging.
- It also means a function-pointer crossing isn't really "cross-language" at the data layer at all — it's *two same-language calls* with a process boundary in the middle. That's a strictly simpler mental model.
─────────────────────────────────────────────────

**Question back:** should I open an amendment to 318 that replaces `$function_pointer` sentinel-passing with the compile-time wrapper-generation approach? Want me to write that up so you can review?

## 3. Data-box-execution vs. call-box — is it the same thing?

You're right to be suspicious. A call box runs a function and emits its return; a "source file in a data box" runs a function and emits its return. The only mechanical differences I can think of:

- Data boxes have no input ports (zero-arg call).
- Data boxes might be evaluated lazily / on pull (issue 244) rather than on every tick.
- Data boxes were originally meant to hold *literal* values, not code.

Mechanically that's just **a call box with zero inputs and pull semantics**. Issue 317 is, on a charitable read, introducing this only because today's data boxes don't run code at all — but the right fix may be to retire the distinction rather than teach data boxes a new trick.

**Question back:** do you want data boxes to remain a distinct *kind* (literal-only, no source files), and any source-file-as-value becomes a zero-input call box? That would eliminate 317's spec responsibility entirely.

## 4. Custom per-port translation — is it just swapping a function pointer?

Yes, that's exactly right. Today the dispatch path for input `i` is conceptually:

```
spec_for(box).json_to_native(...)   // or native passthrough
```

With per-port custom shims, the per-port slot stores its own `lang_bridge_fn` pointer, and the dispatch path becomes:

```
port[i].decode(...)   // defaults to the spec's bridge if user hasn't overridden
```

No new spec responsibility — it's a dispatch-side indirection. The spec doesn't even need to know whether the bridge that produced its input was the default one or a custom one; it just trusts `input_native[i]`. So I'd revise my earlier answer: **issue 246 is not actually a spec-facing change**, it's a slot/port-table change.

## 5. Encapsulated map as a "language"

Think of it this way. Today the dispatch layer has rules like:

- If `box.lang == "lua"`, look up the Lua spec, call it.
- If `box.lang == "c"`, look up the C spec, call it.
- If `box.lang == "map"`, ??? (currently special-cased: open the sub-map, walk it, splice outputs).

Issue 248 says: **stop special-casing maps**. Write a `langs/map/spec.so` that satisfies the same `lang_spec_t` contract every other language does. Its `init` loads sub-maps; its `invoke` runs the sub-graph and returns the outputs in the same packed-bytes form a Lua function would. From the dispatch layer's point of view, a `"map"` box is now indistinguishable from a `"lua"` box.

`★ Insight ─────────────────────────────────────`
- This is the "language spec is the abstraction boundary" point made flesh. Once a *graph* satisfies the language-spec interface, the dispatch layer doesn't know — and doesn't need to know — whether it's executing source code or a sub-graph. Both are just "something callable that conforms."
- The same pattern would let you eventually add `lang: "remote"` (call across the network), `lang: "shell"` (literal bash one-liner stored inline), `lang: "constant"` (which is basically what a literal data box already is). At that point, the spec contract becomes the *only* extension point in the system.
─────────────────────────────────────────────────

**Question back:** if maps-as-specs goes through, does it tempt you to also retire the box `lang` field entirely and just have a `spec` field that points at the loaded `lang_spec_t`? It would make the encapsulation truly flat.

## User

  1. Why invoke alongside invoke_native / invoke_json?

  The two fast paths handle the uniform cases: every input native, or every input JSON. They don't
   handle a third case that genuinely exists: a single call where some inputs are native and
  others are JSON (a Lua box reading from one Lua producer and one C producer). That's the case
  the canonical invoke covers, by routing per-input using the input_native[i] flag.

  You could eliminate it — but only by mandating that the dispatch layer always normalise inputs
  to one form before the call (e.g. "if any input is JSON, convert everything to JSON, call
  invoke_json"). That's a real design choice with a real cost: you'd be doing one needless
  native_to_json per non-matching input on every mixed call.

  ★ Insight ─────────────────────────────────────
  - invoke isn't really a third variant — it's the general form that the other two are
  specialisations of. The two fast paths exist because they let the spec skip per-input branching
  and per-call format checks in the hot loop.
  - A simpler contract would be: spec implements only invoke, and the dispatch layer hints "all
  native" or "all JSON" via the flags. The spec internally chooses the cheap path. That trades a
  few branches for one fewer entry point.
  ─────────────────────────────────────────────────

  Question back: is the hot-loop branch cost worth a second entry point to you, or would you
  rather collapse to one invoke(flags…) and let the spec author decide internally?


my understanding was that native was the default, and if the value is in json we decode it to native first. Then, we only have one call - just invoke - and it handles just the native case, because whenever there's a json value we decode it first. Okay that's a little unclear, let me re-explain - during the dispatch layer, we look at the inputs. We pop the next one off of each input ring buffer, and if it happens to be json then we parse it at this time into a native value. Then, we invoke the box's function using those arguments in native format (because we just parsed any json). In this design, we don't need to have multiple invoke variants. It's just, always native, and sometimes there's some "lag" between the box being started and the function running, because we have to do that translation sometimes. No branching, just "hey input slot buffer notebook, what's the next most recent value? oh this one's in the json ring buffer for this input slot? great I'll pop that and do the encoding, talk to you later" then it just does that. No branching, just execution.

  2. Function pointers — your model vs. issue 318's sentinel

  You're describing something quite different from 318, and frankly more concrete. Let me play it
  back:

  ▎ "Compile the pointed-at function and its dependencies at map compile time. Then ask each
  ▎ language spec for a recipe: how does your language shell out to a binary with arguments?
  ▎ Generate a wrapper from that recipe."

  That's a generate_foreign_call_wrapper(target_lang, fn_signature) responsibility — each spec
  knows how it shells out (Lua: io.popen, C: execve, Bash: literal subshell), and the compiler
  stamps wrappers at boundary crossings. The wire just carries the path to the compiled binary
  plus arguments.

  Issue 318 currently says the spec emits a $function_pointer sentinel and the consumer
  reconstructs it. That's much vaguer — it doesn't say how the consumer turns the sentinel back
  into something callable.

  ★ Insight ─────────────────────────────────────
  - Your design moves the work to compile time and makes it visible (a wrapper is a real file you
  can read); the sentinel design defers it to runtime and hides it in the bridge. The former is
  more debuggable and aligns with the "specs are translators" framing already emerging.
  - It also means a function-pointer crossing isn't really "cross-language" at the data layer at
  all — it's two same-language calls with a process boundary in the middle. That's a strictly
  simpler mental model.
  ─────────────────────────────────────────────────

  Question back: should I open an amendment to 318 that replaces $function_pointer
  sentinel-passing with the compile-time wrapper-generation approach? Want me to write that up so
  you can review?


shelling out to a binary with arguments is the only real path forward I can see. If we rely on language specific integrations like luaJIT's FFI interface, then... it's not only going to be brittle, but also it'll become very specific to languages that use those. We want to be agnostic if we can. So, yes, I think the design I just described is better. Unless you can think of any arguments against?

  Question back: if maps-as-specs goes through, does it tempt you to also retire the box lang
  field entirely and just have a spec field that points at the loaded lang_spec_t? It would make
  the encapsulation truly flat.

yeah actually I like having pointers for that sort of thing.

## Assistant

Three design calls registered. Two clean confirmations and one that has a real wrinkle worth thinking through.

## 1. Single `invoke`, dispatch decodes JSON-to-native at the ring

Your design is cleaner than what's in the header today. Let me play it back to make sure I have it:

- Each input slot has (conceptually) a native ring and a JSON ring fed by upstream.
- Dispatch pops the next-most-recent value from whichever ring it came from.
- If it came from the JSON ring, dispatch calls the consuming spec's `json_to_native` *before* invoke, pushing the value onto the spec's runtime store.
- By the time `invoke` runs, every argument is native. One entry point, no flags.

That works. One thing worth naming for symmetry: **outputs still need bridging** when fan-out is cross-language. If a Lua box feeds one Lua consumer and one C consumer, the Lua box produces native Lua once; dispatch calls `native_to_json` once and pushes the JSON onto the C-side ring. That's still bridge work, but it's symmetric with the input path and not "another invoke variant." So you end up with: one `invoke`, plus the two bridges. The three-variant invoke goes away entirely.

`★ Insight ─────────────────────────────────────`
- Your design also makes the spec **simpler to write** — the spec author never has to reason about "what if some of my inputs are JSON?" That branching moves into the dispatch layer where it belongs (dispatch already knows the wire format because it just popped the value).
- The "lag" you describe is real but invariant — same total decoding work, just done eagerly at pop time instead of lazily inside invoke. The only difference is *who* drives it. Putting the driver in dispatch keeps specs honest: they speak one language, native.
- There's a nice second-order effect: this design means `json_to_native` is called *exactly* at language boundaries (where a JSON-ring value enters a native runtime), which is the only place it should ever be called. Today's contract permits specs to internally decide whether to decode, which is one degree of freedom too many.
─────────────────────────────────────────────────

I'll plan to amend issue 312 along these lines once you confirm. Sound right?

## 2. Shell-out wrappers — yes, with one wrinkle: persistent subprocesses

You asked for arguments against. I tried to think of real ones, not strawmen:

- **Fork+exec cost is ~1ms per call.** For a tick-driven dataflow loop where cross-language calls happen per tick, that dominates everything. FFI is microseconds. This is the main reason teams reach for FFI even though it's brittle.
- **State doesn't survive across calls.** A function pointer that holds a counter, a loaded model, a cached connection — all gone the moment the subprocess exits. The wrapper sees a fresh binary every call.
- **Argument enumeration is hard in dynamic languages.** Lua closures capture upvalues and `_ENV`. Statically figuring out *all the dependencies* of a function pointer at compile time is the FFI problem in different clothes.

But — and this is the part that resolves it for me — the **threading roadmap already commits to Unix domain sockets for cross-language IPC** (per memory). So the actual implementation isn't "fork+exec per call." It's:

- At map compile time, generate the wrapper *and* spawn the target-language subprocess once. It listens on a Unix socket.
- The wrapper, when called, sends `(fn_name, args)` over the socket and waits for the result.
- State lives in the long-running subprocess. Latency is socket round-trip, ~10–50µs, not fork latency.

That sidesteps the cost concern *and* the state concern in one move. The subprocess becomes the natural home for the function pointer's closure state.

`★ Insight ─────────────────────────────────────`
- The shell-out-binary-with-args framing and the persistent-socket-subprocess framing aren't actually different designs — the first is the simple-case skin of the second. You start with `execve(binary, args)`, and as soon as you want repeat calls to cheap, you keep the binary running and talk via socket. Both still respect the agnostic principle: every language can speak a socket protocol, no language-specific FFI required.
- This also means the language-spec contract gains *exactly one new recipe* per language: "how do you generate a wrapper that does a socket round-trip and decodes the reply?" That's a small, well-bounded responsibility, much more so than "implement a sentinel reconstruction path."
- Argument enumeration (the upvalue problem) becomes the user's job at map-compile time, which is honest: if you're putting a function pointer on a wire, you owe the system a sufficient description of what it closes over. The map compiler can refuse to compile pointers it can't enumerate, which is a clear, debuggable failure mode.
─────────────────────────────────────────────────

So: no real arguments against your design, provided we plan the subprocess-and-socket path from the start rather than ship `execve`-per-call and discover the latency cliff later.

**Question back:** when I rewrite 318, should I scope it to "function pointer crossings via long-lived subprocess + socket, wrapper generated by spec," and explicitly cite the threading roadmap as the IPC mechanism? Or do you want a smaller first cut that does `execve` per call and earns the socket later?

## 3. Spec field is a pointer

Noted. So the box record will eventually carry `lang_spec_t *spec` instead of `const char *lang`. The string lookup happens once, at graph-load, when the loader resolves the name to the dlopen'd module — and from then on it's pointer-equality and direct dispatch.

That also means **encapsulated-map boxes, function-pointer-wrapper boxes, and source-code boxes all carry the same field shape** — just pointed at different specs. That's the "flat encapsulation" payoff: the dispatch layer's case analysis on `box->lang == "..."` disappears, replaced by an indirect call through a `lang_spec_t *`. The dispatch loop becomes about a dozen lines.

## User

Yeah this all looks great. I think the 1ms time cost is probably too high, but I don't like the brittleness of FFI because it's just for one specific language pairing - Lua to C. Not ideal, we can't design soramech's systems around a custom integration. We have to be agnostic. What you're describing with Unix IPC... That's agnostic enough. I'm sure, if we needed to, we could design something similar for Windows. Or maybe Windows would just, speak that language a bit for us. Like a loan word, like burrito or sevastavan.

Can you do another pass and just make sure there's no concerns you can think of before we update 312? we're about to implement it tho so we gotta go quickly...

## Assistant

Doing the pre-flight. There are two real concerns that need a decision *before* you write code, and a few smaller ones that are fine to handle as they come up.

## Must decide before implementing

### Concern A — Where do decoded native values *live*?

The current `invoke` signature takes `input_data[]` as bytes-and-sizes. But your new design says dispatch decodes JSON to native *before* invoke. So what does invoke receive?

Two coherent answers:

1. **Native lives in the runtime store.** Dispatch calls `json_to_native(handle, json_bytes)` which pushes onto Lua's stack (or C's typed-pointer table, etc.). `invoke` takes only `n_inputs` and pulls from the store directly. The bytes arrays in the current signature go away.
2. **Native is a separate byte form.** Dispatch decodes JSON-bytes → native-bytes (e.g. msgpack for Lua). `invoke` receives those native bytes and decodes them into runtime values itself.

(1) is faster and aligns with the existing bridge convention (Lua's bridge already pushes/pops via stack). (2) keeps the current invoke signature but means every spec maintains *two* serialised forms — a native one and JSON — which is awkward.

I recommend (1). But it means **the invoke signature changes shape** — that's a contract change, not just adding/removing fast paths. Worth being explicit about in the issue.

### Concern B — What does "same-language wire" actually mean?

Two same-language boxes (Lua → Lua) don't share a `lua_State` (each worker has its own; boxes don't generally share state). So the value can't just sit on a stack across the boundary. *Something* has to serialise it.

Two options:

1. **Same-language wires use language-native bytes (msgpack for Lua, struct memcpy for C).** That needs **two more bridge functions per spec**: `native_to_bytes` and `bytes_to_native`. Cheap, agnostic, but adds to the contract.
2. **Same-language wires force producer and consumer onto the same worker, share the stack directly.** Much faster (no serialisation at all) but it constrains the scheduler — you've now coupled wire topology to worker affinity. Once a wire goes cross-language, that constraint disappears, so it's a heterogeneous policy.

If you want the fast path to be genuinely fast, you probably want (2) for same-language and (1) only as the fallback when affinity can't be honoured. But (2) is the bigger commitment — it touches the scheduler, not just the spec contract.

Worth declaring which one you're committing to in 312, or marking the same-language fast path as "deferred decision; route through JSON for now."

## Smaller concerns — flagging, not blocking

- **Bridge contract becomes mandatory, not optional.** Existing specs (Lua, C, Bash) all need bridges before 312 lands or the whole runtime breaks. Worth a migration note.
- **`invoke_native` / `invoke_json` removal touches the existing header comments and `langs/{lua,c,bash}/spec.c` files** — small mechanical sweep, easy to miss.
- **Error semantics shift slightly**: today a malformed JSON input is the spec's problem; under the new flow, `json_to_native` raises at the dispatch layer before invoke ever runs. That's actually *better* (errors point at the bridge, not the function) but worth calling out so the dispatch error path knows to report "input N of box X failed json_to_native" rather than "invoke returned nonzero."
- **`box_t *box` parameter to invoke** — keep it. Specs still need it for sentinel emission policy, declared return type, compile hints.
- **Output fan-out**: when a Lua box has both Lua and C consumers, dispatch needs to convert the output *twice* — once via `native_to_bytes` (or stack-share) for Lua consumers, once via `native_to_json` for the C consumer. Same value, two routes. The bridge contract handles it but the dispatch code needs to remember to call both, not just one.

`★ Insight ─────────────────────────────────────`
- The contract is migrating from "one entry per format" to "one entry, plus four directional bridges." Symmetric and predictable: `{json, native_bytes} × {to_runtime, from_runtime}`. That's a coordinate system, not a list of cases — much easier to reason about as the system grows.
- Concern B is the one that has long-term scheduling implications. A → B → C where A,B are Lua and C is C: do A and B *have* to be on the same worker? If yes, you've made worker assignment a graph-coloring problem. If no, you've given up the stack-sharing fast path. Worth committing to one answer in 312, even if the commitment is "defer — route through JSON for v1."
─────────────────────────────────────────────────

## My recommendation for 312 scope

- Resolve Concern A as design (1): native lives in the runtime store; invoke loses its bytes arrays.
- Resolve Concern B by **explicitly deferring** the worker-affinity question and routing same-language wires through `native_to_bytes` / `bytes_to_native` (msgpack-style) for v1. The faster stack-sharing path becomes a future issue.
- Make all four bridges (`json_to_native`, `native_to_json`, `bytes_to_native`, `native_to_bytes`) required, not optional.
- Delete `invoke_native` and `invoke_json` from the contract.

Sound like the right scope? If yes, I'll start the implementation now.

## User

  1. Native lives in the runtime store. Dispatch calls json_to_native(handle, json_bytes) which
  pushes onto Lua's stack (or C's typed-pointer table, etc.). invoke takes only n_inputs and pulls
   from the store directly. The bytes arrays in the current signature go away.


  Concern B — What does "same-language wire" actually mean?

  Two same-language boxes (Lua → Lua) don't share a lua_State (each worker has its own; boxes
  don't generally share state). So the value can't just sit on a stack across the boundary.
  Something has to serialise it.


the values are stored in shared memory. This question is about native->native specifically so I won't discuss json here - the values are written, in the format that lua expects, to a piece of shared memory by the producer thread. This piece is the slot allocated for the box's native ring buffer. A pointer to that slot is added to the "FIFO notebook" in the ring buffer manager. Then, when the consumer thread reads from the the ring buffer, the value is pushed to the lua stack, or however else the language is specced to consume values. In native->native, they are stored as bytes, exactly as the language spec expects them, because that's the way they were emitted. I don't think this needs extra bridge functions - it should be part of the functions that write return values to input slot ring buffers. There's no such thing as "bytes vs native" - there is only native.

please don't do any implementation right now. Just update the issue files for all the issues we discussed. I think it was 312 and 318 primarily...?

## Assistant

Got it — the model is simpler than I was making it. Let me play it back in one sentence to confirm, then update the files.

**The model:** Native means "the spec's bytes written into a shared-memory slot, in whatever format that spec expects." Same-language wires write native bytes into a slot, and the consumer reads native bytes from the same slot — nothing in between. JSON-side wires get normalised by `json_to_native` (JSON bytes → native bytes in slot) at pop time, so by the time `invoke` runs, every input slot holds native bytes regardless of producer. Cross-language outputs are handled symmetrically by `native_to_json` after invoke. Two bridges. No "bytes-vs-native" distinction — native *is* bytes.

Let me read the two issues before editing.

## Assistant

_Tools:_

- `Read: /mnt/mtwo/programs/sora/soramech/issues/312-same-language-wire-fast-path.md`

### Tool result

```
1	# 312 — Same-language wire fast path: dual-ring slot, JSON at language borders
2	
3	## Status
4	substantively complete (2026-05-21) — dual-ring slots, per-edge
5	classification, spec invoke signature with per-input flags, Lua
6	input-side branching, and dispatch consultation all landed. The
7	deferred output-side coordinated format (cross-language JSON
8	emission from Lua / C / Bash) lives in issues 307 and 308; this
9	issue's design covers it but the implementation crosses into
10	each spec's per-language work.
11	
12	## Current behavior
13	
14	Today every wire's bytes are whatever the producing spec wrote, and
15	the consuming spec reads them verbatim. The Lua spec emits JSON via
16	`lua_native_to_json` (`langs/lua/spec.c:335`); the C and Bash specs
17	have no bridge implementations yet and only carry whatever bytes
18	their bytes-in-bytes-out `invoke` produced.
19	
20	The dispatch layer picks the spec callback (`invoke_native` /
21	`invoke_json` / fallback `invoke`) based on a single per-box
22	`use_native_invoke` bit computed at graph load — true only when
23	**every** input edge and **every** output edge of the box stays
24	within one language. That's the pessimistic per-box approximation:
25	a box whose output fans to one same-language consumer and one
26	cross-language consumer is classified as cross-language for the
27	whole box, so the same-language neighbor eats the JSON cost too.
28	
29	The wire-format design question — what shape the slot has when
30	same-language hops should stay native and JSON should only show up
31	at language borders — has not been settled in code. This issue
32	settles it.
33	
34	## Intended behavior — dual-ring slot
35	
36	**Each input slot is two ring buffers plus an ordering ring.**
37	
38	```
39	slot {
40	    ring_native  : ring of cells holding consumer-lang's native bytes
41	    ring_json    : ring of cells holding JSON bytes (cross-language values)
42	    ring_order   : ring of (which_ring, idx) tuples in arrival order
43	    ... existing peek / pop modes apply to ring_order ...
44	}
45	```
46	
47	**Producer push** (per outgoing connection):
48	
49	- If the connection's per-edge bit `output_edge_native[j]` is true,
50	  the producer's spec writes its native bytes to the consumer
51	  slot's `ring_native`.
52	- Otherwise the spec converts to JSON via `native_to_json` and
53	  writes to `ring_json`.
54	- Either way, push a `(which_ring, idx)` tuple to `ring_order`.
55	
56	**Consumer pop**:
57	
58	- Pop `ring_order` to learn which ring the next value lives in.
59	- Pop the cell at the indicated index in the indicated ring.
60	- The consumer's spec receives a `(native | json, bytes)` pair per
61	  input and picks the matching decoder.
62	
63	This shape is precise on **mixed fan-in ports**: a port fed by one
64	Lua producer and one C producer keeps the Lua values native in
65	`ring_native` and the C-translated-to-JSON values in `ring_json`.
66	The consumer pays JSON-decode cost only on cross-language cells,
67	not on the entire port. The per-port-classification alternative
68	(slot is all-JSON if any fan-in is cross-lang) is the simpler
69	fallback — strictly correct, but forces native producers to eat
70	JSON on mixed-fan-in ports. Dual-ring keeps each cell at the
71	right cost.
72	
73	### Per-language native bytes
74	
75	Each spec picks its own native byte format. The only contract is
76	internal symmetry: the spec's own native-write and native-read have
77	to round-trip its own runtime values.
78	
79	- **Lua** — bytes the spec can deserialize back to a Lua value
80	  quickly. Candidates: msgpack, `lua_dump` for closures, a custom
81	  binary form. `cjson.encode` is a valid starting point (a JSON
82	  string the Lua spec parses with `cjson.decode`); refine later if
83	  profiling shows the encode/decode is hot.
84	- **C** — `memcpy` of the typed struct. The C spec's generated
85	  wrapper (issue 307) knows the layout, so the ring cell holds the
86	  struct's bytes verbatim.
87	- **Bash** — strings. Bash has no other native form; its native
88	  ring stores string bytes directly.
89	
90	### Why JSON for the cross-language ring
91	
92	JSON's structure is self-describing — six kinds (object, array,
93	number, string, bool, null), each with an obvious mapping into
94	every language's runtime. Adding a new language K means K's spec
95	implements one pair of bridges: `native_to_json` and
96	`json_to_native`. No spec ever needs code that knows another
97	language's types. The N-language matrix collapses to N spec-pair
98	implementations, not N² pair-wise paths.
99	
100	The harder cases — closures, opaque language values, function
101	pointers — don't fit JSON's structure. JSON carries them via
102	sentinel primitives:
103	
104	- `$ref` — pointer + length + type tag for bytes in the large-value
105	  heap. In-process specs deref directly; cross-process boundaries
106	  inline the bytes at the UDS marshaller.
107	- `$function_pointer` — in-process address + signature for
108	  callables. Wire compile fails if the consumer's language can't
109	  reconstruct.
110	- `$lang_opaque` — "these bytes are language-X-internal." The
111	  destination spec either knows what to do or wire compile fails.
112	
113	First-cut behavior: `encode_value` in the Lua spec hard-errors on
114	function / userdata / thread (`langs/lua/spec.c:317`). Sentinels
115	land in a later slice.
116	
117	## Per-edge classification at graph load
118	
119	The graph loader's single-threaded phase walks every connection
120	and computes per-box arrays:
121	
122	- `input_edge_native[i]` — one bit per input port. True iff every
123	  producer feeding that port shares the consumer's language.
124	- `output_edge_native[j]` — one bit per outgoing connection. True
125	  iff the consumer on the other end shares this box's language.
126	
127	Both arrays are already declared and populated — see
128	`src/010-graph-loader.h:145` and `src/010-graph-loader.c:689-754`.
129	
130	Under dual-ring, only `output_edge_native[j]` is consulted on the
131	push path (it picks the ring at write time). The consumer-side bit
132	`input_edge_native[i]` is now informational — the actual per-cell
133	ring tag lives in `ring_order` and is precise per cell.
134	
135	The per-box `use_native_invoke` summary is **removed**. The
136	dispatch reads `ring_order` per pop and consults the cell's tag
137	directly.
138	
139	### Why per-edge, not per-box
140	
141	The earlier per-box approximation collapsed on the first
142	cross-language edge it found — a Lua box with one C neighbor went
143	JSON across all its other edges. Per-edge keeps the cost local to
144	the actual cross-language boundary. The interior of any
145	same-language island skips JSON; only the boundary edges pay. The
146	implementation cost is identical: same number of bits per box,
147	stored per-port / per-connection instead of per-box.
148	
149	Dual-ring takes precision one step further: even on a single port
150	with mixed fan-in, each individual cell is in the right ring. The
151	boundary tightens from "edges that cross languages" to "cells
152	that crossed languages."
153	
154	## Runtime dispatch
155	
156	### Input-read
157	
158	For each input port:
159	```c
160	(which_ring, idx) = ring_order_pop(slot->ring_order)
161	cell_bytes        = ring_at(which_ring == NATIVE ? slot->ring_native
162	                                                  : slot->ring_json,
163	                            idx)
164	input_data[i]   = cell_bytes
165	input_native[i] = (which_ring == NATIVE)
166	```
167	
168	`input_native[i]` is a per-input flag the spec consults to know
169	whether `input_data[i]` is its native form or JSON.
170	
171	### Spec invoke
172	
173	The spec's `invoke` signature gains per-input and per-output flags:
174	
175	```c
176	int invoke(handle, file_path, fn_name,
177	           const void **input_data, const int *input_sizes,
178	           const int *input_native, int n_inputs,
179	           int output_native,
180	           void *out_buf, int out_buf_capacity, int *out_size);
181	```
182	
183	The spec dispatches per input: if `input_native[i]`, decode as
184	native; else decode as JSON. The output is written in whatever
185	form `output_native` indicates — one form, one write. The previously
186	separate `invoke_native` and `invoke_json` callbacks collapse into
187	this single dispatching `invoke`; the per-input dispatch is what
188	the design actually needs.
189	
190	For boxes whose output fans to multiple consumers in different
191	languages, the dispatch picks one form for `output_native` based
192	on whichever ring is dominant (typically native if any
193	same-language consumer exists; the cross-language consumers eat a
194	`native_to_json` conversion at push time). The detail is a runtime
195	optimization, not a correctness concern.
196	
197	### Output-push
198	
199	```c
200	for each outgoing connection j:
201	    if output_edge_native[j]:
202	        push native_bytes      to consumer->ring_native
203	        push (NATIVE, idx)     to consumer->ring_order
204	    else:
205	        if output_was_native: json_bytes = spec->native_to_json(native_bytes)
206	        push json_bytes        to consumer->ring_json
207	        push (JSON, idx)       to consumer->ring_order
208	```
209	
210	If the spec produced native and multiple cross-language consumers
211	exist, the dispatch calls `native_to_json` once and reuses the
212	JSON bytes across every cross-language push (optimization, not
213	required for correctness).
214	
215	## Edge cases
216	
217	- **All-same-language graph**: every connection's
218	  `output_edge_native[j]` is true; `ring_json` stays unallocated
219	  across every slot; activity is on `ring_native` and `ring_order`
220	  only.
221	- **All-different-language graph**: every connection routes to
222	  `ring_json`; `ring_native` stays unallocated.
223	- **Mixed fan-in**: both rings active on the same slot. Consumer
224	  sees a mix of native and JSON cells, picked apart by
225	  `ring_order`.
226	- **Type changes mid-development**: a wire that was Lua→Lua
227	  becomes Lua→C when the user changes a downstream box's language.
228	  The graph loader recomputes the per-edge bits on the next load;
229	  no data conversion at runtime, no migration.
230	
231	## Implementation cost
232	
233	- **Slot store**: two rings per slot instead of one, plus the
234	  ordering ring. Allocate `ring_json` and `ring_order` lazily —
235	  same-language-only slots pay nothing for them. Per-cell cost:
236	  one extra ordering-ring write on push, one extra read on pop.
237	- **Spec interface**: one signature change to the `invoke`
238	  callback adding `input_native[]` and `output_native`. Every
239	  spec's `invoke` gains a per-input branch. Lua's branch is
240	  almost free — both decoders (`json_to_native` and the future
241	  native one) already exist as bridges. C and Bash need their
242	  JSON bridges written, which is shared with the broader
243	  cross-language work in 307/308.
244	- **Dispatch**: one extra step on read (consult ordering ring),
245	  one extra branch on push (which ring to write).
246	
247	The win is exactness: same-language values stay native even when
248	their port has a cross-language sibling producer.
249	
250	## Future: whole-program same-language compilation (issue 313)
251	
252	313 describes merging all same-language source into one
253	translation unit per language at compile time. The merge does
254	**not** eliminate the slot store for same-language wires — boxes
255	still run as separate tasks in the thread pool, possibly on
256	different workers, so values still need a place to wait for
257	sibling inputs to arrive. The thread pool's parallelism depends
258	on tasks being scheduled independently; collapsing same-language
259	hops to direct function calls would serialize execution and defeat
260	the pool.
261	
262	What 313 thins is the **spec invocation overhead**: for boxes in
263	the merged region, the dispatch can call the function directly on
264	the worker's `lua_State` (or compiled C code) without going
265	through dlsym + bytes-marshal + bridge. The dual-ring slot shape
266	remains; 313 makes the native-ring's read/write cheaper. The two
267	layers stack, they don't cancel.
268	
269	## Resolved design choice: JSON, not type-annotated raw bytes
270	
271	An earlier note asked whether cross-language wires should use
272	type-annotated raw byte encodings (length-prefixed ints, raw
273	floats, etc.) instead of JSON. **Decided: JSON across the board
274	for the cross-language ring.** Rationale:
275	
276	- JSON's self-describing structure makes a new language one
277	  spec-pair of `native_to_json` / `json_to_native` — no per-pair
278	  language paths.
279	- The native ring's format is each spec's private choice, so the
280	  per-language performance flexibility is there without universal
281	  type-tag complexity.
282	- Sentinel primitives in JSON (`$ref`, `$function_pointer`,
283	  `$lang_opaque`) carry the structurally awkward cases without
284	  forcing the wire format to grow new top-level shapes.
285	
286	## Suggested implementation sequence
287	
288	1. **Extend the slot data structure**. Add `ring_native`,
289	   `ring_json`, `ring_order` to the slot type in
290	   `src/009-slot-store.{c,h}`. Allocate `ring_json` and
291	   `ring_order` lazily.
292	2. **Extend the spec `invoke` signature**. Update `lang_invoke_fn`
293	   in `langs/lang-spec.h` to add `const int *input_native`,
294	   `int output_native`. Touch every spec's `invoke` stub to accept
295	   (and initially ignore) the new parameters.
296	3. **Update dispatch input-read** (`src/012-dispatch.c`). Pop
297	   `ring_order` first, then the named ring. Populate
298	   `input_native[]` per input port.
299	4. **Update dispatch output-push** (`src/012-dispatch.c`). Select
300	   the consumer's ring based on `output_edge_native[j]`. Write
301	   the ordering-ring tuple. Reuse JSON across cross-language
302	   consumers when the producer fanned to multiple.
303	5. **Lua spec** (`langs/lua/spec.c`). Branch per `input_native[i]`
304	   — decode native or JSON accordingly. Pick the native byte
305	   format (start with `cjson.encode`; refine later).
306	6. **C and Bash spec bridges**. Implement `native_to_json` and
307	   `json_to_native` for C and Bash so cross-language pushes work.
308	   C's native form is `memcpy` of the wrapper's declared struct;
309	   Bash's native form is plain strings.
310	7. **Remove `use_native_invoke`**. Drop the per-box field from
311	   `box_t` and its computation from `graph_load`. The dispatch
312	   consults the ordering ring's per-cell tag, not a per-box
313	   summary.
314	8. **Tests**. A 100k-iteration Lua→Lua pipeline uses `ring_native`
315	   exclusively. A mixed-lang pipeline exercises both rings. A
316	   mixed-fan-in port shows both rings active on one slot.
317	
318	## Relevant files
319	
320	- `src/009-slot-store.{c,h}` — slot data structure; the
321	  dual-ring extension lands here.
322	- `src/010-graph-loader.{c,h}` — per-edge classification (already
323	  done at `010-graph-loader.c:689-754`).
324	- `src/012-dispatch.{c,h}` — input-read, output-push.
325	- `langs/lang-spec.h` — `invoke` signature extension.
326	- `langs/lua/spec.c`, `langs/c/spec.c`, `langs/bash/spec.c` — per-spec
327	  implementations.
328	- `issues/302-wire-value-slot-store.md` — original single-ring
329	  slot design (completed); the dual-ring extension is the structural
330	  amendment captured here.
331	- `issues/306-lua-language-spec.md`, `307-c-language-spec.md`,
332	  `308-bash-language-spec.md` — per-spec typed marshalling lands on
333	  top of this issue.
334	- `issues/313-research-whole-program-same-language-merge.md` —
335	  follow-on that thins spec invocation for merged regions.
336	- `issues/245-wire-color-by-cross-language-classification.md` —
337	  editor-side visual reflection of the per-edge classification
338	  (green for JSON, gray for native).
339	
340	## Implementation status
341	
342	What's in place:
343	
344	- `lang_spec_t` carries `invoke_native`, `invoke_json`,
345	  `native_to_json`, `json_to_native` slots (current shape — to be
346	  collapsed to a single `invoke` with per-input/per-output flags
347	  per this issue).
348	- Lua's `native_to_json` and `json_to_native` are real
349	  (`langs/lua/spec.c:335` and `:437`).
350	- `input_edge_native[]` and `output_edge_native[]` are computed at
351	  graph load (`src/010-graph-loader.c:689-754`).
352	- Per-box `use_native_invoke` summary is computed and consulted by
353	  dispatch — to be removed.
354	- **Slice 1 complete (2026-05-21).** Dual-ring slot data structure
355	  in `src/009-slot-store.{c,h}`: `SLOT_FLAG_DUAL_RING`,
356	  `SLOT_RING_NATIVE` / `SLOT_RING_JSON` constants, three internal
357	  rings per slot (native data, JSON data, ordering) under one
358	  per-slot spinlock, new API `slot_push_native`, `slot_push_json`,
359	  `slot_pop_ordered`. Single per-slot lock makes the data+ordering
360	  write atomic so consumers see push pairs intact. On non-dual
361	  slots `slot_push_native` and `slot_pop_ordered` alias to the
362	  existing `slot_push` / `slot_pop`; `slot_push_json` returns -1.
363	  On dual slots the old single-ring API returns -1 (caller must
364	  pick a ring). `slot_has_value` and `slot_fill_count` consult
365	  the ordering ring on dual slots so spawn-on-input-ready and
366	  test introspection still work. Five new tests in
367	  `tests/009-slot-store-test.c` cover the happy paths
368	  (native-only, mixed arrival order, wraparound), the
369	  order-ring-as-binding-capacity-constraint, and the back-compat
370	  cases.
371	
372	- **Slice 2 complete (2026-05-21).** `lang_invoke_fn` typedef in
373	  `langs/lang-spec.h` gained `const int *input_native` (between
374	  `input_sizes` and `n_inputs`) and `int output_native` (after
375	  `n_inputs`, before `out_buf`). Because all three callback slots
376	  (`invoke`, `invoke_native`, `invoke_json`) share the same
377	  typedef, the change propagates automatically. Every spec's
378	  invoke function picked up the new params and currently ignores
379	  them — the per-input branching is slice 4 (Lua) and 307 / 308
380	  (C and Bash). Dispatch's `do_call_box` passes `NULL` /
381	  `0` placeholder values; slice 3 wires up the real per-cell
382	  flags from the dual-ring's ordering pop. Eleven direct
383	  call sites in the spec test files updated to the new signature.
384	- **Slice 3 complete (2026-05-21).** Call-box input ports become
385	  dual-ring slots (in `graph_attach_runtime`) when their shape
386	  permits — restrictions inherited from slice 1: not LARGE_VALUE,
387	  not TAGGED. Dispatch's `read_inputs` consults
388	  `slot_flags(slot_id)` and routes dual-ring slots through
389	  `slot_pop_ordered`, populating `input_native[i]` from the
390	  per-cell `which_ring` tag returned by the pop; single-ring
391	  slots keep the existing `slot_peek` / `slot_pop` path and
392	  derive `input_native[i]` from the per-port
393	  `input_edge_native[i]` classification. Push side
394	  (`push_one_connection`) now takes the producer's connection
395	  index, branches on the consumer slot's flags, and picks
396	  `slot_push_native` vs `slot_push_json` based on the producer's
397	  `output_edge_native[j]`. Literal pushes from
398	  `dispatch_push_literals` go through `slot_push_native` (literals
399	  are configured in the consumer's own box JSON, so semantically
400	  same-language). `do_write_box`'s `read_inputs` call passes
401	  `NULL` for the flag array since write boxes aren't spec-invoked.
402	  Added `slot_flags(s, id)` accessor to the slot store API so
403	  dispatch can branch without storing per-port bookkeeping.
404	  Existing tests stay green (22 slot-store, 14 graph-loader,
405	  7 dispatch, 11 integration including multi-language `pipeline`
406	  and `driver-test`); test fixtures that pushed directly via
407	  `slot_push` on what's now a dual-ring slot were migrated to
408	  `slot_push_native`.
409	
410	- **Slice 4 complete (2026-05-21).** Lua spec's `lua_invoke`
411	  branches per `input_native[i]`: NATIVE (or NULL slice-2 callers)
412	  push via `lua_pushlstring` exactly as before; JSON
413	  (`input_native[i] == 0`) calls a new shared `parse_json_to_stack`
414	  helper that lifts the bytes into a Lua value via the existing
415	  `decode_node`. On JSON parse failure the spec falls back to
416	  `lua_pushlstring` silently — that's the bridge to today's
417	  cross-language producers (C / Bash) which haven't started
418	  emitting real JSON yet; preserves backward compatibility while
419	  the input-format contract tightens. `lua_json_to_native` was
420	  refactored to use the same helper, just with `verbose=1` so the
421	  bridge path remains loud on parse failures (a cross-language
422	  wire that ought to carry JSON but doesn't is a real wire
423	  error). Output-side branching on `output_native` deferred —
424	  that's a coordinated change with the C and Bash specs and the
425	  dispatch's per-call output format computation; see "what's
426	  still ahead" below. Three tests in `tests/306-lua-spec-test.c`
427	  cover the new behavior: `invoke_json_input` (JSON table parses
428	  and indexes correctly), `invoke_native_input_with_flag`
429	  (NATIVE flag keeps the raw-string path), and
430	  `invoke_json_input_falls_back_to_raw` (non-JSON bytes with
431	  JSON flag fall back gracefully).
432	
433	- **Slice 4.5 (2026-05-21) — per-wire writing style + Lua JSON
434	  output.** Two coordinated halves:
435	  - **Per-wire push.** Each wire keeps its own format. The
436	    dispatch picks the consumer slot's native or JSON ring per
437	    outgoing connection based on `output_edge_native[j]`. A box
438	    with mixed fan-out (some same-lang consumers, some
439	    cross-lang) lets each consumer get the right format for its
440	    own wire.
441	  - **Lua writes JSON when any consumer is cross-language.** The
442	    spec passes `output_native = 1` to invoke if every outgoing
443	    wire is same-language, else `0`. On the `0` branch the Lua
444	    spec serializes the return via the same encoder that powers
445	    the `native_to_json` bridge, so cross-language consumers
446	    receive parseable JSON (numbers, strings, tables, arrays).
447	    On `1` the spec keeps the existing tostring coercion. C-side
448	    JSON-input acceptance landed in 307 alongside this, so a Lua
449	    → C wire with a primitive payload now round-trips through
450	    real JSON in both directions. The pipeline integration test
451	    exercises this end-to-end: Lua produces a number, JSON-
452	    encodes to bare digits, C unwraps and reads.
453	  - **Known limitation for mixed fan-out.** A producer with
454	    both same-lang and cross-lang consumers writes one format
455	    (JSON, picked by the AND of `output_edge_native[j]`). The
456	    same-lang consumers in that case receive JSON bytes through
457	    their native ring — Lua's input-side parse-with-fallback
458	    absorbs this, so primitives still round-trip. A full per-wire
459	    output (producer leaves the value in its runtime; dispatch
460	    calls `native_to_json` per cross-lang wire to materialize
461	    JSON only where needed) is a deeper contract change tracked
462	    as a future slice.
463	- **Slice 5 complete (2026-05-21).** The per-box
464	  `use_native_invoke` field and its computation are gone. The
465	  dispatch's invoke-fn selector collapsed to a single
466	  `spec->invoke` call — the per-input and per-output format flags
467	  carry the information the old `invoke_native` / `invoke_json`
468	  callback split was meant to encode. The spec interface's
469	  `invoke_native` and `invoke_json` slots remain as optional
470	  no-ops; future specs can specialize on them if profiling
471	  justifies, but the dispatch no longer requires them. Three
472	  test-side references in `tests/010-graph-loader-test.c` that
473	  asserted on the per-box bit were updated to assert on the
474	  per-edge bits they were already alongside.
475	
476	What still depends on this issue but lives elsewhere:
477	
478	- **307 / 308 — C and Bash JSON-input handling**, plus the
479	  symmetric output side. Once those land, slice 4.5's deferred
480	  Lua-side JSON write can also land and cross-language tables /
481	  structured values will round-trip.
482	- **318 — sentinel primitives**, the JSON-wire shapes that carry
483	  closures / pointers / opaque language values across the
484	  cross-language ring.
485	
486	## Open questions
487	
488	- **Lua's native byte format**: start with `cjson.encode` (still a
489	  win over the json-ring path because the consumer's spec
490	  recognises its own format and skips a parse), or jump straight
491	  to msgpack? Defer the decision; the dual-ring shape doesn't
492	  depend on the choice.
493	- **Sharing JSON across multiple cross-language consumers**: the
494	  dispatch can call `native_to_json` once and push the same bytes
495	  to N cross-language consumer slots. Worthwhile when N > 1; the
496	  push path needs a small buffer to hold the JSON across the fan
497	  loop. Implement after the basic path works.
498	- **Bash native = JSON?**: Bash has no native form distinct from
499	  strings, and JSON is already a string. Bash's native ring may
500	  hold JSON-shaped strings (effectively aliasing the JSON ring's
501	  content). Resolve by inspection once the bash spec writeup
502	  starts.
503	
```

## Assistant

_Tools:_

- `Read: /mnt/mtwo/programs/sora/soramech/issues/318-sentinel-primitives-in-cross-language-json.md`

### Tool result

```
1	# 318 — Sentinel primitives in the cross-language JSON wire
2	
3	## Status
4	open
5	
6	## Concept
7	
8	JSON's structure covers six kinds of value cleanly — object,
9	array, number, string, bool, null — and the cross-language ring
10	in the dual-ring slot (issue 312) carries JSON for any wire that
11	crosses a language boundary. The structural cases that don't fit
12	those six kinds — Lua closures, libcurl handles, C++ class
13	instances, file handles, function pointers, large byte buffers
14	where serialization would be expensive — need somewhere to live
15	in the JSON ring without forcing the wire format to grow new
16	top-level shapes.
17	
18	**Sentinel primitives** are JSON object shapes with reserved key
19	names that mean "this isn't a structural value, it's a reference
20	to something the consumer must reconstruct using language-specific
21	machinery."
22	
23	Three kinds, each solving a different structural gap:
24	
25	```json
26	{"$ref":              {"ptr": "0x...",  "len": 1048576,
27	                       "type": "bytes"}}
28	{"$function_pointer": {"addr": "0x...", "signature": "int(void*,void*)"}}
29	{"$lang_opaque":      {"lang": "lua",   "tag": 12345,
30	                       "shape": "coroutine"}}
31	```
32	
33	Sentinels are **emitted by the producer's spec** when its language
34	has a value JSON can't represent structurally. They are
35	**reconstructed by the consumer's spec** using whatever runtime
36	machinery that language provides (LuaJIT FFI, ctypes for Python,
37	direct cast for C, etc.). Wires whose consumer cannot reconstruct
38	a sentinel kind **fail at compile time** — the editor catches the
39	mismatch when the user wires a function-pointer output into a
40	Bash input.
41	
42	## Why these three, not more
43	
44	The three sentinels carve up the structural gap by ownership:
45	
46	- **`$ref`** — bytes in a shared heap. The producer wrote the
47	  bytes to the large-value heap (issue 302 / `src/015-large-value-heap.c`)
48	  and emits a pointer-and-length reference. The bytes are
49	  immutable for the wire's lifetime; refcount tracks consumers.
50	  Solves: "I have a megabyte to share; copying it through the
51	  slot would be wasteful."
52	- **`$function_pointer`** — a callable, identified by a raw
53	  address and a signature interpretable in C-function-pointer
54	  syntax. The address is process-local. The signature is the
55	  contract between producer and consumer. Solves: "I have a
56	  function I want my consumer to be able to call directly."
57	- **`$lang_opaque`** — a language-private value. The producer
58	  tags it with the language name and a worker-private reference
59	  the producer's spec can resolve back. Solves: "I have a value
60	  with no general structural representation; only my own
61	  language knows what it is."
62	
63	Anything that doesn't fit one of the three either (a) has a
64	JSON-structural representation and shouldn't be a sentinel, or
65	(b) is genuinely unrepresentable and the wire is wrong, not the
66	format.
67	
68	## Producer protocol
69	
70	Each language spec's `native_to_json` walks its native values and
71	emits the appropriate sentinel when it encounters a non-structural
72	value. The spec author writes the emit logic; it's not generic.
73	
74	### Lua
75	
76	`encode_value` in `langs/lua/spec.c:188` currently hard-errors on
77	function / userdata / thread / lightuserdata. The extension:
78	
79	- **Function** (`LUA_TFUNCTION`) — if the function is a C function
80	  registered with FFI, emit `$function_pointer` with its FFI
81	  signature; if it's a Lua function, emit `$lang_opaque` with
82	  `lang: "lua"` and a tag the spec can use to resolve the
83	  function back later (a registry index, for example).
84	- **Userdata** with an FFI ctype — emit `$function_pointer` if
85	  the type is a function pointer; `$ref` if the type is a sized
86	  buffer; `$lang_opaque` otherwise.
87	- **Coroutine** (`LUA_TTHREAD`) — `$lang_opaque` with
88	  `shape: "coroutine"`.
89	- **Large strings or tables** — at the spec's discretion, the
90	  producer may write the value to the large-value heap and emit
91	  `$ref` instead of inlining the bytes. Decision is per-call,
92	  driven by size threshold or output annotation.
93	
94	### C
95	
96	The C spec's generated wrapper (issue 307) knows the box's
97	declared output type. The wrapper emits the right sentinel
98	shape based on the type:
99	
100	- A declared `function_pointer` return — `$function_pointer`
101	  with the type's signature.
102	- A declared `bytes` return whose size exceeds the inline
103	  threshold — `$ref` with the bytes written to the heap.
104	- A declared opaque struct — `$lang_opaque` if the struct's
105	  shape is C-only.
106	
107	### Bash
108	
109	Bash has no native values besides strings; sentinels don't apply
110	on the emit side. A Bash producer always emits structural JSON
111	strings.
112	
113	## Consumer protocol
114	
115	Each spec's `json_to_native` walks the JSON tree and, when it
116	sees a sentinel key, reconstructs the value using its language's
117	machinery. The reconstruction is the spec author's responsibility.
118	
119	### Lua
120	
121	```c
122	// inside lua_json_to_native, on seeing {"$function_pointer": {...}}:
123	const char *sig = json_string_value(obj, "signature");
124	uintptr_t   addr = json_int_value(obj, "addr");
125	lua_getglobal(L, "ffi");
126	lua_getfield(L, -1, "cast");
127	lua_pushstring(L, sig);                 // "int (*)(void*, void*)"
128	lua_pushinteger(L, (lua_Integer)addr);
129	lua_call(L, 2, 1);
130	// stack top: an FFI callable Lua can invoke as compare_fn(a, b)
131	```
132	
133	`$ref` reconstruction in Lua copies the bytes (`lua_pushlstring(ptr, len)`)
134	since Lua's value model needs values in Lua's heap. The
135	producer's refcount handles the lifetime.
136	
137	`$lang_opaque` with `lang: "lua"` resolves the tag through the
138	worker's per-worker registry; with `lang != "lua"` it's a wire
139	compile error.
140	
141	### C
142	
143	Direct casts. `$function_pointer` becomes a typed function
144	pointer the wrapper can invoke. `$ref` is a `(void*, size_t)`
145	pair the wrapper hands to the user's function. `$lang_opaque`
146	with `lang: "c"` is a typed-struct pointer; other langs fail.
147	
148	### Bash
149	
150	Bash cannot reconstruct any sentinel except possibly `$ref`
151	(by reading the bytes from the file the marshaller staged at the
152	process boundary). `$function_pointer` and `$lang_opaque` fail
153	unless the producer chose the cross-process escape hatch (see
154	below).
155	
156	## Compile-time validation
157	
158	The compile step walks every wire and checks:
159	
160	- If the producer's declared output type can emit sentinel kind
161	  K (per the producer language's spec capability), then
162	- The consumer's language must be able to reconstruct sentinel
163	  kind K (per the consumer language's spec capability).
164	- If not, **the compile fails** with the specific wire and the
165	  specific sentinel kind named.
166	
167	This is editor-time feedback. The user fixes the wire
168	(reroute it, change the consumer's language, attach a custom
169	translation per issue 246) before the program ever runs.
170	
171	The per-spec capability is a static fact each spec declares.
172	Adding a new language K declares K's emit set and reconstruct
173	set; the compile validator consults the declaration.
174	
175	## Cross-process escape hatch for `$function_pointer`
176	
177	Bash (and any other process-isolated language) can't dereference
178	an in-process function-pointer address. Two options when a
179	function pointer's wire targets such a consumer:
180	
181	1. **Fail at compile time** (the default). Clear, honest.
182	2. **Producer-side compiled binary** (opt-in). When the C spec
183	   detects that a function-pointer output is wired to a
184	   cross-process consumer, it compiles the function into a
185	   standalone executable in `compiled/cross-process/<box>__<port>.bin`.
186	   The sentinel becomes:
187	
188	   ```json
189	   {"$function_pointer": {"binary": "compiled/cross-process/...",
190	                          "signature": "int(int,int)"}}
191	   ```
192	
193	   Bash invokes the binary with `$(./bin args...)` per call. Cost:
194	   process spawn per invocation (1-10 ms). The escape hatch is
195	   opt-in per box output — the user explicitly chooses the cost
196	   in the inspector.
197	
198	The default behaviour is option 1 because hiding the spawn cost
199	would mislead the user about runtime behaviour. The user picks
200	their language; the cost follows from the choice. Option 2 is
201	the escape hatch for cases where the user genuinely needs
202	C-from-Bash and accepts the cost.
203	
204	## Relationship to custom translation specs (issue 246)
205	
206	Sentinels are the **spec author's tool** for representing
207	non-structural values. Custom translations are the **user's
208	tool** for interpreting them (or any other value) when the
209	default reconstruction isn't what they want.
210	
211	The escalation path:
212	
213	1. Default spec encode / decode handles JSON-structural values
214	   transparently.
215	2. When the producer's value can't be encoded structurally, the
216	   spec author's emit logic produces the right sentinel.
217	3. When the consumer's spec can reconstruct the sentinel
218	   generically (FFI cast, ctypes, direct pointer), the default
219	   reconstruction runs.
220	4. When the user wants a different interpretation than the
221	   default (treat a function pointer as a method on a class
222	   instance, dereference a `$ref` and parse its bytes as a
223	   custom format, etc.), they attach a per-port custom
224	   translation that overrides the default reconstruction.
225	
226	So sentinels and custom translations are layered: sentinels
227	provide the *bytes that arrive at the wire* for non-structural
228	values; custom translations decide *what to do with those bytes*
229	when the default isn't enough.
230	
231	## Where in the dispatch this lives
232	
233	Sentinels live inside the JSON ring of the dual-ring slot. The
234	slot store (issue 312) doesn't know about them — it just carries
235	bytes. The producer's `native_to_json` emits them when writing
236	to the JSON ring; the consumer's `json_to_native` recognizes
237	and reconstructs them when reading from the JSON ring.
238	
239	Inside the native ring, sentinels don't appear — native values
240	are encoded in the language's own binary form, which can
241	represent its own callables / opaque values directly without
242	needing a JSON-side hack.
243	
244	## Relevant files
245	
246	- `langs/lang-spec.h` — bridges (`native_to_json`,
247	  `json_to_native`) are where sentinel handling lives per spec.
248	- `langs/lua/spec.c:188` (`encode_value`) — emit-side extension
249	  point. Currently hard-errors on function / userdata / thread;
250	  this issue replaces those errors with sentinel emission.
251	- `langs/lua/spec.c:370` (`decode_node`) — reconstruct-side
252	  extension point. Add sentinel recognition before generic
253	  object handling.
254	- `langs/c/spec.c` — C spec's `native_to_json` / `json_to_native`
255	  (currently NULL); both grow when this issue lands.
256	- `langs/bash/spec.c` — Bash spec; only `$ref` is reconstructable.
257	- `src/015-large-value-heap.c` — `$ref` storage.
258	- `scripts/soramech-compile.sh` — compile-time wire validation
259	  walks producer/consumer sentinel capabilities.
260	- `issues/312-same-language-wire-fast-path.md` — dual-ring shape;
261	  sentinels ride inside its JSON ring.
262	- `issues/246-custom-translation-specs-per-port.md` — user-side
263	  override layered on top of sentinel reconstruction.
264	- `docs/001-architecture.md` — wire format section; gains a brief
265	  sentinel reference.
266	
267	## Suggested implementation sequence
268	
269	1. **Define the canonical JSON shapes**. Three sentinel kinds,
270	   exact key names, value-object schemas. Document in
271	   `docs/001-architecture.md`.
272	2. **Lua emit**: replace the hard-errors in `encode_value` with
273	   sentinel emission per the producer protocol above. Start with
274	   `$function_pointer` for FFI callables and `$lang_opaque` for
275	   Lua functions / coroutines.
276	3. **Lua reconstruct**: extend `decode_node` to recognize the
277	   three sentinels and reconstruct them.
278	4. **Per-spec capability declarations**. Each spec declares its
279	   emit set (sentinel kinds it can produce) and reconstruct set
280	   (sentinel kinds it can consume). Lives next to the
281	   `lang_spec_t` declaration.
282	5. **C spec sentinels**. Emit via the generated wrapper based on
283	   declared output types; reconstruct via direct cast.
284	6. **Bash spec sentinels**. Only `$ref` on the reconstruct side
285	   (read bytes from the marshaller's staged file).
286	7. **Compile-time validation**. Walk every wire; check the
287	   producer's emit set against the consumer's reconstruct set;
288	   fail with a clear message on mismatch.
289	8. **Cross-process escape hatch for `$function_pointer`**.
290	   Opt-in flag in the box output declaration; the C spec emits a
291	   standalone binary instead of (or alongside) the in-process
292	   address.
293	
294	## Open questions
295	
296	- **`$ref` lifetime across iterator-fed slots**. A producer's
297	  output is referenced by an N-cell ring of consumer slots; each
298	  cell holds the `$ref`, each pop decrements. Already supported
299	  by the large-value heap's refcount, but worth verifying the
300	  test coverage when sentinels land.
301	- **Sentinel inside sentinel.** A `$ref` whose bytes are
302	  themselves JSON containing a `$function_pointer` is legal but
303	  weird. Document the recursive case; probably falls out of the
304	  walker for free.
305	- **Versioning the sentinel format**. The reserved key names
306	  (`$ref`, `$function_pointer`, `$lang_opaque`) and the
307	  value-object schemas are part of the compile-time contract.
308	  Changing them is breaking. Worth a one-line stability note
309	  somewhere.
310	- **Cross-process `$ref` semantics**. In-process `$ref` is a raw
311	  pointer + length. Cross-process (Bash via UDS), the marshaller
312	  stages the bytes at the socket boundary. Decide whether the
313	  Bash-side `$ref` reconstruction reads from a file path or
314	  inlines the bytes in the request. The latter is simpler; the
315	  former scales to bigger values.
316	- **`bytes` returns on the C spec currently passthrough raw bytes
317	  into the JSON ring.** The C spec's typed-output-JSON path
318	  (issue 307 item 2) wraps strings in JSON quotes and passes
319	  ints / doubles / bools through as valid JSON fragments — but for
320	  a declared `bytes` return it writes the raw payload verbatim,
321	  which is *not* valid JSON in the general case (non-UTF-8 bytes
322	  break parsers; embedded quotes / backslashes mangle the wire).
323	  The proper resolution belongs here, not in 307: small `bytes`
324	  outputs should be base64-wrapped into a JSON string (or its own
325	  sentinel kind), and large `bytes` outputs should follow the
326	  `$ref` path already described for the heap. Until that lands,
327	  cross-language `bytes` wires from C producers are an honest
328	  hazard — flag in the compile-time validator if possible, or at
329	  minimum document the gap so users don't wire a `bytes`
330	  C-output into a Lua / Bash consumer expecting clean JSON.
331	
```

## Assistant

I'll add a clear amendment block at the top of each issue capturing the design we just settled. I'll keep the existing slice history intact since it's an accurate record of what was built — the amendment describes the *next* target.

## Assistant

_Tools:_

- `Edit: /mnt/mtwo/programs/sora/soramech/issues/312-same-language-wire-fast-path.md`

### Tool result

```
The file /mnt/mtwo/programs/sora/soramech/issues/312-same-language-wire-fast-path.md has been updated successfully.
```

## Assistant

Now I'll add a note to the existing "Spec invoke" section to point at the amendment, so anyone reading the body knows it's been superseded.

### Tool result

```
The file /mnt/mtwo/programs/sora/soramech/issues/312-same-language-wire-fast-path.md has been updated successfully.
```

## Assistant

_Tools:_

- `Edit: /mnt/mtwo/programs/sora/soramech/issues/312-same-language-wire-fast-path.md`

## Assistant

Now 318. The function-pointer section needs the wrapper-binary + persistent-socket-subprocess design, and FFI as reconstruction has to be explicitly rejected.

## Assistant

_Tools:_

- `Edit: /mnt/mtwo/programs/sora/soramech/issues/318-sentinel-primitives-in-cross-language-json.md`

### Tool result

```
The file /mnt/mtwo/programs/sora/soramech/issues/318-sentinel-primitives-in-cross-language-json.md has been updated successfully.
```

## Assistant

Now mark the original "Cross-process escape hatch" section as superseded since the wrapper-binary approach is now the *default*, not an opt-in escape:

## Assistant

_Tools:_

- `Edit: /mnt/mtwo/programs/sora/soramech/issues/318-sentinel-primitives-in-cross-language-json.md`

### Tool result

```
The file /mnt/mtwo/programs/sora/soramech/issues/318-sentinel-primitives-in-cross-language-json.md has been updated successfully.
```

## Assistant

Same supersession marker on the consumer-protocol Lua FFI example so a future reader doesn't follow it by mistake:

### Tool result

```
The file /mnt/mtwo/programs/sora/soramech/issues/318-sentinel-primitives-in-cross-language-json.md has been updated successfully.
```

## Assistant

_Tools:_

- `Edit: /mnt/mtwo/programs/sora/soramech/issues/318-sentinel-primitives-in-cross-language-json.md`

## Assistant

Both files updated. Quick summary of what changed:

**312 — same-language wire fast path**
- New "Amendment (2026-05-22)" section at the top declaring the final-target spec contract:
  - Single `invoke` entry point; no `input_native[]` flag, no `output_native` flag, no `invoke_native` / `invoke_json` variants.
  - Dispatch normalises every input to native bytes *before* invoke (calling `json_to_native` on JSON-tagged cells).
  - Dispatch handles cross-language output by calling `native_to_json` once after invoke and fanning the JSON bytes to cross-language consumers.
  - Explicitly states: no `native_to_bytes` / `bytes_to_native` bridges; "native" is just bytes-in-slot in the spec's chosen format.
- Final-slice plan added (six concrete steps to retire the per-input flags and the dual invoke variants).
- Old "Spec invoke" section flagged as superseded — left in place so the slice history still reads coherently.

**318 — sentinel primitives**
- New "Amendment (2026-05-22)" section replacing the FFI-cast reconstruction story for `$function_pointer` with: producer's spec compiles the function to a standalone binary, consumer's spec emits a wrapper that round-trips through subprocess + Unix-domain socket. Long-lived subprocesses preserve closure state; per-call latency is socket round-trip rather than fork+exec.
- New `emit_foreign_call_wrapper` responsibility added to the spec contract.
- `$ref` and `$lang_opaque` retained unchanged.
- Explicit "why agnostic wins" rationale: LuaJIT FFI ties us to one impl, leaves Bash out, doesn't generalise — sockets work for every language on every socket-speaking OS.
- Original Lua FFI-cast code block and the "cross-process escape hatch" section both flagged as superseded.

`★ Insight ─────────────────────────────────────`
- The amendments are additive — they sit above the original text rather than replacing it — so the issue files still read as a history of how the design evolved. That matches the project rule that issue files are blueprints, not changelogs, while keeping the reasoning intact for the next reader.
- 312's amendment moves *responsibility* (format conversion) from spec to dispatch without changing the runtime data flow at all. The cells in the rings, the per-cell tags, the bridge functions — all already exist. The change is purely about which layer holds the for-loop. That's a sign the original dual-ring design was structurally right; only the labelling needed work.
- 318's amendment is the bigger philosophical commitment: it picks "agnostic and a bit slower" over "fast but pair-wise." That single choice will keep cascading — every new language added to SoraMech will be cheap to integrate because the recipe is "implement five small functions and a wrapper emitter," not "write FFI bindings to every other language."
─────────────────────────────────────────────────

Ready to implement 312's final slice when you are.
